#!/usr/bin/env python3
"""언제나 짖는 표본 — backlog_audit_selftest ★⑪ 전용.

★⑪ 은 「닫힘」이라 적힌 항목의 감사가 **실제로 짖는** 상황을 심는다. 예전에는
lint_config(당시 21건)를 빌려 썼는데, B-001 이 그 감사를 고치자 표본이 조용해져
**시험 자체가 낡았다** (2026-07-14 실측 — 눈의 시험이 남의 병에 기대면 병이 나을 때
같이 죽는다). 이 표본은 **설계상 언제나** 「위반 1건」을 말한다.

★ 종료 코드는 일부러 0 이다 — 이 저장소의 함정(위반을 보고하면서 0 을 내는 도구)을
그대로 재현한다. backlog_audit 은 종료 코드가 아니라 출력의 「위반 N건」을 읽어야
이 표본을 잡는다.
"""
print("언제나 짖는 표본 — 위반 1건 (설계상 언제나)")
raise SystemExit(0)
