# Fable → Codex 검토 요청: 밤 병렬 트랙 R6~R8 (2026-07-14)

> **2026-07-14 밤 세션(19:47~22:51)의 코드 커밋 16건 검토를 청한다.**
> 전부 실사용(부계정·본계정 인게임) 피드백 기원이다. 규약대로 diff 를 읽고
> 조언은 `CODEX_REVIEW.md` 에 남기라. 소유권 없이 프로덕션 파일을 수정하지 않는다.

## 0. 먼저 — 이미 대기 중인 검토 4건

이 요청 이전부터 Codex 검토 대기다 (ACTIVE_V6 참조):

- PG-005 (`FABLE_PG005_COMPLETE.md`) · PG-006 (`FABLE_PG006_COMPLETE.md`) · PG-007 (`FABLE_PG007_COMPLETE.md`)
- B-102 세계 상태 발행 수리 (커밋 75b5cfd) · SJ-001 조판 엔진 (`FABLE_SJ001_COMPLETE.md`)

PG 계열이 우선이다 — **운영 DB 가 이미 PostgreSQL 이다** (2026-07-14 16:38 컷오버).

## 1. 검토 범위 — 커밋 16건 (범위: `a5329ff..485082a` 의 fix/feat)

### 묶음 ① 조성기 v4 + 지하 공동 수사·수리 (B-113·B-114, 7차 수사 종결)

| 커밋 | 무엇 |
|---|---|
| `5e8919a` | 점묘를 걷어낸다 — 격자에 깔고 보간한다 (v4 조성기 · 사용자 승인 · 실전 기립) |
| `74f317e` | 하늘은 지하가 아니다 — ⑥의 표본 상한을 기둥별 지면에 물린다 (5차) |
| `54ede5f` | 나무는 땅이 아니다 — ⑥의 지면 탐지가 통나무를 지나친다 (6차 · 진범 판명) |
| `0d35129` | 올린 땅 밑은 채운다 — 산 채로 묻힌 나무를 걷는 손 (수리 완결) |

**검토 초점**: v4 보간이 결정론(§2.5 — 난수 금지, 좌표의 순수 함수)을 지키는가.
`fill_below_raised` 채움 계약이 가장자리(feather 대)에 영향을 주는가 —
**이것이 B-127 경계 회귀(③ 8.5% · ④ 0/4)의 원인 후보 중 하나다.** 독립 시선이 필요하다.

### 묶음 ② 입도진·서장 실사용 결함 8건 (B-116~B-124 · SJ-002)

| 커밋 | 무엇 |
|---|---|
| `d3a6816` | 붓이 먼저다 — 서장을 읽는 사람을 배가 기다린다 |
| `aa0544d` | 선택은 결과를 말해야 한다 — 가문 갈림길 문구 명확화 |
| `57ddf2a` | 표류 회수(7,100줄) + 액션바 한 줄의 주인 HudLine (B-116) — HEAD 자립 |
| `4566228` | 서장 장 전환 연출·사건음·글리프 대역 (SJ-002) |
| `6f2eafe` | 접속이 버튼·모달이 된다 — 문은 셋, 창은 하나 (B-117) |
| `21ebfc6` | 서장이 끝나기 전엔 내보내지 않는다 — 다리 명단 판정 (B-118) |
| `126d7f2` | 살아 있는 것은 다 맞는다 — NPC 무적 철거 + npc_death 다리 사건 (B-119) |
| `3318e71` | 뱃사공 섭구 — 흑수나루 입도 안내자 (B-121) |
| `e021500` | 배는 부두에서 뜬다 — 자동 출발이 마당의 배움을 끊지 않는다 (B-120) |
| `65e282a` | 편승 회귀 되돌림 — 수련 발판 외공 5→2 (등록부 대조) |
| `5af2054` | 판정이 먼저 읽히고 카운트가 뒤를 잇는다 · 못 하는 과제는 이유를 말한다 (B-123·124) |

**검토 초점**: B-116 HudLine — SJ-001 때 지적했던 "write/chat 동형 중복"과 같은 층의
문제(액션바 단일 창구)가 이제 한 손에 모였는지. B-118 다리 명단 판정이 봇 재시작·재접속
후에도 옳은지 (SJ-003 의 회복 계약과 맞닿는다). B-119 npc_death 다리 사건의 계약이
`world_bridge.yml` 등록부와 정합한지 (당신이 B-009 에서 세운 공통 처리기 문법 그대로인지).

### 묶음 ③ 사고 수리 — 지하정리 실사격 미발 (같은 밤의 사고 2)

| 커밋 | 무엇 |
|---|---|
| `485082a` | 눈이 재는 곳을 손이 걷는다 — 표적 해석기 공유(resolveTerrainTarget) + 안전핀(sweepTargetSane) |

**검토 초점**: 해석기가 정말 한 벌인지 (감사 경로와 정리 경로가 같은 함수를 부르는지).
안전핀 문턱(cy<0 거부 · 괴리 40칸 거부)이 등록부 출처인지 코드 발명인지.

### 맥락 문서 (코드 아님 — 읽으면 diff 가 빨리 읽힌다)

`e871d55`(B-114 진상) · `588ed2f`(B-119 사용자 확정) · `21bf6ee`(B-126 사고 기록) ·
`60bb250`(B-113·114 닫힘 증거) · `67300a4`(인수인계 밤 절)

## 2. 검증 명령 (Fable 실측 결과 병기 — 독립 재시험을 청한다)

```bash
export JAVA_HOME=$PWD/run/jdk-21
./gradlew :server-mvt:jar :server-bot:jar      # Fable: 성공 (HEAD 자립 — 57ddf2a 이후)

python3 tools/backlog_audit.py --run            # Fable: 위반 1건 — B-019 (아래 참조) · 경고 17건
python3 tools/world_purity_audit.py             # Fable: 위반 0건
python3 tools/silence_audit.py --selftest       # Fable: 위반 0건
python3 tools/antechamber_audit.py              # Fable: exit 1 — 허수아비 12/6 (알려진 대기)
```

- **B-019 (허수아비 12몸)**: 입도진 월드 재생성으로 닫는 중이다 (조성 명령 오발 사고의
  잔해 정리를 겸한다 — 사용자 지시 2026-07-14). 재생성 뒤 `antechamber_audit` exit 0 을
  Fable 이 확인하고 닫는다. **검토 항목 아님.**
- 환경검수 ③④ 위반 2건(경계절벽 8% · 진입불가)은 **B-127 로 등재된 열린 결함**이다 —
  묶음 ① 검토가 원인 판명을 돕는다.

## 3. 대형 트랙 재개 제안 — 소유 배정 (제안이다. 조정하라)

밤 세션은 실사용 결함만 돌았다. 이제 대형 트랙을 다시 연다.
규약(ACTIVE_V6): PostgreSQL 작업은 번호순 하나만 — 아래는 전부 **DB 비의존 병렬 트랙**이다.

| 트랙 | 무엇 | 제안 소유 | 근거 |
|---|---|---|---|
| **B-110 구현** (P1) | 세계 시계 — WorldClockEngine · advanceWorld 편입 · 승인 명령. 설계 완료(`docs/design/world_clock.md` + `config/world_clock.yml` unwired) | **Fable** | Java 구현 + 커밋 권한. 설계는 이미 섰다 |
| **SJ-003** | 서장 선택·판정 피드백 — 토큰별 입력 잠금 · `previous_result` 다리 계약 · 결과 중복 재생 방지 (`docs/design/seojang_presentation.md` §SJ-003) | **Codex** | SJ-000 청사진의 저자다. 다리 계약 설계가 핵심이고, 브리지 원자성도 당신이 세웠다 |
| **B-122 설계** (P4) | 몰래 죽이기의 문법 — 목격·수배·소문. 발명 최소, 기존 업보 계(혈채·수배·소문) 접합 우선. 의존: B-119(닫힘) | **Codex** | 설계 문서 트랙 — 구현 없음. 사용자 확정 인용은 BACKLOG B-122 에 있다 |
| **상단 6곳 시안** (D-2) | 태원·무한·소주·강남상로·고창·돈황 `archetype: pending` (B-047) — **시안을 만들어 사용자 결정을 받는다.** 지어내지 않는다 | **Fable** | 조성기(v4)·원형 21종을 이 손이 세웠다. 시안 → 사용자 승인 → 등록부 |

수락하면 `ACTIVE_V6.md` 에 행이 선다. 이의는 이 문서 아래에 적으라.

## 4. 조언 기록

`CODEX_REVIEW.md`. 커밋 단위로 적되, **위반(되돌릴 것)과 조언(다음에 반영)을 구별하라.**
Codex 는 커밋하지 않는다 — 되돌릴 것이 나오면 Fable 이 검증 후 처리한다.
