# 혼천(渾天) — 세션 인수인계

> **2026-07-14 작성.** 다음 세션이 **읽고 바로 일할 수 있게** 쓴다.
> 이 문서는 **지도**다. 자세한 것은 각 문서로 넘긴다. **거짓말하지 않는 것이 유일한 목표다.**

---

## 4-학습. ★★★★★★★★★★★★★★★★★★ 최신 — 2026-08-27~28 (이 절이 가장 새롭다. ★★여기서 시작하라)

**한 줄**: ★★**공간 데이터 학습 파이프라인이 섰고**(E-1~E-4 · 도구 5 · 눈 40+),
**조선풍 한옥 퓨전으로 방향이 전환**됐으며(중국 도관 폐기 · 틀·설정 유지), 코퍼스에서
캔 문법의 첫 실기동(**뜬 껍질** — 본전 대지붕 lift 2)까지 한 바퀴 돌았다.

### ★★ 다음 세션 첫 걸음
0. ★★**정본 사슬**: `docs/design/hwasan/SYSTEM_REVIEW.md` ⑥ (파이프라인 전체) ·
   `corpus_candidates.md` (★머리에 조선풍 전환 확정) · `hanok_layout_analysis.md`
   (배치 근간 5 + 지붕 얹힘 실측 + 뜬 껍질 적용 기록).
   검수 갤러리(사용자 이미지 확인): https://claude.ai/code/artifact/373dd14a-f372-48bd-ab90-06be557079b7
1. ★★**미결 안건 (사용자)**: ① **기준 사진 재정의** — D2 5지표의 기준(ref/09_honjeon.png)이
   중국풍 렌더였다. 후보: 코퍼스 실측 분포를 기준으로 / 새 조선풍 레퍼런스 / 병용.
   ② 담장 시안 3안 판정 (갤러리) ③ E-5 사전 증축 (적주 상부 어긋남 — 기록만 하기로 가닥).
2. ✅**물매 조선풍 전환 완료** (같은 날): `grandRise` 1.0 → **3/4** (화북 결정 은퇴 ·
   실기동 0.77 = 코퍼스 p50). 처마 내밈도 실측 — 저택 2 · 궁궐 3 (격이 깊이를 정한다) ·
   우리 값(3~4·2)은 정합이라 교정 불요 · ★새 문법 「지붕 끝 ≈ 마루 끝」(툇마루) 기록.
   남은 후보: 층간 지붕 0.43(완만)은 상층 몸체에 마루가 잘려 해석이 흐리다 — 보류.
3. 코퍼스 확장: 후보 장부의 한옥 계열을 사용자가 받아 오면
   `world_import`/`schem_import` → `stack_mine --auto` → `corpus_priors build` 로 분포 갱신.

### 도구 (전부 눈 있음 · tools/)
`schem_import`(schem·litematic→TSV) · `world_import`(앤빌 scan/box) · `stack_mine`(쌓임 캐기
· --auto 경계 자동) · `relation_stats`(주기·이웃·물매 — 건물 단위) · `corpus_priors`
(분포 정본 `config/corpus_priors.json` + check) · `blueprint_3d`(air·계단 컬링 병 2 수리)

### 이 세션의 확정 (사용자)
- ★★**조선풍 한옥 퓨전** — 중국 도관 레퍼런스 폐기, 틀(20구역·계약)·설정 유지
- ★★**뜬 껍질 채택 · lift 2 확정** — `roof.upper.lift`(기본 0 · 산문 동결 보호 눈) +
  `atticCeiling`. 실기동: 마루 30→32 · 상층 살창 띠가 드러남 · 눈 511/511 ·
  덮임 0. 다락 공백은 없다(속이 통짜 채움 — 정직 기록)
- 코퍼스 C-01 한옥마을(월드 1.21.11) 수입 — 상자 셋(궁궐·저택·사찰) 승인

### 함정·계율 (이 세션 것 · 전부 실증)
- ★★**air 는 칸이 아니다** — 코퍼스 TSV 의 air 행이 컬링 occupied 에 들어가 바닥·통벽이
  지워졌다 (도면시험 덤프는 air 를 안 적어 본전 도해는 옳았다). **계단·반블록도 가리지
  못한다** — 같은 병의 다른 얼굴. 눈 2 신설.
- ★★**「층에 깔림」과 「꼭대기로 흔함」은 다른 구조다** — 갈래 포장이 층 점유 30% 에
  못 미쳐 트인 땅이 1블록 몸통 9천 개로 쏟아졌다. 트인 기둥의 꼭대기 재료를 기단에 더했다.
- ★**상자가 건물로 꽉 차면 자동 경계가 지붕을 지면으로 오인한다** — 우리 덤프는 사전
  모드가 정본, --auto 는 땅 여유 상자용.
- ★파이썬 대입문은 우변을 먼저 평가한다 — `d[self.s()] = self.payload(t)` 가 NBT 를 깨뜨렸다.
- ★배운 문법의 반영은 **후보 diff·사용자 판정**을 거친다 — 자동 반영 금지 (E-5 계약).

---

## 4-정밀. ★★★★★★★★★★★★★★★★★ — 2026-08-08~12 (한 세대 전)

**한 줄**: **D2 모델링 계약 트랙** — 옛 통짜 부품(`plasterHall`·옛 `pavilion`)이 전부
도면으로 갔고(분류 동결), 「가까이서 봤을 때 얼마나 정교한가」를 계약으로 만들어
본전 정면을 레퍼런스 5지표로 수렴시키는 회차를 돌았다. 같은 트랙에서 **도구 다섯**이 섰다.
커밋 57개 (08-08 02:41 ~ 08-12 00:27) · 라이브 무접촉.

### ★★ 다음 세션 첫 걸음
0. ★★**정본 둘을 먼저 읽어라**: `docs/design/hwasan/detail_contract.md`
   (D2 모델링 계약 + 이 트랙의 작업 일지 전체 — 맨 끝이 마지막 상태다) ·
   `docs/design/hwasan/SYSTEM_REVIEW.md` (시스템 점검 + 도구 5종 + ★눈이 실패하는 세 유형).
1. **남은 어긋남 3** (detail_contract.md 맨 끝 · 레퍼런스 `ref/09_honjeon.png` 상자 대비):
   · **어두운 면 20.4% vs 45.5%** — 남은 열쇠는 문짝 재료다 (그늘·깊이로는 못 얻는다 — 실측함)
   · **붉은 면 48.6% vs 30.1%** — 붉은 목구조의 면적 자체가 과다
   · **최빈색 20.7% vs 6.7%** — 레퍼런스가 3배 잘게 쪼개져 있다. ★단, **축척의 벽**이 있다
     (아래 계율 1) — 어디까지 가능한가를 정하고 남길 것은 미해결로 남겨라.
2. **사람 눈 관문** (사용자): 5지표는 색만 재는 자다(눈 실패 유형 ③) — 최종 판정은
   테스트 서버 25566 접속 실물. 여기서 D2 계약 확정 여부가 갈린다.
3. **전파**: 확정되면 detail_contract.md 순서대로 본전 → 산문 → 생활관 → 암자 → 창고.
   그 다음이 §4-표준의 02 외원 잔여 (E-07 정자 · E-06 행각 · E-04 연무장).
4. ★★**함정**: 커밋 메시지의 `mainhall_ref.png` 는 **파일로 존재한 적이 없다** —
   시각 정본은 `docs/design/hwasan/ref/` 13장이고 본전 정면은 `09_honjeon.png` 의 상자다.
   그 이름으로 찾다 「없다」고 결론지으면 방향 없이 변화량만 재게 된다.

### 이 트랙에서 선 도구 다섯 (SYSTEM_REVIEW.md 가 정본)
| 도구 | 하는 일 |
|---|---|
| `tools/block_palette.py` → `config/block_colors.json` | 색표를 손에서 뺐다 — 클라 jar 실제 텍스처에서 1110종 (팩이 덮은 것은 팩 것) |
| 실물 덤프 (`혼천 도면시험` 뒤 자동) → `run/mvt-test/dump/*.tsv` | 조성기를 안 믿고 놓인 블록을 직접 적는다 — **블록 상태까지** (탭 구분) |
| `tools/blueprint_3d.py` + `tools/blockshape.py` | 덤프 → 등각 3D 도해. 반블록·계단·트랩도어를 **제 모양으로** (0.15초) |
| `tools/ref_measure.py` | 사진 → 치수. 레퍼런스 정면에서 칸 수·어칸:협칸 비를 수로 뽑는다 |
| `config/techniques.yml` + `tools/design.py` | 기법 그래프 — 부재(노드)·쌓임(간선)·기법·위계. 「격 강당·높이 9·5칸」 요청 한 줄로 기둥 처방이 나온다 |

### ★눈이 실패하는 세 유형 (스무 번 넘게 자를 고친 사유를 모으니 셋뿐이다)
1. **오늘의 값을 외운 자** — 도면 치수가 바뀌면 죽는다 → 규칙을 물어라 (확립됨)
2. **이름으로 구조를 고르는 자** — 팔레트가 바뀌면 죽는다 → 구조를 물어라 (확립됨)
3. ★**색만 재는 자** — 「무엇으로 읽히는가」를 못 잰다. 1·2는 헛짖지만 **3은 안 짖는다**
   (벽돌 기둥에 최고점을 줬다). 부분 답이 읽힘 자(결 방향)다 — 격자 주기·배치의 읽힘은 미해결.

### 함정·계율 (이 트랙 것 · 전부 실증)
1. ★★**축척의 벽** — 31칸 폭·블록 1칸 최소에서는 「고운 세로 리듬」과 「큰 판」을 동시에
   못 가진다. 문선을 넣자 지표 둘이 나빠졌고, **칸을 넓혀서**(주기 4→6 · 7칸→5칸) 풀었다.
   레퍼런스 건물은 사실상 더 큰 블록 축척에 있다 — 밀도를 그대로 베끼려 들지 마라.
2. ★★**어두움은 그늘이 아니라 재료다** — 레퍼런스 벽은 아래로 갈수록 어둡다 (처마 그늘이면
   반대다). 깊이·툇간으로는 그 어두움을 못 얻는다. **재기 전에 알았다** — 한 회차를 아꼈다.
3. ★★**도면에 없는 것이 화면의 절반을 차지할 수 있다** — 살창 뒤판(조성기가 자동으로 놓음)이
   정면에서 가장 밝은 면이었다. 덤프와 3D 가 필요한 까닭이 이것이다.
4. ★**레퍼런스가 조사를 이긴다** — 웹 조사(단청·산중 도관)로 청색을 넣었다가 철회했다.
   조사는 **형태만**, 색·재료는 레퍼런스가 정한다.
5. ★**색을 맞추려다 재료를 잃지 마라** — 기둥을 「짙은 주칠」로 갈았다가 red_terracotta 로
   되돌렸다. 색표 수치보다 재료의 정체성(민판·결)이 먼저다.
6. **우연은 계약이 아니다** — 개구 폭 3+문설주가 마침 주기 4 와 같아 눈이 통과하고 있었다.
   주기를 6 으로 넓히자 깨졌다. 자가 재는 것이 규칙인지 우연인지 확인하라.

---

## 4-표준. ★★★★★★★★★★★★★★★★ — 2026-08-06 (한 세대 전)

**한 줄**: **산문이 승인·동결됐다.** 화산파 공통 건축 언어를 뽑을 첫 정본이다.
다음은 산문을 더 고치는 것이 **아니라** ② 외원/입구 광장 + 산문 주변 절벽·정원이다.

### ★★ 다음 세션 첫 걸음
0. ★★★**먼저 읽어라**: `docs/design/hwasan/contract.md` — **새 구역이 반드시 따라야 하는
   공통 계약 한 장** (2026-08-07 · 바닥·축·행각·정자·지붕·인스턴스·투영 비움·표면 소유권).
   맨 아래 「새 구역을 세울 때의 순서」가 다음 회차의 첫 걸음이다.
1. **그다음**: `docs/design/hwasan/README.md` (체계·시선 고정) ·
   `docs/design/hwasan/module.md` (공통 모듈 + 갈리는 숫자) ·
   `docs/design/hwasan/01_산문/` (도면 5종 — 첫 정본) ·
   `config/blueprints/hwasan_gate.yml` (좌표의 정본 · `design_intent` 절을 꼭 읽어라)
2. **산문은 동결이다.** 치수·좌표를 건드리지 마라 — selftest 에 「★동결」 눈 5건이 박혀 있다.
   바꿀 일이 생기면 **그 눈을 고치는 것이 곧 결정의 기록**이다.
3. **다음 일**: `02_외원` — E-01·02·03·08 은 닫혔다 (BACKLOG B-196 §02 외원).
   남은 것: **E-07 정자 문법**(아직 옛 언어) · **E-06 종문·중정 행각** · **E-04 연무장 모래**.
   ★도면 기계가 이제 **자리·방향(`instances`)** · **위계(`rank`)** · **쓰임(`usage`)** ·
   지붕 문법 **셋**을 안다 — 다음 구역은 이 문법을 쓰면 된다:
   `low_gable`(행각·생활 부속) · `hip_pyramid`(정자·망루) · `sweep`(핵심 전각).
   ★**망루(11)도 사모로 바꿨다** (2026-08-07) — 같은 `hip_pyramid` 를 tower 비례로.
   지붕 문법 셋이 실물에서 다 돈다: 행각(low_gable) · 정자/망루(hip_pyramid) · 산문/본전(sweep).
4. **사용자 결정 대기 1건**: 문 앞을 가리는 것은 소나무가 아니라 **바위 기둥**이다 (아래 ㉧).
   ㉦(깃대·등롱)는 **닫혔다** — 문전을 비웠다.

### 재현 (테스트 서버 25566 · RCON 25576 · 라이브 무접촉)
`혼천 산세시험 hwasan` → `혼천 산군시험 hwasan` → `혼천 캠퍼스시험 hwasan` →
`혼천 도면시험 hwasan_gate hwasan` · `혼천 도면시험 hwasan_honjeon hwasan` ·
`혼천 도면시험 hwasan_outer_corridor hwasan` (★02 외원 행각 — 한 도면이 네 모듈) ·
`혼천 도면시험 hwasan_pavilion hwasan` (★02 외원 정자 — 사모지붕)

촬영: `python3 scripts/stage_shot.py sanse_test_hwasan overworld "이름=x,y,z,yaw,pitch"`
★같은 좌표로 회귀 검사한다: 산문 정면 `(-7,-9,203) yaw -172.6 pitch -7.4` ·
목표 구도 `(-34,22,258) yaw 197 pitch 30` · 본전 정면 `(1,63,102) yaw 180 pitch -2.7`
★★촬영 뒤 **클라 회수**는 스크립트가 `finally` 에서 하지만 **출력을 확인하라**
 (2026-08-04 에 헤드리스 클라 24개가 램 120GB 를 먹어 세션·테스트 서버가 함께 죽었다).

★도면 yml 과 jar 는 **짝으로** 배포한다. ★테스트 서버 리소스팩은 꺼 뒀다 (바닐라 판정).

---

### ★★★★2026-08-06 — 이 세션이 남긴 것

#### ㉠ 승인·동결 (사용자)
```
B-196 산문 구조: 승인      치수와 좌표: 동결      입면 판독 문제: 해결
lint 체계: 승인            최종 색·재료: 리소스팩 적용 후로 보류
목표 사진 재현도: 아직 외관·조경 단계 필요
```
동결값 — 중앙 통로 7 · 적주 간격 3·3·5·(7)·5·3·3 · 정면 좌우 대칭 ·
계단 폭 입구 9 → 전이 참 11 → 문 앞 7 · 표고 h46→h148 (총 102칸).

★**표고는 콘셉트 이미지 수치(Y90→Y280)로 안 올린다.** 사용자 판단: 그 수치는 설계 치수가
아니라 과장된 시각 비례다. 올리면 계단·석축·다리·동선이 전부 연쇄로 바뀐다 (척도를 한 번
되돌린 전례가 있다). 웅장함은 **주변 산세**로 낸다.

#### ㉡ 새 계율 넷
1. **도면의 칸수는 화면의 면적이 아니다.** 트랩도어 3/16 · 울타리 2/16 은 한 칸을 차지하고도
   화면의 5분의 1만 덮는다. 얇은 블록은 **가시 면적을 따로 계산**하라.
   → D-39 를 세 회차 오진한 원인. 도면 22.6% 가 화면에서 5.1% 였다.
2. **얇은 살 뒤에 밝은 벽을 두지 마라.** 살 사이로 비쳐 칸 전체가 그 벽 색으로 읽힌다.
   문짝 뒷줄을 어두운 판으로 바꾸자 화면 목재가 5.1% → 31.5% 가 됐다.
3. **온기는 재료가 아니라 햇빛이다** (밝기·색온도 상관 +0.94). 「웜톤 석재」라며 섞은
   사암·점적석이 곧 석축 얼룩·월대 바둑판이었다. 한 면에는 한 재료.
4. **신고표가 실물보다 넓으면 눈이 헛것을 지킨다.** `palette()` 에 이미 폐기한 재료가
   남아 「표에 있다」로 통과하고 있었다. 신고와 실물을 맞추는 눈을 세웠다.

#### ㉢ 레퍼런스의 정체 — 실측으로 확정
진짜 마인크래프트 렌더는 같은 블록이 **픽셀까지 똑같은 조각**으로 되풀이된다. 무작위 조각
420개를 견주면 우리 스크린샷은 「거의 같음」 쌍이 64·5 건인데 **레퍼런스는 0 건**이다
(파일명도 `ChatGPT Image`, 고유색 272,001).
→ **「어떤 블록을 썼나」에는 답이 없다 — 블록을 쓴 적이 없다.**
읽을 수 있는 것은 치수·비율·재료의 **수**·색 계열·배치 리듬·실루엣뿐이다.
우리가 고른 블록은 「레퍼런스가 쓴 블록」이 아니라 **「색·비율에 가장 가까운 바닐라 블록」**이다.

#### ㉣ 형태 — 원뿔을 버리고 대(臺)로
옛 공식 `crest·(1−de)^0.55` 가 봉우리를 전부 고깔로 만들고 있었다 (레퍼런스에는 원뿔이 없다).
`SpireField.mesaProfile` 로 갈았다: 마루 de≤0.55 평평 → 벽 ~0.78 에서 높이의 85% 낙차 → 애추.
그리고 `SpireField.jointed` 로 **1칸 폭 주상절리**를 심었다 (레퍼런스 실측: 기둥 폭 1칸 ·
이웃 마루 높이차 2~6 · 둘셋이 무리 짓는다). 산군과 본산이 **같은 함수**를 부른다.

#### ㉤ 도면 5종을 정본에서 뽑는다
`tools/blueprint_draw.py` 가 `config/blueprints/*.yml` 에서 정면도·측면도·평면도·지붕 층별·
**센** 팔레트·입면 lint 를 뽑는다. 손으로 안 그리므로 **도면과 실물이 갈라질 수 없다.**
★`.schem` 을 최종 산출물로 삼지 않는다 — 이진이라 `git diff` 가 안 되고 눈이 못 읽는다.

#### ㉥ 눈의 자를 옮긴 사례 (문턱을 낮춘 것이 아니다)
- 「축대 ≥ 산면 × 0.5」: 산면이 매끈하던 시절의 자였다. 주상절리로 산면이 거칠어지자 같은 식이
  축대더러 절벽만큼 깨지라고 요구했다 → 관계를 뒤집었다 (**산면 > 축대**).
- 「배후봉 ±20 에서 능선꼴」: 마루가 ±24 까지 평평해져 226 vs 228 로 구분 불가 →
  **마루 끝을 눈이 스스로 찾아** 그 밖에서 잰다 (±30 에서 장축 227 vs 급벽 70).
- 「최고점」을 상자 300~380 에서 찾았는데 진짜 꼭대기는 그 서쪽 → 상자 모서리를 재고 있었다.
★가르는 자: **모양이 틀렸나, 자가 틀렸나.** 실물을 먼저 재고 나서 눈을 고쳤다.

#### ㉦ 문전 비움 — **닫혔다** (사용자 확정 2026-08-06)
사용자 진단이 진단을 바꿨다: **문제는 보행 폭이 아니라 문간의 시각 폭이다.** 통로를 7 로
넓혔는데도 정면에서는 「깃대|적주|문살|통로|적주|등롱」으로 읽혀 다시 5칸으로 압축돼 보인다 —
독립 수직물이 적주보다 **앞에** 서서 문루의 위계를 나눠 먹기 때문이다. **넓혀서는 못 고친다.**
```
문 앞 i 0~17 : 깃대 금지 · 높은 독립 등롱 금지 · 낮은 석등(3칸) 한 쌍만 (좌우 대칭·난간 부착)
깃대 첫 자리 : 11칸 전이 참(i18~21)의 외곽 모서리 ±8   (참의 난간 ±6 밖)
i22~         : 깃대·등롱 리듬 유지 (주기 9·11 그대로 — 자리만 옮겼다)
소나무 줄기  : 문전 구간에서 ±12 → ±16 (산문 정면 투영 밖)
```
★역할을 갈랐다: **깃대는 계단의 리듬**, **문 앞 조명은 건축에 붙인다.**
눈 12건 신설 (`문전 비움` · `정면 투영`) — 262/262. **변이 시험**으로 짖는 것을 확인했다.

#### ㉧ 문 앞을 가리던 것은 **소나무가 아니라 바위 기둥**이었다 — 깎았다 (사용자 확정)
사진의 「오른쪽 큰 줄기」를 실측했더니 우리 소나무가 아니었다:
**축선 +5 · i 4~9 의 주상절리 돌기둥**(`minecraft:stone`)이 포장(y−15)에서 **y+8** 까지 서고
(산문 지붕 마루 y+2 — **지붕보다 6칸 높다**) 꼭대기의 산군 소나무가 통로 위까지 수관을 드리웠다.
★**까닭**: 산세·산군이 먼저 서고 캠퍼스가 뒤에 깎는데 `clearAbove` 는 **제 보행 폭만** 비운다.
난간 한 칸 밖의 20칸 바위는 아무도 건드리지 않았다.
→ 사용자 확정: **정면 투영을 통째로 깎는다.** `clearHalf(i)` 가 문전 구간에서 ±14 를 표고까지
비운다 (그 밖은 종전대로 보행면+난간만 — 계단 곁 절벽·숲은 그대로다).

★★**그리고 한 번 더 물렸다**: 바위를 걷어내자 **그 자리에 조경 소나무가 남았다** —
캠퍼스 조경은 접근로 포장 **뒤에** 심으므로 깎아 놓은 자리에 도로 꽂는다 (산문 자리
축선 ±5·6). **비우는 손과 심는 손이 같은 표를 읽어야 한다** → `inGateFacade` 를 두 손이
함께 읽고, 산문 조경 소나무 자리를 ±16 으로 밀었다.
★계율: **가리는 것은 재료를 안 가린다.** 「나무가 건축을 가리지 않는다」는 계약이 **나무만**
보고 있었다 — 바위도, 조경도, 소품도 같은 투영을 지켜야 한다.

증거: `run/stage_render/산문정면.png` (정면이 통째로 읽힌다) · `문전평면.png`

#### ㉨ ★★`facade_projection_clearance` — **상위 계약으로 승격** (사용자 2026-08-06)
사용자 승인 회차에서 승격됐다: 「화산파 **전체 생성기의 공통 규칙**으로 승격할 만하다.
`tree_clearance` 보다 의미가 넓은 이름이 적합하다.」 이름을 계약의 넓이에 맞췄다.
```yaml
facade_projection_clearance:      # 정본 문서: config/blueprints/hwasan_gate.yml
  half_width: 14   depth: 18   standoff: 16   clear_to_elevation: true
  forbidden: [terrain, mountain_foliage, campus_tree, tall_prop, banner_pole]
  allowed:   [low_lantern, railing, attached_fixture]
```
- **숫자는 한 곳에만 산다** — `TerraceForge.FACADE_CLEAR_HALF·FACADE_CLEAR_DEPTH·FACADE_STANDOFF`.
  생성기·조경·산군 식생 제외·눈이 전부 이 이름을 부른다 (±16 을 두 번 적던 `+2` 도 없앴다).
- **문서가 조용히 늙지 않게** 눈이 yml 의 세 값과 코드를 **대조**한다 (변이 시험으로 확인).
- **산군 식생 제외**가 이제 같은 상자(`facadeBox`)를 읽는다 — 「먼저 심고 나중에 깎는다」에
  기대지 않는다 (조성 순서가 한 번 바뀌면 조용히 되살아나므로). 제외 사각 39 → **40**.
- ★**02 외원을 비롯한 어느 구역도 침범할 수 없다** — 전 구역(20)의 조경·소품 자리를 눈이
  이 상자로 훑는다. **외원은 빈 문전을 채우는 공간이 아니라 그 비어 있는 중앙축의 양옆을
  구성하는 공간이다** (사용자 확정). 외원이 들어올 때 이 눈이 먼저 짖는다.

#### ㉩ 최종 판정 (사용자 2026-08-06)
```
01 산문 구조: 동결 유지     문전 투영 방해: 해결
지형·조경 생성 순서 충돌: 해결     깃대와 조명 역할 분리: 완료
회귀 검수: 충분             추가 산문 수정: 불필요
```

---

## 4-도면. 2026-08-04~05 (한 세대 전 — 오염 사건과 도면 문법의 확립)

**한 줄**: B-196 화산파가 **목표 사진 한 장(1호)에 고정**되고, **설계도가 좌표의 정본**인
구조로 전환됐다. 도면이 코드의 오해를 연달아 잡아내는 중이다.

### ★★ 다음 세션 첫 걸음
1. **정본 셋을 먼저 읽어라**: `docs/design/hwasan_target_diff.md` (목표·차이 목록 D-01~36) ·
   `config/blueprints/hwasan_gate.yml`·`hwasan_honjeon.yml` (도면 — **좌표의 정본**) ·
   `docs/design/hwasan_gap_review_20260804b.md` (진단과 미채택 기록)
2. **D-34 부터** — 본전 하층 회벽이 목표보다 넓다. 7호를 다시 재서 도면을 고쳐라
   (「하층은 넓은 회벽」 실측이 다른 각도를 본 결과일 수 있다).
3. 그 다음: D-35 망루 위계 · D-36 처마가 벽을 덮음 · D-33 본전 폭(패드 재설계 — 사용자 결정) ·
   성벽·광장 도면화 · GREEN/PROPS 되살리기 · 산군의 시야 회랑.

### 재현 (테스트 서버 · 라이브 무접촉)
`혼천 산세시험 hwasan` → `혼천 산군시험 hwasan` → `혼천 캠퍼스시험 hwasan` →
`혼천 도면시험 hwasan_gate hwasan` · `혼천 도면시험 hwasan_honjeon hwasan`
★도면 yml 과 jar 는 **짝으로** 배포한다 (yml 만 넣으면 옛 jar 가 새 필드를 무시하고 조용히
틀린 것을 세운다). ★테스트 서버 리소스팩은 꺼 뒀다 (바닐라 그대로 보려고).

### ★★★★2026-08-05 밤 — 오염 사건 (가장 비싼 교훈)

세 회차 동안 「본전 회벽이 넓다」(D-34)를 고쳐 왔는데 **전부 오진**이었다. 진범 둘:
1. `캠퍼스시험`이 먼저 통짜 본전을 세우고 `도면시험`이 그 위에 도면을 얹는데,
   **BlueprintBuilder 가 빈 칸(.)을 안 비워** 앞 건물이 도면 껍데기 **안에 남아 있었다**.
2. 트랩도어를 setType 으로 놓으면 **바닥에 눕는다**(half=bottom) — 칸이 뚫려 그 너머가 비쳤고
   그 밝은 면이 「회벽」으로 읽혔다.

→ **우리가 재던 것은 도면이 아니라 두 건물이 겹친 자리였다.** D-38 은 통째로 허상이었다.
★★**계율: 판정하기 전에 판정 대상이 오염됐는지 먼저 확인하라.** 특히 **두 조성이 같은 자리를
덮는 순서**가 있으면 뒤엣것이 앞엣것을 완전히 지우는지 확인해야 한다.
★★**산문·망루도 같은 오염을 겪었을 수 있다** (조성 순서가 같다) — 오염 이전 판정은 재판정 대상.

★함께 얻은 것: **비율에 문턱을 두는 눈은 위험하다.** 「붉은 ≥50·회벽 ≤35」로 적었더니 고치기
전 상태가 그 문턱을 통과했다 (칸을 세는 자와 화소를 재는 자가 다르다). 문턱을 조여 통과시키면
**오늘의 처방을 지키는 눈**이지 실측을 지키는 눈이 아니다.

### ★★★이 구간의 계율 (전부 실증)
1. **색은 재고 고른다** — 눈짐작으로 세 번 틀렸다 (금빛·배너·회벽).
2. **「튀지 않게」와 「안 보이게」는 다르다** — 형태가 사라지면 그 요소는 없는 것과 같다.
3. **가리는 정도는 높이가 아니라 두께가 정한다** · **통행의 폭과 시야의 폭은 다르다**.
4. ★**팩이 덮는 재료로 색을 판단하지 마라** — 「흰 벽」이라 여긴 것이 팩 텍스처였고 바닐라는
   살구색이었다. 팩 대조표는 커밋 28aa569 에 있다.
5. ★★**픽셀은 섞이지만 블록은 안 섞인다** — 두 재료 혼합의 채도 중간대가 1%(바둑판).
   평균을 노리지 말고 **단일 재료**로. 눈도 「평균」이 아니라 「가짓수」를 재라.
6. ★★**AI 두 대가 일치해도 사용자의 눈을 대신하지 못한다** — 클로드·코덱스가 독립 판정에서
   둘 다 「건물이 크다」로 빗나갔고 표적은 산이었다.
7. ★**눈이 조용히 거짓말한다** — 상자를 6원소로 읽던 눈(실은 4원소)·yml 글자를 찾던 눈이
   아무것도 안 재면서 통과했다. **뮤테이션 없이 눈을 믿지 마라.**
8. ★**촬영 클라는 반드시 회수** (`ps | grep -F mcdata | grep jdk-21` → PID kill).
   24개 누수가 램 120GB 를 먹어 기계를 죽인 적 있다 (2026-08-04).

---

## 4-화산. ★★★★★★★★★★★★★★ 최신 — 2026-08-02~03 (이 절이 가장 새롭다. ★★여기서 시작하라)

**한 줄**: ★★**B-196 화산파 건축 강화** — 사용자 레퍼런스 13장(마스터플랜 포함)을 정본으로
시험 월드(`sanse_test_hwasan`)에 **산군 속 화산파 캠퍼스**를 세웠다. 슬라이스 9개(테라스→
건물→곁봉·운무교→조경→실측 재구성→산군→건물 형태→척도 교정→산이 건축이 되게) · 실기동
빨간펜 ~20회 · 커밋 ~20개 · selftest 19→**80종** · **라이브는 내내 무접촉**.

### ★★ 다음 세션 첫 걸음
1. **사람 눈 재검** (사용자): 테스트 서버 `127.0.0.1:25566` 접속 → `/tp -4 -14 540` (접근로
   남단)에서 북쪽으로 걸어 오르기 — 대계단→참→소문→산문 도착 시퀀스가 이번 트랙의 본체.
   갤러리: https://claude.ai/code/artifact/ef392c2d-bb45-44f7-89c1-1293654eb554
2. 재현 명령 (테스트 서버 콘솔함): `혼천 산세시험 hwasan` (~250초) → `혼천 산군시험 hwasan`
   (~560초) → `혼천 캠퍼스시험 hwasan` (~30초). 전부 결정론·멱등 · 검수 눈이 끝에 소리친다.
3. 남은 것: 전각 캔틸레버 (실측표 §10-b — 접지 계약 새 범주 필요) · 접근 회랑 폭 상향 여부
   (침봉이 구도를 좁힘 — 위압 vs 트임, 사용자 판단) · **본세계 재조성 회차** (build_radius
   64→128 등록부 개정 포함 — 시안 §6) · 리소스팩 연출 (사용자 확정: 후속).

### 정본·계율 (이 트랙 것)
- 정본 사슬: `docs/design/hwasan_build_enhancement_v1.md` (시안 v2 — 20구역 마스터플랜) ·
  `hwasan_block_measurements.md` (★실측표 — **대계단 20 이 사용자 확정 기준자**) ·
  `hwasan_codex_review_20260803.md` (독립 검토 — 「산이 건축이 되게」 진단 5).
- ★★★**렌더 실측은 확대 크롭으로** — 2×2 픽셀 뭉치를 1블록으로 세면 전부 절반 척도가 된다
  (사용자가 눈으로 세서 잡았다). 여장 이빨·석등·디딤이 세는 자다.
- ★★★**웅장함 = 인간 단위의 누적** — 문·기둥 간격을 키우면 오히려 작아 보인다 (코덱스 진단
  ⑤). 몸체는 실측대로 크게, 개구·창·칸 리듬은 사람 크기로.
- ★★**상자와 실물이 별도 손으로 적히면 어긋난다** — 마른 조성(parts 목록 = 조성·검수 상자
  공동 정본)이 그 처방. 이 트랙 빨간펜의 절반이 이 병이었다.
- ★★기준면 프로브는 산군 필드 밖 정본 한 곳 (SpireField.PROBE_X — 다섯 명령이 한 함수).
  프로브가 지형에 덮이면 캠퍼스가 통째로 떠서 앉는다 (실제 54칸 떴었다).
- ★테스트 서버 view-distance 6→16 (96칸 밖을 안 보내던 진짜 벽 — 접속해도 산이 안 보였다).
- ★헤드리스 촬영 tp 는 `execute in minecraft:sanse_test_hwasan run tp` + Dimension 확인.
- ★★★**촬영 클라는 반드시 회수한다** (2026-08-04 실증 — 기계가 죽었다): kigi 관례가
  「클라를 띄운 채 둔다」였는데, 촬영 회차마다 하나씩 남아 **24개가 램 120GB + 스왑 전량을
  소진**해 세션·테스트 서버가 사망했다. 최고령이 1일 21시간째였다. **촬영이 끝나면
  `pkill -f "username kigibot"` 로 거둔다** — 클라는 무상태라 잃을 것이 없다.
  ★징후: 라이브의 「s / 3.0ms」 계측 경고가 실은 스왑 스래싱일 수 있다 (코드 성능 문제로
  오진하지 마라). 세션이 끊기면 코드보다 `free -h` 를 먼저 보라.

---

## 4-무대. ★★★★★★★★★★★★★ 최신 — 2026-07-31~08-02 (이 절이 가장 새롭다. ★★여기서 시작하라)

**한 줄**: **B-190(세계관→기계) 닫힘** → 전체 배포 → 튜토리얼 전수 조사(장부 밖의 빚 셋 발굴)
→ B-191 입문식·B-187① 배선 → ★★**B-194 체험형 서장(B안) 파일럿** — 3D 공간 인식
파이프라인을 세우고, 「그날 밤」 무대를 짓고, 생명(넋등 점등·행동 감지)을 넣고,
**실측 빨간펜 9회**를 돌았다. 사용자가 「잘 모르겠다」로 마감 — 9호 실측 확인이 미완이다.
라이브: MVT Done 29.5s · ERROR 0 · 봇 PID 136970 (7/31 3차 재기동분 — 이후 봇 변경 없음).

### ★★ 다음 세션 첫 걸음 (위에서부터)
1. **B-194 체험 9호 실측 확인** (사용자): `/혼천 서장무대 체험` — ①식구=이불 속 흰 둔덕이
   보이는가(눈 뜨면 그쪽을 본다) ②잠든 숨소리 ③머리맡 우클릭=깨우기. ★식구는 이제
   **엔티티가 아니라 형태다** (주민 NPC 폐기 — 9호).
2. ★★**소리 회차 (본체)** — 사용자 판정 「전혀 느껴지는 게 없다」의 처방: ffmpeg 으로
   심장박동·말발굽 크레셴도·바람 ogg **합성** → 팩 수록(sound_channels 문법) → 리듬 재구성
   (개 짖음 → **정적 두 박자** → 말발굽이 커진다 → 심장박동). 합성 파이프는
   video-reference(yt-dlp+ffmpeg)가 이미 개통돼 있다.
3. B-194 잔여: 발단별 연출 7종(지금 근사음 4종뿐) · **실배선**(항해 정거장→무대→seojang_choice —
   통로는 기존 다리 재사용) · 2인 동시 실측 · 글리프 제목 카드·모델 소품(디자인 사다리).
4. **B-195 방향 결정 (사용자)**: 가구_3D 유령 벽 — ①비폐색 블록 이사 ②BetterModel 디스플레이
   이사(깨끗·공사 큼) ③「가구는 벽에서 한 칸」 배치 계율 수용.
5. 밀린 것: 【제안】 수치 빨간펜(B-190 — 노선 임계 0.5·맹_전력 4+1·NPC 붙듦 3·지목=최초
   통과자·자릿세 의뢰 문안) · B-191 인게임 1회(닫는 조건) · B-193(입문식 이후 — ② 무공
   튜토리얼 본체) · 화산 루트 종단(태스크 #7 — 사용자가 ①인지 튜토리얼 우선으로 보류).

### 이 세션에서 선 것 (전부 커밋 · 라이브)
| 무엇 | 핵심 |
|---|---|
| ★★★B-190 닫힘 | 해소 그릇(막해소:) · 노선 집계(개인_노선→연대_폭) · 산술 폴백(침공 vs 전력) · 자리 판독(npc_fallback 첫 배선) · 엔딩 판정(wiring_status 배선) · 레이드 통로(raid_resolved+/혼천 레이드해소) · 천마 루트(접촉 DM→기명각 전량 버림→시험돌 1만→지목→자리:천마) · 눈 ⑥+④-c · selftest 30/30 |
| 막별 목적·경지 창 | 전 막 개인_목적+경지_창(1막 삼류~이류 → 5막 화경) · ★설립전 박 신설 · ★★순서 교정: **자릿세는 맹보다 먼저 있었다** — 창설 이유 세 겹(힘·기억·명분), 명분만 배신당한다 |
| 배포 | 봇 3회(토큰 선확보 프로토콜)·MVT 십수 회 재배포 — 전부 라이브 |
| 튜토리얼 전수 조사 | ①인지 튜토리얼(접속~입문식)과 ②무공 튜토리얼(입문 후 — B-193)로 갈림(사용자). 장부 밖의 빚 발굴: B-191(입문식 — 결승선 부재)·B-192(undecided)·B-193(사문이 아무것도 안 연다) |
| B-191 입문식 | 심사 합격 패에 [입문식] — 사문=hwasan·겸적 가드·tutorial_audit 결승선 눈. 인게임 1회 남음 |
| B-187 ① | 뽑기 풀 눈(start_regions 첫 독자) + 탄생 임베드 운명 뽑기 가시화 |
| ★★★3D 공간 인식 | 「사진·좌표 왕복」 폐기: **도면이 좌표의 정본**(config/stages/*.stage.yml) · 렌더 3종(평면·아이소·분위기) · **1인칭 POV**(stage_pov — AI 가 직접 본다) · **공간덤프**(/혼천 서장무대 덤프 — 콘솔 가능 + stage_dump_diff) |
| B-194 무대+생명 | 「그날 밤」(30×22×7층 — 마당집·우진각·담장 틈·화광) 라이브 · SeojangStagePlay(넋등 자리 점등·웅크림/응시/우클릭 감지·발단 소리·veil 격리·개인 자정·60초 폴백) · /혼천 서장무대 체험 |
| 빨간펜 9회 | 밤눈·인과 재배열(소리가 먼저→생각이 자리를 가리킨다→물음은 타이틀)·시간차 내레이션·소품·고아 청소·유령 벽 추적(청크 재전송 무효→light 누명→★진범=가구_3D barrel — 사용자 재현 확정)·식구=형태 전환 |
| B-195 신설 | 가구_3D 유령 벽 — 컬링은 모델이 아니라 블록 종류로 판정한다 |

### 함정·계율 (이 세션 것)
- ★★★**selftest 가 타임아웃에 죽으면 변이가 남는다** — backlog_audit --run 을 3분 묶음에 넣었다가
  PackPusher 변이가 남아 「정체불명 수정」으로 나타났다. 감사류는 단독·넉넉한 타임아웃,
  죽은 감사 뒤엔 git status (메모리에도 박음).
- ★★★**눈 없이 배포 금지** — 등록부 따옴표 하나 빠져 **플러그인 전체가 못 떴다** (YAML 파스
  한 줄이면 잡혔다). 배포 전 최소: yaml.safe_load + 해당 눈.
- ★★★**가구_3D 의 유령 벽**: 마크 면 컬링은 모델이 아니라 **블록 종류**로 판정 — 불투명
  큐브(barrel 등)를 비큐브 가구 모델로 덮으면 이웃 벽이 뚫려 보인다. **무대에 barrel 금지**
  (chest 로 대체). 근본 수리는 B-195. ★수사 경위 교훈: 청크 재전송(무효)·light 블록(누명)을
  거쳐 사용자의 「통을 직접 설치」 재현이 결정타였다 — **재현 가능한 최소 실험이 스샷 열 장보다 낫다.**
- ★★**공간덤프가 사진 왕복을 대체한다** — 「뚫려 보인다」는 이제 덤프+diff 로 몇 초 만에
  블록/엔티티/시각 문제를 가른다 (첫 실측: 블록 어긋남 0종 · 보이지 않는 고아 주민 둘).
- ★★**형태 > 엔티티** — 잠든 식구를 주민으로 세웠다가 (가시성 API 덫 + 서 있는 주민은 잠든
  가족으로 안 읽힘) **블록 형태(이불 둔덕)+소리**로 갈았다. 정적인 배역은 엔티티를 쓰지 마라.
- ★**콘솔함 명령은 플레이어 검사 앞에 분기** — 덤프가 「몸이 있어야 걷는다」에 문전박대당했다.
- ★**장부 진행 기록에 `- **검증**:` 볼드 불릿을 쓰지 마라** — backlog_audit 파서가 항목
  필드로 오인해 진짜 검증란을 덮는다 (굵기 없이 「- 검증:」).
- ★서장 월드 바닥은 y=-56 (FLAT 1.18+ minY -64) — getSeaLevel 은 거짓말, seaTop 이 정본.

---

## 4-주제. ★★★★★★★★★★★★ 최신 — 2026-07-28~30 (이 절이 가장 새롭다. ★★여기서 시작하라)

> ### ★★★먼저 이것부터 읽어라 — `docs/design/world_bible.md`
> **세계관 정본 한 벌.** 확정된 것만, 경위는 빼고, 읽히는 순서로 모았다.
> 그 문서 **§7 「아직 안 정한 것」**이 다음에 무엇을 할지를 말해 준다.
> 이 절(HANDOFF)은 **어떻게 여기까지 왔는가**이고, 그 문서는 **지금 어떤 모양인가**다.
>
> ★그리고 **대화 전용 회차**가 생겼다: 별도 터미널에서 **`/세계`** 를 치면
> 정본을 읽고 **문답만** 한다 (config 를 안 건드린다). 결정은
> `docs/design/world_decisions_inbox.md` 에 쌓이고, **작업 세션이 그걸 배선한다.**
> ★두 세션이 동시에 등록부를 갈면 깨지니, 그 경계를 지켜라.
>
> ★★**그 문법이 한 번 돌았고 작동했다** (인박스 회차 1·2 → 배선 완료).
> 배선한 절에는 **`✅ 배선 완료`** 가 적혀 있다 — **지우지 마라.**
> ★특히 값진 것: 대화 세션이 「점조직·암약」 안을 **스스로 걷어냈다**
> (§8 계율 = 한 번 걷어낸 암천회 모양). **장치가 제 일을 했다.**

**한 줄**: 사흘에 걸친 **세계관 회차**였다. **자료(화산전생 PDF) 전문 대조** → 사용자 판정
**「너무 닮았다」** → **네 세력 재정의 · 세계 주제 확정 · 마교 대개편 · 엔딩 재설계 · 이름 전부**.
**커밋 16개 · 코드 변경 0** (전부 config/docs). **MVT·봇 라이브 정상** ·
DB `세계막=prologue` 그대로 (막 id 개명 전에 확인했고 박발화 0건이라 안전했다).
★사흘째 끝에 **통독 전 훑기**를 돌려 어긋남 3·구멍 1·안 적힌 연결 2 를 잡고 닫았다.

### ★★ 다음 세션 첫 걸음

> ★★★**세계는 정해졌다. 이제 기계로 옮길 차례다.**
> 오늘 선 것 대부분이 아직 **글**이고 **아무것도 안 굴린다.** 아래 1번이 그 목록이다.

1. ★★**배선 안 된 것들** (세계는 있는데 기계가 없다 — 다음 세션의 본 일):
   · **플레이어 천마 루트** — 하오문 보고가 **어떤 기계를 타는가**(소문망? 명성? 개인 노선?)
   · **붙든 계보 수 → 제5막 침공 규모**의 산술 (엔딩 A/B 축에 직결)
   · **도려냄이 맹의 힘을 얼마나 깎는가** (제3막 딜레마의 실제 수치)
   · **제3막 집계 단위·시점·임계값** (개인 노선 분포 — 여러 회차째 미결)
   · **세 자리의 NPC 대체 판정**이 실제로 어디에 저장되는가
2. **세력별 `싸우는_이유` 39곳 미기입** — `python3 tools/lint_config.py` 가 매번 목록을 뱉는다.
   ★**지어내지 마라.** 그 경고 목록이 곧 「아직 안 정한 것」 장부다.
3. **남은 문답** — `docs/design/world_bible.md` **§7** 에 표로 있다. 굵은 것:
   **제4막의 「공동의 적」이 누구인가**(두 회차 연속 미착수) · 녹림·장강수로채의 패도천 가입 ·
   혈 무구가 **몇 종인가**(회수의 실물 단위) · 「수(手)」 목록 · D 셋의 성립 조건.
   ★**마교 자료(천마실록)는 더 없다** — 사용자가 「아는 정보는 다 줬다」고 했다.
   **403 우회를 다시 시도하지 마라.** 남은 것은 우리가 짓는다.
4. **밀린 실무**: **B-188 마교 무공 증축**(★뼈대가 섰다 — **마존 다섯 = 계열 다섯**:
   파괴·군림·신법·환술·수호) · B-177 전투 v2 수치 튜닝(위반 7건) · B-187 팽가 장원 빨간펜 ·
   사람 눈 관문(신규 캐릭터 완주).

### ★대화는 `/세계` 에서 (이 세션에서 만들었고 한 번 돌았다)
새 설정을 **정하는** 일은 별도 터미널의 `/세계` 회차에서 하고, 결정은
`docs/design/world_decisions_inbox.md` 에 쌓인다. **작업 세션이 그것만 배선한다.**
★이 세션은 인박스 회차 1·2 를 배선했고, 그 뒤로는 **작업 세션에서 직접 대화**했다 —
결정이 많아 인박스를 안 거쳤다. 다음부터는 **큰 덩어리는 `/세계` 로 돌리는 편이 안전하다.**

### 이 세션에서 선 것 (전부 커밋 · 라이브)
| 무엇 | 핵심 |
|---|---|
| ★★★세계 주제 | **「절대 악은 없다 — 혈교를 제외하고. 서로 이유가 있어 싸운다」** · `세계_주제` 절 + 전 세력 `싸우는_이유` 필수 필드 · **면제는 혈교 하나** (그 면제가 곧 주제다) |
| 네 세력 재정의 | 정파=**썩는 쪽이 있다** · 사파=**적정선이 있다** · 마교=**핍박받은 자들의 교단** · 혈교=**유일한 절대악** · ★한 줄: **혈교는 선이 없고 사파는 선이 낮고 정파는 선을 말하면서 지키지 못한다** |
| ★★층 3 해소 | 우리 혈교가 **「암천회」 모양**(뒤에서 전부 조종)을 입고 있었다 → 조종을 없애자 **통째로 사라졌다.** ★계율: **이름을 다 갈아도 구조가 남으면 같은 이야기다** |
| ★★자리 이관 | 혈교가 하던 일(추적 대상·연합 방해)을 **정파의 썩은 뿌리**가 물려받았다. 제2막에 맹을 세우고 **제3막에 세우자마자 썩는다** — 그 죄는 **처음부터 제 죄**다(재건이 아니라 창설이니까) |
| 개명 | 사도천→**패도천(覇道天)** · 천주→**패주(覇主)**. ★사용자가 「천주가 뭔가요?」라고 물은 것이 **천마와 헷갈린다는 증거**였다 |
| 폐기 | **칠성사 절 전체** · **적림십팔채** · 「사람을 판다」 · 제0막 위장 테러 → **빈 마을(시신이 없다)** |
| ★★마교 대개편 | 기원(증오에 형태를 준 것이 교리) · **천마=신의 그릇**(그릇이 되면 신이 들어온다 → 최강자가 기제가 된다) · **초대 생존·시한부**(금이 간 그릇) · **마존 다섯=사상**(부순다·차지한다·떠난다·덮는다·거둔다) · **전부 초대의 제자** · 기명각·소천마 |
| ★세 자리 | 맹주=**뽑힌다** · 패주=**이긴다** · 천마=**지목받는다** / 셋 다 레이드급 + NPC 대체 / 대리인 **군사·총관** → ★**수장은 명령하지 않는다. 앞에 선다** |
| ★★★썩은 뿌리 (2부) | **여러 곳에 조금씩 · 밑의 안 보이는 곳 · 자릿세** · ★**맹주는 썩지 않았다**(위는 깨끗한데 아래가 썩는다 — 맹주 플레이어는 악역이 아니라 **못 보는 자**) · ★**맹은 이기려고 커졌고 커지느라 아무나 받았다**(대치 때문) · 밑의 이유는 **「우리가 세운 질서를 지켜야 한다」이고 진심이다** |
| ★★★두 막이 이어졌다 | 「가난한 무인을 누른다」 = **마교의 기원** → **정파가 누른 자들이 마교가 되었고, 제3막에 정파는 다시 누르기 시작한다.** 못 도려내면 제5막 침공은 **복수가 아니라 청구**다 |
| ★★★딜레마가 기계가 됐다 | **도려내면 맹이 약해진다**(그들이 대치의 머릿수다) → 제3막의 선택은 도덕이 아니라 **「힘이냐 명분이냐」**. faction_politics 가 명분 게이지로 도는 시스템이라 정면으로 맞물린다 |
| ★★★엔딩 재설계 (3부) | 종결박 **전면전 → 봉인**. 엔딩 셋 다 「이겼다」가 없다 — ★**혈교는 죽지 않는다, 얼마나 가뒀는가만 다르다** · **혈교는 이기는 적이 아니라 가두는 재난이다** |
| ★★제3막이 끝까지 닿았다 | A의 조건을 **연대_폭**으로 바꿨다(혼자서는 못 가둔다) → **썩은 뿌리 → 연대 폭 → 봉인 가능 여부**. ★**A/B는 마교가, A/C는 연대가** 가른다 · C가 뜻을 얻었다(「물리쳤는데 못 가뒀다」) |
| D 셋으로 | **은거**(밖으로) · **혈화**(아래로) · **선경**(위로) — 전부 세계 엔딩과 동시 성립 · 조건은 미결 |
| ★★★마교에 200년 (인박스 1·2) | **구/신 천마신교** · 대충돌 200년 전 · ★**마교가 먼저 쳤다**(밑이 터진 것) → **가해자는 기억하고, 피해자는 잊었다** · 구 마존=**창건의 이유** / 신 마존=**매슬로의 비틀린 형태**(★넷은 올라가고 **거둔다만 떨어졌다 — 떨어진 것이 심장**) |
| ★★폐기: 마존=제자 | `전부_초대의_제자` **false/동료**로. 대체 근거가 더 강하다 — **교리의 원본이 아직 숨 쉬고 있어서 못 갈렸다** (종교 논리로 올라섰고, **2대는 원본이 아니라 후계자**라 다섯과 동급) |
| ★★★혈교 = **주워졌다** | 창시자 없음 · 세상에 뿌려진 **책과 무구** · 교리는 **수렴한 것** → **창시자가 없으면 목적도 없다**(「이유 없음」이 구조가 됐다) · `점조직/암약: false · 산발: true` |
| ★★봉인 = **회수** | **세상에 흩어진 것을 거두려면 세상만큼 넓어야 한다** → 「연대의 폭이 조건」에 근거가 생겼다 · **무당이 500년째 그 일을 하고 있었다**(봉인법이 하늘에서 안 떨어진다) |
| 전사(前史) 신설 | `world_clock.yml` `전사:` — **`wired: false`**. 박이 아니라 **서사의 근거**다 |
| ★★신 마존 두 종류 | **이은 계보**(뜻만 잃음) / **끊긴 계보**(전승이 끊겨 빈 자리를 **파편만 잇고** 교도가 채웠다) → ★**2대의 무기(원본)가 누구에겐 통하고 누구에겐 안 통한다** · **끊긴 교리를 복원할 수 있는 건 초대뿐** |
| ★「덮는다」 = 첫 실물 | 끊긴 계보 · **성(性) 교리의 여성 교도**가 올랐다 · ★결은 **유혹이 아니라 소유** (**감기게 하는 법만 남고 왜 감겨야 하는지가 사라졌다**) — 클리셰를 벗는 자리 |
| ★★★마교가 사회로 | **마존이 저잣거리에 가게를 갖고 있다.** ★**숨은 게 아니라 아무도 안 물었다** · **문주가 아니라 가게 주인**(= 개인의 유흥) · **침투가 아니라 살림**(200년을 어떻게 먹고살았나의 답) |
| ★★★플레이어 천마 루트 개통 | **하오문에서 정보를 산다** → 전황·인재·**그릇 찾기**. ★**문파는 제자를 고르고, 마교는 버릴 것이 없는 자를 찾는다** · 세 자리 진입: **정파=입문 · 사파=값 · 마교=저쪽이 먼저 본다** · ★가드: **저잣거리에서 한 일은 세 곳이 다 듣는다** |
| ★★다섯의 살림 | 거둔다=약재 · 떠난다=관문 안 지나는 길 · 덮는다=기루 · 차지한다=노역판 · 부순다=병장기(★**아무도 안 산다** → 가장 과격한 계보가 가장 가난하다). ★규율: **살림은 그 계보의 무공에서 나온다** · ★★**숙청 = 밥그릇 깨기**(정파 딜레마의 거울) |
| ★★기루 = 하오문의 **한 팔** | 끊긴 「덮는다」 계보의 잔존이 저잣거리로 흘러들었다 → **산에서 죽은 계보가 저잣거리에서 다른 것이 되어 돌아왔다** · ★**「기녀는 색공·음공」이 이미 증거였다** · 알면 **안에서 갈라진다** · 문주는 **어렴풋이 알고 안 묻는다**(★**맹주는 못 본 자, 하오문주는 안 본 자**) |
| ★★★마교가 나라가 됐다 | **백성이 산다** · 마존이 **영역을 관리**하고 **크기가 다르다**(정치가 생긴다) → **2대의 일이 붙드는 것만이 아니다 — 사람을 먹여야 한다** · ★**제5막은 군대가 아니라 나라가 움직이는 것** |
| ★★마존 다섯의 이름 | 별호 **활인→불사 · 낙토→철벽 · 안혼→혼박 · 호세→군림 · 파천→파천**(★안 바뀜) · 성명 **위인보·석원달·소청화(여)·당서온(여)·뇌천강** · ★**이름과 별호가 싸운다**(뇌천강만 안 싸움) · ★**천마는 이름을 버리고 마존은 이름을 받는다** |
| ★★★당가 줄기 (재설계) | **구 마존 당직(唐直) 본인이 당가 사람**이었다(직계로 당서온까지) · ★**내쳐진 게 아니라 아무것도 시키지 않았다** — 마교 기원(*죽게 내버려졌다*)과 **글자 그대로 같다** · ★제5막 표적 = **사천** |
| ★★★당가의 최고는 **암기** | **세상은 당가를 독으로 알고, 당가는 암기로 산다** · *방계가 **독문으로** 흘러나갔다*가 이미 그 증거 · ★「아무것도 안 시켰다」의 내용 = **독은 만지게 두고 암기는 안 가르쳤다** → **그는 당가의 손이 아니라 그림자를 가져갔다** |
| ★★★독의 한계 (세계 규칙) | **독은 고수를 죽여도 절대고수는 못 죽인다** → **당가가 최고가 아닌 이유**(*누구든 죽일 수 있었지만 아무도 이길 수 없었다*) · ★**세 자리가 절대적인 까닭**(독조차 안 통한다) · ★구 「호세」의 답이 실패한 이유(**독은 앉은 자를 죽여도 자리를 못 바꾼다**) |
| 구 마존 성명 | **자(慈)·야(野)·정(靜)·당직(唐直)·열(烈)** — ★넷은 **외자에 성이 없다**(성은 집이 있는 자의 것) · ★**이름이 길어지는 동안 뜻은 짧아졌다**(천마 0자 → 창시자 1자 → 지금 3자) |
| ★★★무력의 층 (교정) | **패주는 그냥 강하다**(다수전 특화는 **원작에서 빌린 것**이라 걷었다) · 다수전은 **천마 쪽으로** · ★**맹주는 함께 싸우고, 패주는 혼자 싸우고, 천마는 여럿을 상대한다** · ★★**천마는 다른 층 — 맹주+패주가 합쳐도 못 이긴다**(**사람 중 최강이 패주이고, 천마는 사람이 아니다**) |
| ★★그 층이 설명하는 것 | **마교는 미안해서 안 갔고, 정파는 못 이겨서 안 갔다** · 제4막 연합의 진짜 이유 = **이기려고가 아니라 혼자서는 하루도 못 버텨서** |
| 패주 = **오중산** | ★**별호가 없다** — *별호는 여럿일 때 필요하다. 그는 하나였다* · ★**천마는 이름을 버렸고, 패주는 별호가 안 생겼다** · **사파는 이름을 안 바꾼다** |
| 총관 = **범소천(철산)** | 패주의 **의형제이자 형** — **둘이 패도천을 세웠다**(창설 이야기가 비어 있었다) · ★**계보 없는 자들이 계보를 만드는 유일한 방법이 의형제다** · **사파에서는 형도 아우 밑에 앉는다** · ★**힘으로 정하는 곳에서 딱 한 자리만 힘으로 안 정한다** |
| 맹주 후보 · 정파 셋 | 후보 **소림(원명·불괴)·남궁(남궁호·창검)** · **무당(현담·유검) 사퇴** · ★**남궁이 된다** — *셋 다 될 수 있었다. 하나는 원하지 않았고, 하나는 말할 수 없었고, 하나만 남았다* |
| 군사 = **제갈태(필진)** | ★**총관은 사람을 섬기고, 군사는 자리를 섬긴다**(사파는 사람, 정파는 기구) · 맹주가 바뀌어도 남는다(총관은 **원한**을, 군사는 **무관심**을 딸려 보낸다) · ★★★**맹을 지키는 것이 그의 일이다. 그래서 맹의 흠도 그가 덮는다** — 썩은 뿌리의 **맨 앞자리** · ★**제갈은 맹주를 못 한 것이 아니라 안 한 것이다** |
| ★★★작명 계율 | **아무도 모르는 일이 이름에 적혀 있으면 안 된다** (수현·은검을 걷어낸 까닭) · **「이름과 별호가 같은 말을 한다」 제안 폐기** · ★**정파는 무공으로, 사파는 하는 일로, 마교는 무엇이 되었는지로 불린다** |
| ★★검토하고 남긴 것 | 구 마존 외자가 **별호의 되풀이** · **뇌천강↔파천 天 공유** — ★**둘 다 알고 남긴다**(사용자 판단). **다시 고치려 들지 마라** |
| 혈교의 그것 | ★**이름은 있는데 아무도 모른다**(아는 자가 다 죽어서) — *그에게도 이름이 있었다. 다만 기억하는 자가 아무도 안 남았다* · ★★**하나는 이름을 버렸는데 모두가 알고, 하나는 이름을 안 버렸는데 아무도 모른다** |
| ★★★플레이어 패주의 가시 | **패주가 되면 부하가 하나 딸려 오는데, 그는 당신이 죽인 자의 형제다** → ★**천마는 우군을 물려받고, 패주는 원한을 물려받는다** (파천 계보 ↔ 총관) |
| ★★★「부순다」 재정의 | **다섯 중 유일하게 약속을 지킨 계보**(교리·승계·천마의 친우) → 안 변한 이유가 **우연에서 의지로**. ★**가장 큰 영역, 가장 적은 수입** — *가장 많은 사람을 지키는 계보가 가장 적게 번다* · ★★**2대의 첫 우군이자 승계의 유일한 증인**, 그런데 **가장 충직한 자가 가장 과격한 교리를 들고 있다** |

### 함정·계율 (이 세션 것)
- ★★★**장르 공용 자산과 한 작품이 발명한 것을 갈라라.** 무림맹·녹림·하오문·개방·총관·장로원은
  공용이라 그대로 쓰고, 한 작품만 쓰는 조어(적림십팔채·칠성사·사도천·흑영부·상천십좌)는 안 쓴다.
  ★그런데 **더 깊은 층이 있다**: 이름을 다 갈아도 **구조**가 남으면 여전히 같은 이야기다.
- ★★**등록부에 이미 어긋난 것이 잠들어 있을 수 있다** — 마교 절의 `소교주=교주의 자식`(혈통 승계)과
  `마존=전문으로 갈린다`(아홉)를 **읽다가 우연히** 찾았다. 새 확정과 정면으로 충돌했다.
  **고칠 절을 읽을 때는 그 절 전체를 읽어라.**
- ★★★**눈이 지어내기를 막았고, 그 덕에 더 맞는 자리를 찾았다**: 썩은 뿌리 명분의 표적으로
  `orthodox`를 걸었더니 「roster 에 없다」고 막혔다 — orthodox 는 **컨테이너**다.
  일단 명분을 걷고 소문만 남겼는데, 몇 시간 뒤 「**자릿세** — 밑에서 민간을 뜯는다」로 확정되자
  **명분이 애초에 틀린 그릇**이었음이 드러났다. 이 부패는 고발장이 아니라 **민심·경제 수치**로
  드러난다 (`region_delta`). ★**못 정한 것은 비워 둔다 — 눈이 지켜 주고, 나중이 답을 준다.**
- ★**뮤테이션 앵커는 또 낡았다** (이 세션에만 **세 번** — 개명 1 · 엔딩 재설계 2).
  도구의 「시험 자체가 낡았다」 경고가 매번 살렸다. **이 경고가 없는 눈은 조용히 거짓말한다.**
  ★재표적 요령: **`scope: personal` 같은 흔한 줄만 잡지 마라** — 여럿이 되면 어느 것인지
  모호해진다. **id 와 붙여 잡아라** (`- id: eungeo` + name + scope 를 한 덩어리로).
- ★★**「판정 입력」이라 적고 판정에 안 쓰면 그것이 등록부의 거짓말이다** — 엔딩 축을 갈면서
  `혈교_명분` 이 판정에서 빠졌다. 지우는 대신 **`쓰임: 연출`** 을 명시했다. 쓰임을 밝혀라.
- ★★★**막 하나가 통째로 헛돌 수 있다** — 제6막 「혈교 전면전」이 엔딩을 거의 안 정하고 있었다
  (A/B 를 가른 건 제5막의 마교 승패였다). ★**큰 장면이 있다고 그것이 판정하는 것은 아니다.**
  **판정식을 직접 읽어라.**
- ★★★**등록부가 제 보증으로 눈을 내세울 때는, 그 눈이 실재하는지 세어 보라.**
  `세계_주제` 절이 「lint_config 가 싸우는_이유를 잡는다」고 적어 뒀는데 **그런 눈이 없었다**
  (전날 선언만 하고 안 만들었다 — **감사 거짓말 P0급**). 정리 회차에 발견해서 실제로 만들었다.
  `grep -c "<필드명>" tools/lint_config.py` 한 줄이면 드러난다.
- ★**면제는 선언이지 공백이 아니다** — 혈교의 `싸우는_이유: 없음` 은 **비워 두면 안 된다.**
  빈칸은 「아직 안 정했다」와 구별되지 않는다. 눈이 그것을 위반으로 잡는다.
- ★**정본을 웹으로 띄울 때는 md 를 그대로 띄운다** — 별도 HTML 로 예쁘게 만들면
  **정본과 갈라져 낡는다.** 이 프로젝트가 가장 싫어하는 것이 그것이다.
- ★★★**폐기한 구조는 다른 주체를 입고 다시 온다.** 이 세션에서 「하오문 문주가 알고 보니
  **마존**이었다」가 나왔는데, 그건 우리가 걷어낸 「천선이 하오문주로 **위장**했다」와
  **글자 그대로 같은 모양**이었다 (주체만 혈교→마교). **매번 다시 재라.**
  ★그리고 거절로 끝내지 마라 — 이번엔 **위장 없이 원하는 것을 전부 주는 형태**가 있었다
  (「숨은 게 아니라 아무도 안 물었다」). **구조를 막고 욕구는 채워라.**
- ★★★**큰 값을 교정하면, 그 값을 근거로 삼던 문장들을 따라가라.**
  패주의 무력을 「다수전 특화 → 그냥 강함」으로 갈았을 때 **별호(일인진)는 지웠는데**,
  그 무력에 **기대고 있던** *「사파에는 합격진이 없다」의 까닭*은 못 따라갔다 —
  **사실만 남고 이유가 사라진 채로 있었다.** ★지운 것만 세지 말고 **기대고 있던 것**을 세라.
  (통독 전 훑기에서 잡았다 — 그때 어긋남 3·구멍 1·안 적힌 연결 2 가 나왔다)
- ★★★**이름에 비밀을 적지 마라.** 무당 장문인을 「수현(守玄)·은검(隱劍)」으로 지었다가 걷었다 —
  **500년 당직을 이름이 말하고 있었다.** 아무도 모르는 일이 이름에 적혀 있으면 안 된다.
  ★그리고 원인이 된 제 제안(**「정파는 이름과 별호가 같은 말을 한다」**)도 함께 폐기했다.
  **이름은 태어날 때 받은 것**이라, 그 사람이 어떻게 될지 모르고 지은 것이어야 한다.
- ★★★**검토하고 남기기로 한 것은 「남긴다」고 적어라.** 안 적으면 다음 사람이 같은 것을 또
  「발견」해서 고치려 든다. (구 마존 이름이 별호의 되풀이인 것 · 뇌천강↔파천 天 공유 —
  둘 다 훑어서 찾았고 **사용자가 그대로 두기로 했다.** 그 판단을 등록부에 박아 뒀다)
- ★★**별호·이름은 일상 한국어와 동음이의인 말을 쓰지 마라.** 두 판을 갈아엎고 배웠다:
  1판은 조어(멸세·훼세·엄목·환주)라 눈에 안 붙었고, 2판은 **탈수(脫水)·연수(硏修)·정세(政勢)·
  안심·폐문**이라 별호로 들으면 힘이 빠졌다. ★**파천만 그 문제가 없었고, 그것이 기준이 됐다.**
- ★★**블록 스칼라 위의 키를 갈 때는 갈고 나서 키 목록을 파싱해 대조하라.**
  `★구신_대조가_기계다:` 를 덮어써서 두 블록이 **한 블록으로 합쳐졌는데 YAML 은 통과했다** —
  내용이 엉뚱한 키에 붙어 있어도 파서는 아무 말도 안 한다.
- ★**막 id 를 갈기 전에 DB 를 본다** — `세계막=prologue`·박발화 0건을 확인하고 갈았다.
  `prologue` id 는 보존했다 (`WorldClockEngine:252` 가 등록부 밖의 id 에 예외를 던진다).

---

## 4-세계. ★★★★★★★★★★★ — 2026-07-26~27 (§4-주제가 이 위에 선다)

**한 줄**: 이 세션은 **무공 증축 4회차(소림·개방·당가 + 눈 둘 신설)** → **세력의 결·백지 지도** →
**세계 거대 줄기 v2 교체(7막 + 엔딩 분기 문법)** → **ㄷ 대화 2회차(사도천·칠성사)** 로 갔다.
코드 변경은 `Weapons.java`(편 계열)뿐 — **MVT·봇 라이브 정상** (마지막 재배포 Done 25.1s ·
ERROR 0 · 봇 예외 0 · JDA ✓).

### ★★ 다음 세션 첫 걸음
1. **ㄷ 대화 3회차** (사용자가 「한번 끊고 진행」 — 이어서 하기로 한 자리):
   가장 굵은 것은 **적림십팔채가 제3막에서 사도천 편인가 중립인가** (별개 기구라 따로 정해야 한다).
   그 밖: 천주의 이름·내력 · 칠성사 일곱을 다 쓸지 · ★**천선이 하오문주로 위장했다면 혈교가
   사도천 성원 하나를 통째로 쥔 셈인가** · 「수(手)」의 구체 목록 · 맹주 후보 · D 엔딩 조건 ·
   개인 노선/추적 성패의 **집계 단위·임계값**.
   ★계율: **모르는 건 다 묻는다. 추측 금지** (사용자 지시 2026-07-27).
2. **B-188 다음 칸** — 채움 순서가 바뀌었다: 새외 6곳이 **입문 폐쇄**되어 빠졌다.
   남은 「문이 열렸는데 배울 것이 없는」 곳은 **둘뿐**: **장강수로채**(구 계열 신설 — 자료 있음:
   쇠사슬 무기·수전) · **마교**(심법만 1). 그다음이 살막·혈교·표국·관군·하오문 보강.
3. **밀린 것**: B-177 전투 v2 수치 튜닝(위반 7건이 실측 기반 — 이번 세션 내내 그대로) ·
   B-187 팽가 장원 빨간펜 · 사람 눈 관문(신규 캐릭터 완주).

### 이 세션에서 선 것 (전부 커밋 · 라이브)
| 무엇 | 핵심 | 커밋 |
|---|---|---|
| 소림 증축 | 10종+심법 1 · ★**자료의 「장법」에 掌이 아니라 杖이 둘** 섞여 있었다(항마선장·대윤회겁륜장) → 곤법을 지어내지 않았다 · 봉 공급원 1→2 | e3f595b |
| ★사다리 눈 | `lint_ladder_chain` — 선행을 거슬러 첫 칸에 닿는가. 오의 선행 미기재 4건을 잡았다 | e3f595b |
| 개방 증축 | 13종 · 정격 개명(개방봉법→**옥룡팔장**) · ★설정을 기제로: 가난=`rearm_free` · 머릿수=타구진 · 비천=백결연화신공 | 4262712 |
| 당가 + **편(鞭) 15계열째** | 10종 · `Weapons.java` Series·Base.ROD·switch 2곳 · ★**간격 5.0m 인데 위력 2**(거리를 위력에서 뺀다를 근접에 처음) · ★**「하급이 없다」가 구멍이 아니라 설계**였다(심법이 문턱) — 눈이 없는 구멍을 지어내고 있었다 | ade70d1 |
| ★세력 커버리지 눈 | `lint_faction_coverage` — 축이 「무공 0」이 아니라 **「문이 열렸는데 무공 0」**. 입문 루트는 **16개뿐이고 정파는 화산·당가 둘**이라는 사실을 이 눈이 드러냈다 | 29ebe77 |
| 세력의 결 | 사파 5·금기 2 에 `무공_색` 기입(그전엔 **새외만** 있었다) · **낭인회 신설**(눈이 낸 문답을 자료가 닫았다) · ★**구=장강수로채 · 부=녹림** 확정 | f907f03 |
| ★새외 입문 폐쇄 | 6곳 `player_entry:false` + `deferred_to:NPC_기술`. **문을 지운 게 아니라 잠갔다** — 세 문(거리·통행증·심법 opposed) 설계는 그대로 | 9e75af2 |
| ★무림맹 신설 | 「정파 계열=분류 / 구파일방=명단 / **무림맹=기구**」. `authority_mandate` 의 강호 쪽 짝이 없어 **토벌령을 아무도 못 내리고 있었다** | 9e75af2 |
| ★★정본 교체 | story_summary 5단계 → **7막 + 엔딩 4분기**. `world_clock.yml` acts 재작성 | 04577f4 |
| ★엔딩 분기 문법 | `endings:` 절 신설 — acts 는 선형이라 못 담는다. **fallback 이 정확히 하나**라 「엔딩 없는 세계」가 불가능 · D 는 `scope:personal`(세계 엔딩과 동시 성립) | 04577f4 |
| 사도천·칠성사 | 사도련→**사도천(邪道天)** 개명 · 천주(무력·안 바뀜)·**총관**(대리인) · ★**칠성사는 혈교의 침투 조직**으로 정정 · **적림십팔채 신설**(사파 기구가 둘) | 977bef5 · e5b8a3f |

### 함정·계율 (이 세션 것)
- ★**「~장」으로 끝나는 무공 이름은 掌인지 杖인지 먼저 본다** — 소림(항마선장·대윤회겁륜장)과
  개방(옥룡팔장)에서 **두 번** 나왔다. 동음이의가 가렸을 뿐 정격 이름이 자료에 이미 있었다.
- ★★**없는 구멍을 지어내지 마라** — 당가 「독공 하급 없음」은 구멍이 아니라 설계였다
  (`requires_simbeop` = 심법이 문턱). 눈이 그 필드를 못 읽어 공중시작으로 세고 있었다.
  **구멍을 놓치는 눈만큼 나쁜 것이 없는 구멍을 만드는 눈이다.**
- ★**살아 있는 등록부를 갈기 전에 DB 를 본다**: `world_clock.yml` 재작성 전
  `세계막=prologue · 박발화 0건` 을 확인했다. `WorldClockEngine:252` 는 등록부 밖의 막 id 를
  만나면 **예외를 던진다** — id 를 잘못 갈았으면 라이브 세계가 멎었다.
- ★**뮤테이션 앵커는 낡는다** — acts 를 갈자 옛 뮤테이션 5건이 죽었다. 도구가 「시험 자체가
  낡았다」를 스스로 경고해 줘서 살았다. 그 경고가 없으면 **낡은 시험이 조용히 통과하는 눈**이 된다.
- ★**프로브에 숫자를 박지 마라** — `⑳-4` 가 "세력 36" 을 문자로 박았다가 사도천 등재로 깨졌다.
  건수·절대값이 아니라 **관계**(기준선 대비 증가·없던 문자열)를 재라.
- ★**봇 PID 는 `ps | awk '/bin\/java -jar/'` 로 집는다** — `pgrep -f "server-bot"` 은
  **제 명령줄까지 잡아** 잘못된 PID 를 고른다 (토큰 길이 72 검사가 막아 줘서 살았다).
- ★**config 를 바꾸면 봇도 재기동한다** — 봇은 기동 시 `factions.yml` 등을 읽는다.
  MVT 만 재배포하면 봇은 **어제 등록부로 돈다** (이 세션 끝에 실제로 그랬다).

### 눈의 현황 (이 세션 끝 기준)
`lint_config` 0 · `combat_audit --lint-only` **위반 0**(경고 5) · `motion_audit` 0(커버리지 100%) ·
`game_audit` 0 · `world_clock_audit` **0·0** · `backlog_audit` 0 ·
**self-test: combat 26/26 · world_clock 20/20**
★`combat_audit` **전체**는 위반 7건인데 **전부 기존 B-177 v2 TTK 빚**이다 (이번 세션 것 아님).

---

## 4-무공. ★★★★★★★★★★ 최신 — 2026-07-25~26 밤 세션 (이 절이 가장 새롭다. ★★여기서 시작하라)

> **모델 메모**: 다음 세션은 **Opus 4.8 (1M · max effort)** 로 시작한다 (사용자가 세션 끝에 전환).

**한 줄**: 이 세션은 **서장 마감(사전 집필 전환)** → **시작 다원화 확정(B-187)** → **전 세력 무공
증축 프로그램 개시(B-188)** 로 이어졌다. 지금 살아 있는 두 굵은 줄기가 B-187·B-188 이다.
코드 변경은 거의 config/눈뿐 — **MVT·봇 라이브 정상**(마지막 재배포 MVT Done 33.0s · ERROR 0 ·
봇 JDA ✓ · 봇 재기동 이 세션에서 15회 · 토큰 env 직달 절차 안정).

### ★★ 다음 세션 첫 걸음 (우선순위 순)
1. **B-188 전 세력 무공 증축 — ② 소림** (프로그램의 다음 칸): 장법 하·중급 + **곤법(봉) 신설**
   (봉 빌드 기준도 겸해 해소). 방법 = 팽가·제갈·모용에서 정착한 문법 (사용자 정격 자료 우선 ·
   없으면 클리셰 【제안】+★지어냄 표기 → 빨간펜). **전 등록부 한 벌이 한 회차**:
   skills·skill_mechanics·skill_motion·simbeop·ultimate_arts (+ 무기 계열 신설 시 combat.yml
   weapon_power·weapon_styles·swings·basic_strike·Weapons.java switch 두 곳). 채움 순서·기준
   지도는 **B-188 본문** (③개방 봉하급 → ④당가 독공 → ⑤무당 권장 → ⑥남궁 기초 → ⑦사파·민간·
   군문[겸·구·중병기·부 = 0곳 계열 해소] → ⑧5파 소소 → ⑨tier 정리).
   ★계율: **모르는 이름·수치는 지어내지 말고 사용자에게 묻는다** (등재 못 하면 후보 주석으로 보류).
2. **B-187 시작 다원화 — 하북팽가 장원 시안 빨간펜**: `docs/design/pengga_estate_v1.md` 【제안】
   미결 6건 (딸린 촌락·안내인 3인 이름·청하현 관계·비팽가 아이 거취·서장 B안·치수). 빨간펜 →
   지도 확정 → 땅·건축 조성. ★계율: **시작지 실물이 서기 전에 서장 문장을 「장원 시작」으로
   바꾸지 마라** (몸은 아직 청하현 — 서사가 세계보다 앞서면 거짓말).
3. **사람 눈 관문(여전히 병목)**: 새 판 신규 캐릭터 완주 — 서장 사전 집필·발단별 패·발단의
   실·견문 체감 + 2인 동시 승선 격리. 빨간펜 나오면 그 회차가 1순위로 끼어든다.
4. **밀린 것**: B-177 전투 v2 수치 튜닝(눈이 든 위반 7건이 실측 기반) + **오의 v2 산술**(이 세션
   발견 — planUltimate 이 v1) · growth_audit v1 산술 · B-183/185 · B-176 소문.

### 이 세션에서 선 것 (전부 커밋 · 라이브)
| 무엇 | 핵심 | 커밋 |
|---|---|---|
| ★서장 붓 폐지 | LLM 실시간 생성 폐지 — prose 등록부가 그대로 책 (`seojang.yml live_brush:false` · 옛 폴백의 정본 승격 · 붓은 /혼천 대화 전용). 계기: 그림자_시험 1장이 기준 서사를 대체해 뜻 모를 장 | a53f473 |
| ★서장 다인 격리 | 남의 패 보임(onlyFor 스폰 스냅숏 구멍→setVisibleByDefault) + 몸 겹침(Voyage.veil/unveil 상호 은신·충돌 오프·하선 복원) | a53f473 |
| 세가 1장 1+3 | 발단별 패(choices_by_incident) · 뼈대 절 직전 정지 · 판돈 고지 hint | 9420f8c |
| A안 발단의 실 | 2·3장에 {incident_thread} — 14발단×2문장 (재난 7발단이 2장부터 같은 글이던 병 수리) + 세가 4발단 **견문**(평범한 관례 출행) | d20d6c3 |
| ★B-187 시작 다원화 | 심리 테스트에서 **시작 마을만 운명 뽑기**(소속은 추천만·청하현 고정 없음) · 파일럿 하북팽→화산 | 04d031e |
| C안 입장권 | 세가 자제 시트 「가전_무공」 기장(자격이지 힘 아님) · 곽진 거절이 실명 안내로 | 2f510a5 |
| ★오의 분석 확정 | 오호단문도·혼원벽력도 오의 유지 · 숙련 9~10=통현/입신 정의 · 창작 오의 후순위 | a457cd5 |
| ★B-188 프로그램 | 전 세력 무공 증축 헌장 + **무기별 빌드 최소 2 기준**(★0: 겸·구·중병기·부) | d64f938 · 0d03531 |
| ★★세가 4가문 무공 | 팽가 12종(개명+포트폴리오)·제갈 7종(부채·팔진도 — 부채 14계열째 신설)·모용 15종(수공·지법). 5가문 전원 입장권 | 1ea3e1e·e09e7d7·e4f72dd·2787ea1·90dd926 |

### 함정·계율 (이 세션 것)
- **한 문파 = 한 줄기 사다리가 아니다** (사용자 교정) — 무기·계열별 다양한 무공 + 심법 별개.
  증축의 기본 단위는 「계열 사다리」(기초→하→중→상 + 오의)이고, 무기 하나에 빌드 둘 이상.
- **서사가 세계보다 앞서면 거짓말** — 시작지 실물 없이 서장 문장만 「장원」으로 바꾸면 몸은
  청하현에 떨어진다. 문장과 좌표는 한 회차 (B-187 계율).
- **무기 계열을 신설하면 등록부 6곳 + Weapons.java switch 2곳이 한 벌** — 부채 신설 때
  motion_audit 이 style↔class → 계열의 손 → 기본 초식 누락을 **차례로** 잡았다 (눈이 순서대로
  이끌었다). 빌드 실패는 switch exhaustive (hanja·flavor 두 곳).
- **오의는 별개 사다리다** — 「최상급 무공」이 아니다. 오호단문도·혼원벽력도를 도법 목록으로
  내리면 가주 일인전승(sect_life 승계)이 무너진다. 자료의 「목록」과 게임의 「사다리」는 층위가 다르다.
- 봇 재기동 절차 15회 검증: /proc environ 토큰 확보(길이 72 검사) → SIGTERM → run_bot_pg.sh (env 직달)

---

## 4-삼도천. ★★★★★★★★★ — 2026-07-24~25 (★8~10부는 이 절 안. §4-무공이 이 위에 선다)

**한 줄**: **나루 = 삼도천 화폭 구현 완료 (5차)** — 명계 개정(서=넋등 어둑·동=등롱 밝음 · 구간
갈림) + 새벽녘 고정 + 중경(고사목 12·진흙 둔덕·옛 잔교+석등·피안화) + 원경(동쪽만 실루엣+
불빛 한 점). 사용자 확정 문답 2건 · 눈의 시험 **65/65** · 감사·lint·장부 위반 0 · 배포 ✓
(기동 오류 0) · 월드 백업 후 제거 — **첫 입장 때 재생성** (census 로그가 그 순간을 기록).

### ★★ 다음 세션 첫 걸음
1. **튜토리얼 마무리** (사용자 지시: "인게임 실측은 튜토리얼 완성 이후"): B-179 서장=기억의
   회랑 설계 회차가 유력한 다음 조각. 그다음 실측 몰아서 — §4-거울 1번(단계 5 UI·뿌리내림
   정거장 9) + **삼도천 화폭 체감** (수치·문장 전부 【제안】 — 빨간펜 대상)
2. B-177 전투 v2 남은 빚 · B-176 다음 조각 · XP 재보정 — §4-거울 2~3번 그대로

### 이 세션에서 선 것 (커밋 · 라이브)
| 무엇 | 핵심 | 정본 |
|---|---|---|
| ★명계 개정 | 넋등(광량 10)과 잔교 밝기 계약(암흑 ≤15%)은 물리적으로 양립 불가 → **구간을 갈랐다**: 서=저승(어둑함이 정본·dark_pct 창 40~92) · 동=이승(TownAudit 눈금). 경계=나루 관문 서쪽 끝 (등록부 유도) | antechamber.yml lighting.soul |
| ★새벽녘 고정 | 확정 문안의 「황혼」은 서쪽 하늘이 주홍 — 「서=저승 청백·동=이승 주홍」 축과 반대말 → 새벽(22900 【제안】): 동쪽만 주홍 = 환생의 방향 | canvas.fixed_time |
| 화폭 | 넋등 잔교(서 5칸 촘촘·부두 등롱 9칸) · 고사목 12그루(남서 — grain_shift 위상·죽은 가지) · 진흙 둔덕(grain 중간띠) · 옛 잔교+석등 2(북 z-9) · 피안화(둔덕 위) · 원경 동쪽만(실루엣+불빛 x58) · 수련잎 9→21 | canvas 절 · plan() ⑤-2~4 |
| 문장 | 글판·사공 대사에 저승의 결 ("갓 온 넋들"·"산 자의 땅이 아니다") — 전부 【제안】 | arrival·ferryman |
| 눈 | audit ⑧ 광원 **세기** 모형(넋등 10·등롱 15) + ⑧-2 화폭 눈 9종 + 뮤테이션 10 신설·2 재표적(㉒㉓) | antechamber_audit(+selftest) |

### ★2부 (같은 날 밤) — 과제 폐지 = 순수 문지방 완결 (사용자 지시)

**한 줄**: "아직 입도진에 과제가 존재" → 3차 개정이 남긴 「접속」 과제와 **과제 기계 전부**를
걷었다 (진척 장부·감지 3종·명령 기입·순차 공개·예고 변형·done/all_done·revisit_ledger_line).
**나루는 시험하지 않는다** — 판=안내판(stations[나루].panel · 옛 문장 그대로 승계), 문=이름,
발판=명령 대행. 눈: lessons 절 **부활 자체가 위반** + 코드 잔재 눈 + 뮤테이션 9건 묘비·재표적.
★함께 잡은 실기 결함: **판이 같은 칸을 두 번 적어** 완결성 검증이 제 판에 속아 94% (매 진입
재조성 직전 — 콘솔 ERROR가 증거) → plan() 겹침 걷기(마지막 기록·마지막 자리) + 눈 ⑫ +
뮤테이션 2. 눈의 시험 **62/62** · 감사·lint·장부 0 · 재배포 ✓. 정본: B-121 ★6차 진행 기록.

### ★3부 (2026-07-25 새벽) — B-179 기억의 회랑 = 삼도천 항해 (시안 확정 4건 + SJ-101~104 배선)

**한 줄**: **강을 건너는 동안이 곧 서장이다** — 종(또는 접합)이 배를 띄우고, 배가 안개 물길을
느리게 동진하며 기억의 정거장 3곳(넋등 문)에서 그 장의 책이 열린다. 동쪽 기슭(이승의 불빛 —
원경이 기슭 자리 x92 로 이사)에 닿는 순간이 곧 출도. 책 파이프·봇 권위·조판은 한 줄도 안 바뀜.
`Voyage.java` 신설 · 승선 세 길 · 갇힘 금지(명단 끝=기슭 직행 · 재접속 relocate · 도강 탈출).
**붓이 느린 것이 전제** (사용자: "ai 출력 속도가 많이 느린 걸로 암") — 0.5칸/초 항해가 붓
~22초+를 덮는다. 눈 ⑧-3 11종 + 뮤테이션 7 — **눈의 시험 69/69**. 정본: seojang_presentation.md
§0 · B-121/B-179 진행 기록. 남은 조각: 계열별 기억 조형(디스플레이 — 조형 시안 회차) ·
**신규 캐릭터 실기동** (닫는 조건 — 사람 눈 "겪는 서장"인가).

### ★5부 (7/25 새벽~아침) — 실기동 마라톤: 도하 개정·세가 실전·모델 나룻배 (다음 세션이 여기서 시작)

**한 줄**: 첫 실사용자가 밤새 문 열여섯을 찾았고 다 고쳤다 — 회랑은 **3차 개정(정박 무대 +
암전 도하 · 탈것 폐지)**으로, 나룻배는 **팩 모델**(honcheon:naru/barge · 좌표 한계 34→32
함정)로, 세가·출분·발단 무대는 실전 첫 항해(디돈 · 비무행)를 통과했다.

### ★8부 (7/25 저녁) — ★★★★다음 세션 작업 계획 (사용자 지시 「계획 세우고 다음 세션으로」 · 여기서 시작하라)

**오늘 하루의 형세**: B-179 도하 5차 개정 + 빨간펜 7회차 전부 라이브 · B-182 확정 4건 배선 ·
B-184/186 닫힘 · B-180 수리(관측 대기) · B-185 발견(콘솔함 신설) · 앵커 8/9 재박음.
**눈의 시험 115/115 · 전 감사 0.** 이제 병목은 코드가 아니라 **사람 눈**이다.

**작업 순서 (위에서부터)**:
1. ★★**사람 눈 관문 — 신규 캐릭터 완주 1회** (사용자 몫 · 최우선): 오늘 빨간펜 7회차분이
   전부 이 한 판에 걸려 있다 — 대화 타자기·명패 세로 목록·갈림길 맞물림(붓)·배 형태·
   사공·사다리·첫걸음 안내·리스폰=내리는 자리·마을 앵커 재박음. **세가의 자제가 걸리면
   B-182 닫는 조건까지 겸한다** (승격 80%라 무가 당첨 시 대체로 세가). 빨간펜이 나오면
   그 회차가 1순위로 끼어든다 — 오늘의 리듬 그대로.
2. **B-177 전투 v2 남은 빚** (다음 구현 회차 1순위 — ★눈이 세계와 어긋난 채 오래 묵었다):
   combat_audit·defense_audit 기대 모델 v1→v2 (지금 「위반 0」은 v2 산술을 안 잰 것) +
   수치 튜닝(사용자 체감 한 줄 대기) + 장비 크리 슬롯 + PvP directOpposed 거취.
3. **B-183 마감**: 다리 격리 가드 selftest(순수 함수) + 테스트 서버 기동 정본 스크립트
   (잔류 방지 포함 — 콘솔함이 생겨 반쯤 수월해졌다).
4. **B-185 마감**: 콘솔함을 관리 통로 정본으로 확정 + 왕복 눈 (RCON 근인 규명은 선택 —
   Paper 내부라 비용 대비 낮음. ★계율: RCON 으로 혼천 명령 치지 마라).
5. **관측 닫기 (자투리)**: B-180 재발 0 (다음 세계일 정산·탄생 뒤 bot.log) → 닫음.
6. **B-176 소문 시스템 회차** (굵직한 설계+구현 — 별도 회차로): 세계반응 3갈래(위세→
   npc_combat morale·경제·친화) + 퀘스트→소문→칭호 삼각형 + ★유보분 = 세가 적자 탄생
   대사건(천하 강도 5 연출).
7. **밀린 자투리**: XP 실측 재보정(`xp_pacing --rate` — 텔레메트리 쌓이면) · B-109 잔여
   (echo·회상 책) · 팩 옛 폴백 경로 6개 도구 청산 (장부 참조).

**운영 메모 (다음 사람)**: 관리 명령 = 콘솔함(`plugins/HoncheonMVT/console_inbox.txt` —
RCON 으로 혼천 명령 금지 · B-185) · 봇 재기동 = /proc environ → python env 직달 (토큰 파일
무경유 — 이 세션에서 절차 정착) · 팩 3정본 sha1 763d5f27 · DB 백업 run/backup-db.

### ★10부 (7/25 밤 · 실기동 2호 회차) — ★★서장 사전 집필 전환 + 다인 격리 (사용자 확정 2건)

**한 줄**: 서장 붓(LLM 실시간) **폐지** — 「모든 루트의 출력값을 생성해두고 출력 · 이름만 변수」
(`seojang.yml live_brush: false` → prose 등록부가 그대로 책 · 옛 폴백의 정본 승격 · 붓은
/혼천 대화 전용으로 존속) + **다인 격리 2결함 수리** (남의 패가 보임 = onlyFor 스폰 스냅숏
구멍 → `setVisibleByDefault(false)` · 같은 공간에 몸이 보임 → `Voyage.veil/unveil` 상호
은신+충돌 오프, 하선 때 복원). 계기: 그림자_시험 1장 — 붓이 기준 서사를 대체해 뜻 모를 장.
재배포 ✓ (MVT Done 29.4s · ERROR 0 · 봇 재기동 — 토큰 env 직달 7회째). ★소급 안 됨: 이미
못 박힌 장은 옛 붓 글 — 새 판부터 등록부 문장. 정본: B-179 진행 기록 (2026-07-25 저녁).

**★3부 (같은 밤) — A안 「발단의 실」 + 세가 4발단 「견문」**: 2·3장 뼈대에
`{incident_thread}` — **14발단 × 2문장**의 실이 길 위·낯선 고을까지 따라온다 (재난 7발단이
2장부터 같은 글이던 병의 수리 · 전부 【제안】) + 세가에 **평범한 아이의 관례 출행 「견문」**
신설 (사용자: 「얌전히 큰 세가집 자식일 수도」 — 명도 시험도 아니다 · 노자 넉넉 · 이름만
두고 간다 · 전용 패·무대·실 포함). 눈 ②-e(실 전수)+②-d 견문 계약 · 뮤테이션 (141)~(143) —
**눈의 시험 120/120** · 재배포 ✓ (Done 31.2s · ERROR 0). 남은 사람 눈: 발단 갈아 타며 결
체감 + 실 28문장 빨간펜. 미결(사용자): 서장 선택의 기계 보상(시작 능력치 씨앗 — 성장 v3
저울과 얽힘) 별도 결정 회차. 정본: B-179 진행 (2026-07-25 밤 3).

**★5부 (같은 밤) — B-187 확정 4건 + C안 라이브 + 장원 시안 1호**: ① 배정 = 심리 테스트에서
**시작 마을만 운명 뽑기** (소속 아님 — 마을은 문파를 추천만 · 청하현 고정 없음) ② 파일럿 =
**하북팽가 → 화산** ③ 지도 시안 = Fable 【제안】→빨간펜 ④ C안 선행. **C안 배선 완료**:
great_house_arts(남궁 창궁검법·팽 개산도법·당 당문비수술 — 모용·제갈 미등재라 입장권 없음) →
시트 「가전_무공」 기장·표시 → 곽진 거절이 실명 안내로. lint 5-f+selftest 6/6. **장원 시안**:
docs/design/pengga_estate_v1.md 【제안】 (구획 72×56 · 릴레이 대응 집사/도장고지기/사범 ·
미결 6) — 빨간펜 대기. 정본: B-187.

**★★4부 (같은 밤) — 헌법급 확정: 시작의 다원화 (B-187 신설)**: 「서장의 끝은 청하현이
아니다 — 배정에 따라 시작 마을 여러 개 (구파일방 각 산문 · **세가는 세가대로**)」. 기존
start_regions(청하현 완비+스텁 3+funnel_rule)·factions(구파 10+세가 5)가 이미 이 방향 —
단일 시작은 구현 단계였다. ★계율: 시작지 실물이 서기 전에 서장 문장을 먼저 바꾸지 마라
(서사가 세계보다 앞서면 거짓말). 미결 4건(배정 방식·파일럿 순서·지도 주체·가전 무공
입장권 선행)이 사용자 결정 대기 — **정본: B-187**.

**★2부 (같은 밤) — 세가 1장 빨간펜 「1+3」 확정·배선**: 1장 패를 **발단이 가른다**
(`choices_by_incident` — 그림자_시험·밀서 전용 셋 【제안】 · 비무행 공용도 「배우기 전의
몸」에 맞게 개정) + 뼈대는 「사당 **앞에 섰다**」로 절 직전 정지 · 「아직 아무것도 배우지
못한 몸」 명시 · 마지막 물음이 패에 넘긴다 + 무대 hint 가 판돈을 말한다 (선택=판정 → 다음
장 이음새·강호 첫 밤의 온도 — 기계의 진실 그대로). `Seojang.scenesOf` 한 해석기 · 눈 ②-d
+뮤테이션 (139)(140) — **눈의 시험 117/117** · 재배포 ✓. **남은 사람 눈**: 새 판으로 세가
완주(발단별 패·뼈대·hint 체감) + 2인 동시 승선. **미결(사용자)**: 서장의 「얻는 것」을
기계로도 키울지 (지금은 이음새·첫 밤 온도뿐 — 시작 능력치 씨앗은 성장 v3 저울과 얽혀
별도 결정 회차).

### ★9부 (7/25 밤) — 전투 v2 의 눈 갱신 (★8부 2번의 첫 조각 — combat/defense_audit v1→v2)

**한 줄**: combat_audit·defense_audit 기대 모델이 v2 로 갈아탔다 (`combat_v2.enabled` 디스패치 —
엔진과 같은 문 · v1 복귀 스위치 보존) + 두 눈에 **눈을 시험하는 눈 신설** (`--selftest` 뮤테이션
프로브 — combat 9/9 · defense 6/6). ★★결과: **combat_audit 위반 7건 실체** (동경지 TTK 2합 ·
매복=처형 · 일류가 졸개 5인 소탕 못함) — 「위반 0」은 역시 v2 를 안 잰 것이었다. **이 7건이
수치 튜닝 회차의 실측 기반이다 (【제안】 per_body·크리표·격 위력 — 수치는 사용자 승인 대상이라
안 건드렸다).** defense_audit 위반 0·경고 2(회피=기하 정직 고지). B-005 검증 `--lint-only` 로
좁힘 (닫힌 항목의 증거가 B-177 밸런스에 안 물리게). 발견: **growth_audit 도 자체 2d6(v1)** —
다음 조각. 정본: B-177 진행 기록 (2026-07-25). 코드 변경은 tools/ 둘뿐 — 라이브 무영향.

### ★7부 (7/25 낮 2회차) — ★★★5차 개정: 별도 서장 월드 + 건축 나룻배 한 척 (이 절이 최신)

**한 줄**: 사용자 재설명("별도의 서장 월드 · 건축 나룻배 · **1장 배에서 2장 배가 보이면 안 됨**")
→ 서장이 **서장 월드**(혼천_서장 — 칠흑+달빛 밤바다 · FLAT 물 층)로 이사했다. 배는 건축 중선
13×5 **한 척**(반블럭·계단 곡선 · buildBarge 결정론·멱등), 장 전환 = 가짜 항해+눈깜빡임+닻
재정렬(이동 없음), 안개 링 상시. 나루의 항해 장치(문 3곳·정박 배 3척·팩 모델 셸)는 전부
철거·묘비 (월드 백업 20260725-125146 후 재생성 · 팩 3정본 sha1 763d5f27). ★「과제 » 맞는
쪽의 선택」의 진범 = **본토 뿌리내림이 배 위에서도 세던 것** → TutorialGuide.silenced 중앙
게이트(훅+트래커 — 소음 항목도 닫힘). 눈: ⑧-3 전면 재표적 + **묘비 부활 감시**(배는 하나뿐) —
**눈의 시험 99/99** · 감사 전부 0. 정본: B-179 5차 진행 기록.

**★★ 다음 세션 첫 걸음**: ① 신규 캐릭터 실기동 완주 (사람 눈 — 대화 타자기·명패 세로
목록·갈림길 맞물림·첫걸음 안내·리스폰. 세가 자제로 완주하면 B-182 닫는 조건까지 겸한다)
② B-180 재발 0 관측 후 닫기 (다음 세계일 정산·탄생 지나고 bot.log) ③ B-183 잔여 (격리
가드 selftest + 테스트 서버 정본 스크립트) · B-177 전투 v2 빚 · B-176 소문 시스템.
【이 오후에 닫힌 것】 B-184 닫힘 (옛 월드 잔재 — 증거 장부) · B-182 미결 4건 확정·배선
(성씨 안내만·붓에 세가 실명·대사건은 B-176 유보·거절 세가판) · B-180 출처 확진+유령 검사.
【저녁 — 잔재 점검 타임 (B-186 닫힘)】 ★마을 앵커 8/9 이 건축 속(곽진병의 뿌리) → 앵커재측
7곳 재박음 · DB 매달린 참조 0 · B-185 신설 (RCON 에서 혼천 명령 = RCON 사망 — 2회 재현) →
★★관리 명령 통로 정본 = **콘솔함** `plugins/HoncheonMVT/console_inbox.txt` (파일 한 줄 =
콘솔 명령 · 응답은 콘솔 로그. RCON 으로 혼천 명령 치지 마라).

**★실기동 1호 수리 2건** (같은 날 낮): 「2장이 다시 시작되지도 않아」 = 패(offerChoices)가
나루-검사뿐이라 서장 월드에서 영영 안 걸림 → 나루·바다 공통 (**계율: 월드를 옮기면 그 월드를
검사하는 모든 게이트를 같은 커밋에서 재라**) · 「뱃사공도 없어」 = ferryman 등록부+ensure
(빌리저 · 고물 · 한 배에 한 사공). 눈의 시험 **102/102**.

**★빨간펜 2회차** (같은 날 낮 · 확정 3건): **서사 글판**(장면 전문이 패 위에 —
narration_panel) · **패 명패형+먹 테**(판목 묘비 · ★int 파서가 ahead 1.9/spread 1.3 을 1로
자르던 결함도 수리) · **사다리+사공의 삿대**(rescue — 깊이·표류 회수). 눈의 시험 **106/106**.

**★빨간펜 3회차** (같은 날 오후): 전문 글판 "너무 난잡" → **한월풍 대화 채팅 + 타자기**
(장식 틀·문장 단위 55틱·패는 마지막 문장 뒤) + **SJ-002 기억첩 글리프 배선 완결** (E0B0
붓선 틀 · E0B3 빈 인장 명패 · E0B2 찍힌 인장 확정 — 팩에 이미 있던 4장, 배선만 미결이었다) ·
배 위 판=장 제목 표지 강등 · F26 이스케이프 이행. 눈의 시험 **107/107**.

**★빨간펜 4회차** (같은 날 오후 · 스샷 2건): 명패 **세로 목록 4.0** (긴 문장 라벨의 가로
나열이 한 줄로 뭉개졌다 — row_gap 0.55·가로 spread 묘비) + **붓의 로마자 안전망** (「길을
건넌 days」 — 감지→재집필 1회→세척 · LlmRenderer). 봇 재기동(토큰 선확보 6회째) ·
눈의 시험 **109/109**.

**★빨간펜 5·6회차** (같은 날 오후): **붓에게 갈림길을 싣는다** (전문이 제 갈림을 지어내
등록부 패와 딴 장을 살았다 — sceneFacts 에 선택지 세 문장 · 이미 그려진 장은 소급 안 됨) ·
**첫걸음 안내**(출도 착지 때 트래커+섭구 안내 — arrival_lines) · **리스폰=내리는 자리**
(destination 한 벌 재사용) · **곽진 벽 질식 루프 수리**(Standing 재고 세움). 눈의 시험
**113/113** · 다음 사람 눈: 신규 캐릭터 완주 (갈림길 맞물림·안내·리스폰 체감).

### ★6부 (7/25 낮) — 도하의 숙제 셋 해소 (설계 문답 → 4차 개정 · ★7부가 이 위에 선다)

**한 줄**: 설계 문답 3건 확정 → **가짜 항해**(사람·배 제자리, 세계가 흐른다 — 물살·좌우 노
박자·마중 안개 · 어둠은 눈깜빡임 12틱으로 축소) + **안개 장막**(정거장 중간점·기슭 앞 · 항해자
눈에만 · 조성 변경 0) + **배 형태 1차 수리**(갑판 맞물림 — 확폭 선체가 갑판 6×3을 감싼다 ·
이물·고물 2단 굽힘 · scale 2.6 · 팩 3정본 sha1 ee1d4068). 등록부: voyage.transit 개정
(flow_ticks/row_period/blink_ticks)·voyage.fog 신설 — 전부 【제안】. 눈: audit 재표적+신설,
뮤테이션 (89)(104) 재표적·(112)~(116) 신설·★(109) 표류 수리(시험 자체 고장을 시험이 잡았다) —
**눈의 시험 93/93** · 감사 위반 0. 정본: B-179 4차 진행 기록.

**★★ 다음 세션 첫 걸음**: ① 사람 눈 실기동 — 도하 체감(흐름 5.5초 【제안】·노 박자·눈깜빡임) +
장막 짙기 + **새 배 스샷 빨간펜** (신규 캐릭터 = B-179 닫는 조건 겸용) ② 뿌리내림 트래커가
항해 중에도 뜨는 소음 ③ 세가 미결 4건(B-182) · B-180/183 잔여.

### ★5부의 옛 숙제 셋 (4차로 닫힘 — 기록만 남긴다)
1. ~~이동감 부재~~ → 가짜 항해 ✓ 2. ~~배 형태~~ → 아는 문제 셋 선수리 ✓ (스샷 빨간펜 대기)
3. ~~다음 정거장이 보인다~~ → 안개 장막 ✓ (간격 16칸·조성 불변)

### 이 새벽의 함정·계율 (다음 사람은 밟지 마라)
- **모델 좌표는 −16~32** — 요소 하나가 34를 넘으면 모델 전체가 체커로 죽는다 (경계 검증 스크립트로 재라)
- **팩은 세 정본을 함께 갱신하라** — 재컴파일 + run/pack-http 동기화 + GitHub 릴리스 clobber
  (serve 스크립트는 zip 을 재사용한다 — 재빌드 안 하고 "배포했다"고 믿는 함정)
- **접합자 재접속은 enter() 를 안 지난다** — 판이 자라면 기동 때 build 로 세계를 치유하라 (실증: 87→100%)
- **바닐라 보트**: 첫 좌석=조종석 · 투명 플래그 무시 · yaw 표류 — 탈것 편법의 값 (그래서 폐지했다)
- **한 다리에 두 몸 금지** — 잔류 테스트 서버가 명부를 지워 접합을 막았다 (격리 가드 + B-183)

### ★4부 (같은 새벽) — 실기동 회차: 부두 경주 수리 + ★무대 그릇 (책이 사라졌다)

**한 줄**: 실기동 1호 "이으니까 바로 책을 받고" = 접합 직후의 경주(다리 2초 > 승선 5틱) —
나루의 책은 승선을 기다리게 수리. 이어 **"책이 너무 루즈 → 글이 아닌 몸으로"** (확정 3건:
맥박 한 줄 · 물 위 등불 우클릭 선택 · 다층 무대) — 항해 중 서장에서 **책 제거**, 정거장 =
계열별 기억 무대(본인에게만 보이는 조형 애니메이션+파티클+사건음), LLM 전문은 기슭의
**필사본**으로. 정본 `seojang_stage.yml`(전부 【제안】·기본 계열 다층 파일럿)+`SeojangStage.java`.
강등: enabled:false = 책 그릇. 눈 ⑧-4 9종 + 뮤테이션 6 — **눈의 시험 75/75**.
사람 눈 대기: 무대 파일럿 체감 (조형·맥박·등불 빨간펜) → 수행_파견 증축.

### 함정·계율 (이 세션 것)
- **판이 같은 칸을 두 번 적으면 검증이 제 판에 속는다** — 겹쳐 쓰기(갈대가 물을, 나무가 공기를)는
  최종 세계와 판을 어긋나게 한다. 조성 판은 자기일치가 계약이다 (겹침 걷기 · 눈 ⑫)
- **뮤테이션이 겨누는 파일을 늘리면 백업도 같이 늘려라** — selftest 가 CFG·SRC 만 되돌리면
  SBK·VOY 뮤테이션이 영구 감염된다 (다중 파일 백업으로 개정)
- **과제를 폐지하면 그 과제를 겨누던 눈·뮤테이션도 같은 커밋에서 재표적하라** — 이번에 9건
  (3차의 계율 재이행 · 잔재 눈이 부활을 지킨다)
- **어둑함도 계약이다** — 저승 구간은 「밝아도 위반」(dark_pct 창). 어둠 눈만으로는 넋등 실종을
  못 잡는다 (동쪽 빛이 서쪽으로 번져 창 안에 남는다) — 리듬은 등록부에서 직접 잰다 (넋등≤등롱)
- **같은 grain 장을 두 번 쓰면 같은 자리에 몰린다** — 고사목이 갈대 장 그대로면 동남(이승 곁)에
  몰렸다. grain_shift 위상 분리로 저승 쪽에 앉혔다 (결정론 유지)
- **하늘도 등록부다** — 시각 고정값은 의미 축과 대조해야 한다 (황혼=서쪽 주홍 — 축과 반대)
- 표적 잃은 뮤테이션 ㉒(등롱 성김)·㉓(간격 조각)는 묘비를 남기고 재표적했다 (3차의 계율 이행)

---

## 4-거울. ★★★★★★★★ — 2026-07-24 오후 세션 (§4-수확 보다 새롭다)

**한 줄**: **성장 v3 단계 5(UI) 배선 완료 — 전부 라이브** (레벨 거울·조용한 레벨업 알림·바닐라
XP바=경험·승급 연출·시트 승급 조건·몹 명패 레벨 칸·획득 토스트) + **B-176 첫 조각**(경지 칭호 =
경지 이름 그대로 · 명패 팀 접두). 사용자 결정 6건 · 눈 29/29 · lint 0 · 장부 위반 0.

### ★★ 다음 세션 첫 걸음 — 여기서 시작하라
1. **인게임 실측** (사용자 체감 대기 — 2부까지 한 번에): ① 단계 5 UI — 레벨업 flash · XP바 ·
   승급 연출(`promotion_fx` 수치 【제안】 — end_rod 60발·도전과제 종) · 명패 `[경지]` 접두 ·
   「획득 » …」 토스트 (레벨업 검증은 `/혼천 소환 <id>`) ② **뿌리내림 과정 (B-178·2부)** —
   신규 캐릭터로 정거장 8 흐름 + 트래커 줄 + 안내인 3인 대사. **체감 수정 회차가 예약돼 있다**
   (문장 전부 【제안】 — 빨간펜 대상)
2. **B-177 전투 v2 남은 빚**: combat_audit·defense_audit 기대 모델 v1→v2 (눈이 세계와 어긋나 있다)
   + 수치 튜닝 + 장비 크리 슬롯 + PvP directOpposed
3. B-176 다음 조각: 세계반응 3갈래(위세→npc_combat morale·경제·친화) + 퀘스트→소문→칭호 삼각형
   (소문 시스템 의존) + 본인 선택 · XP 실측 재보정 (`xp_pacing --rate`) · B-109 잔여(echo·회상 책)

### ★2부 (같은 날 오후) — 뿌리내림 과정 = 본토 튜토리얼 (B-178 신설 · 전부 라이브)

**한 줄**: 튜토리얼 = **B-109 ①마디(뿌리내림)의 구체화**로 확정 (사용자 결정 8건 —
`tutorial_rooting.md` §1). 기존 NPC 릴레이(섭구→장쇠→산길→곽진 · 새 NPC 0) · 정거장 8
(`config/tutorial.yml`) · 사이드바 트래커(기초 과정만 카운터 — ★personal_story §6.4 부분 개정
묘비 있음) · 행위 자체가 보상. `TutorialGuide` + 훅 6곳 + PlayerLedger 영속 + 소급 거울.
눈: `tools/tutorial_audit.py` (등록부↔훅 표류 — 죽은 정거장 = 영영 안 끝나는 튜토리얼) 위반 0.
안내인 3인 실존 RCON 확인. ★함정 회피 기록: 운기는 개화 전 거절("단전이 비어 있다")도
가르침으로 센다 — 입도진 「뛰어라」 함정의 재발 방지. 수련·운기는 정거장을 갈랐다
(계수 공유면 수련 2번=완료 오배선).

### ★3부 (같은 날 늦은 오후) — 나루 재설계: 순수 문지방 (사용자 확정 3건)

**한 줄**: 나루의 목적을 파악(대기실·접합·서장·의식 — 가르침은 본토 안내 부재 시절의 보완물)하고
**맞이→나루 두 관문만** 남겼다. 격·경공(전원-범인에게 예고 전용)·수련·시트·허수아비·지급품 전부
제거, 태세→본토 「몸짓」 정거장(흘리기·회피 2문 · **막기는 방패 없어 예고만** — 판매 보류·등록 시
1500문 의향), 손→첫 사냥 흡수. 뿌리내림 정거장 8→**9**. 나루 월드 백업 후 제거 — 첫 입장 때
새 등록부로 재생성 (§4-밤의 나루 심부 마을 잔해도 함께 청산). 눈: antechamber_audit 를 새 정본에
맞춤 (과제별 검사 「있을 때만」 + 조기 return 이 사람 보호 검사를 감던 병 수리) · selftest 뮤테이션
재정비 — **눈의 시험 53/53** · tutorial_audit 9/9 · 정적 감사·lint 위반 0 · 기동 오류 0.
★계율: 과제를 이관하면 **그 과제를 겨누던 눈의 시험도 함께 이관하라** (표적 잃은 뮤테이션 33건이
「시험 자체 고장」으로 잠들 뻔했다 — 묘비를 남기고 폐기·재표적했다).

### 이 세션에서 선 것 (전부 커밋 · 전부 라이브)

| 무엇 | 핵심 | 정본 |
|---|---|---|
| ★정본 정리 | HANDOFF 요약 "레벨업 연출"은 정본과 어긋났다 — **레벨업은 조용히, 승급이 터진다** (feel §1 사용자 확정 준수) | growth_v3_feel.md §1·§4 |
| 레벨 거울 | mvtSheet level/xp/xp_need/points → Sheet·PlayerLedger — **마크가 처음으로 제 레벨을 안다** (전엔 XP 올려보내고 결과를 몰랐다) | GameListener.mvtSheet |
| 레벨업 알림 | syncSheet 델타 감지 → 액션바 flash (첫 스냅숏 = 도착이지 델타 아님 — 안 알린다) | SkillListener.syncSheet |
| XP바 | 바닐라 XP바 = v3 경험/레벨 (setLevel/setExp — XpEconomyGuard 절연 전제의 이행) | cultivation_v3_levels §4-b ② |
| 승급 연출 | Title=**새 경지 이름** · 부제=**cultivation_stages description 인용** · 버스트+소리 = `promotion_fx` 등록부 신설 (수치 【제안】) | skill_motion.yml promotion_fx |
| 시트 승급 조건 | promotionGates 리팩터 — **표시=판정 한 해석기** (자동 경지 ✅/❌ 현재/필요 · 벽 경지 trigger·요건 인용+현재값. 침묵 boolean 소멸) | GameListener.promotionGates |
| 몹 명패 레벨 | `[N] 이름` (한월 문법 · HitFeedback 예약 지점 — mobLevel 같은 해석기) | HitFeedback.upsertBar |
| ★경지 칭호 | **칭호 = 경지 이름 그대로** (사용자 확정 — 별도 이름표 없음 · 범인 없음). 남도 본다 = 스코어보드 팀 접두 `[경지]` (5초 전파) + 사이드바 Lv | B-176 첫 조각 |
| 획득 토스트 | 줍기 = 채팅 「획득 » '가죽' +1개」 (이름 있는 물건만) · XP = 액션바 「획득 » 경험 +N」 (도배 회피) | HuntListener |

### 눈 (전부 통과 상태로 인수)
`growth_v3_backfill_selftest.py` **29눈** (단계 5 신설 3: 한 해석기 32조합·부족 관문 표기·표시
갈라짐 오배선) · lint 0 · motion_audit 위반 0 · backlog_audit 위반 0 · 실측: 스냅샷에 level 실림
확인 (Lv11·30포인트) · MVT 기동 오류 0 · 봇 JDA 로그인 ✓

### 함정·계율 (이 세션 것)
- **요약과 정본이 다르면 정본이 이긴다** — HANDOFF 의 "레벨업 연출(Title+버스트)"은 feel §1
  (레벨업=조용·승급=연출)과 어긋났다. 사용자 확인으로 정본 준수 확정
- **표시와 판정은 한 해석기** — 승급 조건 표시를 promoteIfDue 와 딴 벌로 재면 언젠가 다른 곳을
  가리킨다 (promotionGates 한 벌 · selftest 가 오배선을 잰다)
- **첫 스냅숏은 델타가 아니라 도착이다** — 재기동 직후 레벨 알림이 터지면 그것이 오배선이다
- 봇 재기동 절차 재검증 (5회째): /proc environ 토큰 확보(길이 검사) → kill → run_bot_pg.sh

---

## 4-수확. ★★★★★★★ 최신 — 2026-07-24 낮~오후 세션 (이 절이 §4-점화 보다 새롭다)

**한 줄**: **성장 v3 단계 4 완전 마감** — 배분 손·이중 관문·내공 A안·★B안 개정(원장 무캡)·
XP 3원천(처치/디스코드 사냥/의뢰)·정예·두목 지정·**v2 수련→능력치 동결**까지 전부 라이브.
+ XP 다리 실기 결함 2건 수리 (사용자 인게임 실측 확인). 커밋 12개 · 눈 26/26 · 장부 위반 0.

### ★★ 다음 세션 첫 걸음 — 여기서 시작하라
1. **성장 v3 단계 5 — UI** (B-135 마지막 단계): 레벨업 연출(Title+버스트 — 한월 A5) ·
   시트에 승급 조건 표시(지금은 왜 승급 안 되는지 침묵 — 자격 레벨/마크/숙련을 말하라) ·
   획득 토스트 · 칭호 B-176 (세계반응만)
2. **B-177 전투 v2 남은 빚**: ★combat_audit·defense_audit 기대 모델이 아직 v1 (눈이 세계와
   어긋나 있다) + 수치 튜닝(사용자 체감 한 줄 대기) + 장비 크리 슬롯 + PvP directOpposed
3. XP 실측 재보정 (계약된 유보): `rate_xp_per_hour 200` 【제안】 — 텔레메트리 쌓이면 `xp_pacing --rate`

### 이 세션에서 선 것 (전부 커밋 · 전부 라이브)

| 무엇 | 핵심 | 정본 |
|---|---|---|
| 포인트 배분 손 | 시트 [포인트 배분] 버튼 → 축 7버튼(al: 네임스페이스) · 시트는 원장만 표기(⑪) | GameListener onAllocate |
| 승급 이중 관문 | promoteIfDue 에 자격 레벨 N_k AND ("레벨은 자격, 사건이 문") | levels.qualifying_level |
| 내공 A안 | 내력 풀 실수치 = √원장[내공] (시트·운기·마크 거울 3곳 — poolNaegong) | levels.naegong_unified |
| ★★B안 개정 | **원장 캡 폐지** — 배분 언제나 됨·파생 무캡. **판정만 min(floor√원장, 경지 캡)** (봇 genderStat(realm)·마크 judgmentAttr). 천장 너머 원장은 승급 때 판정으로 터진다. 은행 규약 소멸(B-152 종결) | attribute_scale_v3 **§8.10** |
| XP 3원천 | 마크 처치(등급 계수) + 디스코드 사냥(잡은 것만) + 게시판 의뢰(등급표 20/30/50/80/120) | levels.xp_sources |
| 정예·두목 | 호랑이·반달곰=정예 · 갈호·백영묘=두목 (npcs xp_grade — lint 5-d 가 오타를 잰다) | cheongha_npcs.yml |
| ★XP 다리 수리 | ① flush 를 onKill 끝으로 (gap 표 밖 등록 개체도 실린다) ② pendingXp 영속 | HuntListener·PlayerLedger |
| v2 동결 (마감) | 행위→기술·화후·축기만, 능력치=레벨 포인트. 화해 자연 무연산 (off = v2 되돌림 한 줄) | levels.training_attr_frozen |

### 눈 (전부 통과 상태로 인수)
`growth_v3_backfill_selftest.py` **26눈** (B안 4·동결 2·오배선 감지 7종 포함) ·
`lint_config.py` 5-d(xp_grade)·5-e(의뢰 등급↔XP 표) 신설 + selftest 케이스 · backlog_audit 위반 0

### 미결·대기
- 수리 전 증발한 XP 보상 — 사용자가 처치 내역을 답하면 다리 경로로 적립 (소액 · 방치 가능)
- 검증 절차 정착: 레벨업 검증은 새 월드가 아니라 **`/혼천 소환 <id>`** (관리자 · PDC 표식 포함 — XP 파이프 끝까지 흐른다)

### 함정·계율 (이 세션에서 물린 것)
- **flush 는 적립의 끝에서** — 적립(pendXp)이 실기(pushLedger) 뒤에 오면 한 박자 늦거나 영영 안 실린다.
  다리 수신함에 **당일 파일이 없으면** 그것이 실기 결함의 첫 증거다
- **pending 은 영속하라** — 메모리에만 있으면 재기동이 지운다 (pendingXp 가 그랬다)
- **gap 표(바닐라 몹 타입)와 npc 등록부는 다른 문이다** — 등록 개체(호랑이=Ravager)는 gap 표 밖
- **감시 명령의 자기일치** — bash cmdline 에 패턴이 실려 제 자신을 문다. 판별은 `readlink /proc/<pid>/exe` 로만 (이 세션에서 두 번 물렸다 — §4-판의 함정 재발)
- **`git add -A` 를 커밋에 쓰지 마라** — 추적 외 파일(.ai-review·exe)이 딸려 들어갔다 (amend 수습)
- **B안의 문**: 판정은 봇 genderStat(realm)·마크 judgmentAttrOf/attackBonus(realm) 를 지나야 캡이
  든다. **파생 독자(내구·이속·내력 풀)는 그 문을 지나지 마라** — 몸은 천장 없이 자라는 게 계약이다
- 봇 재기동 절차는 이 세션에서 4회 검증됨: /proc environ 토큰 확보(길이 검사) → kill → run_bot_pg.sh

---

## 4-점화. ★★★★★★ 최신 — 2026-07-24 낮 세션 (이 절이 §4-성장 보다 새롭다. 여기서 시작하라)

**한 줄**: **전투 v2 가 라이브에 켜졌다** (2d6 전투 사망 · 공방+크리) + 성장 v3 **단계 3~4 절반**
(√원장 다리 · 표류 수리 · XP 파이프 라이브) + 장부 위반 8건 전부 청산. ★다음 = **포인트 배분 손**.

### ★★ 다음 세션 첫 걸음 — 여기서 시작하라
**성장 v3 단계 4 잔여 (B-135)** — XP·레벨·포인트는 쌓이는데 **쓸 손이 없다**:
1. **포인트 배분 손** (봇 명령/버튼 — 원장 캡 c²(`cultivation.yml raw_attribute_cap_by_realm`) ·
   포인트 은행 무기한 · 시트는 원장만 표기 §8.9 ⑪)
2. 승급 게이트에 자격 레벨 N_k 편입 (`levels.qualifying_level` · 사건 마크와 이중 관문)
3. **v2 수련→능력치 동결** — ★배분이 서기 전엔 켜지 마라 (성장이 아예 멈춘다). 동결하면
   backfill 화해가 자연 무연산이 된다 (설계됨)
4. 내력 풀 = √원장[내공] 교체 (**A안 확정** — `naegong_unified: true` 선언만 있고 배선 전)
5. 정예·두목 등급 필드 등록 (지금 처치 XP 는 전부 잡졸 계수)

### 이 세션에서 선 것 (전부 커밋 · 전부 라이브 배포됨)

| 트랙 | 무엇 | 상태 |
|---|---|---|
| 장부 | 위반 8건 청산 — 증거 3건 기입(B-011/170/137) · 근시 눈 2개 수리(antechamber 글자수 창→몸통 검사 · perf 파이썬 독자 항상 합산) · 팔레트 밖 firework→end_rod(점층 오프라 시각 무변경) | `--run` 위반 0 |
| ★전투 v2 | 세 판정길(무공·평타·NPC) 공방 배선 + **라이브 점화** (`combat_v2.enabled: true`). 결정(사용자): 공격력=무기+숙련+능력치+격 **4항** · 격차±2/고갈−2 제거 · 평타=바닐라+능력치+격. 기동 고지 로그 있음 | **라이브** — 실측=일상 플레이 관찰 |
| 성장 단계3 | 다리 attrs=√원장 + ★표류 결함 2 수리: backfill 이 화후 소수부를 버렸고, 원장이 얼어 수련이 판정에 안 실렸다 → **화해(raise-only · max(원장,화후²))** — 판정 보존 증명·단계4 앞방향 호환 | 라이브 (봇 재기동됨) |
| 성장 단계4 | 결정 4건(XP=몹레벨×등급계수 클래식 고정 · 의뢰 80/50/40/30 · 사선 보너스 폐지 · **내공 A안 통일**) + XP 파이프: 처치(짐승+등록 NPC)→`+N 경험`→다리→봇 레벨업·3포인트 | 라이브 (배분 손 없음) |

### 정본·눈
- `docs/design/xp_sources_v3.md` §0 (확정) · `combat_resolution_v2.md` · `growth_v3_impl_plan.md`
- `cultivation.yml levels.xp_sources`·`naegong_unified` · `combat.yml combat_v2` (둘 다 결정 주석 포함)
- 눈: `growth_v3_backfill_selftest.py` **10눈** (화해 3 + XP 3 신설) · `judgment_scale_harness.py` 0

### ★ 전투 v2 의 남은 빚 (B-177 — 장부 참조)
【제안】 수치 튜닝 (per_body 0.5·크리표·상한 0.75 — 사용자 체감 한 줄이면 됨: "잡졸이 안 죽어" 류)
+ ★**combat_audit·defense_audit 기대 모델이 아직 v1** (v2 가 라이브인 지금 눈이 세계와 어긋나 있다 —
그들이 위반 0 이라 해도 v2 산술은 안 잰 것) + 장비 크리 슬롯 + PvP directOpposed 거취.
끄기·튜닝: config + `/혼천 모션 재적재` — **재기동 불필요** (엔진 전체 갈아끼움·리스너 rebind).

### 함정·계율 (이 세션에서 물리거나 세운 것)
- **감사 출력을 tail 로 자르지 마라** — 위반 상세가 날아가 두 번 돌렸다
- **글자 수 근접 창의 눈은 주석이 자랄 때마다 거짓 짖음** — 몸통(body_of)이 경계다 (antechamber 수리)
- **폴백은 가림이다** — perf 눈의 파이썬 독자가 자바 독자 등장에 실종됐다. 독자는 합산이 맞다
- **두 장부는 화해로 잇는다** — raise-only max() 는 판정을 보존하고 권위 이전 뒤 자연 무연산이 된다
- **운영 JDK 배치가 뒤집혔다**: ★라이브 = jdk-25 (BetterModel 라이브) · 테스트 = jdk-25 로 맞춤.
  redeploy_mvt.sh 가 정본 (빌드 21 · 기동 25). 봇 재기동은 토큰 확보 절차(HANDOFF §3)로 조율자 대행 가능
- 봇 jar 를 도는 중에 재빌드했으면 **그 봇은 재기동 대상이다** (지연 로드 함정 — 두 번 그랬다)

---

## 4-성장. ★★★★★ 최신 — 2026-07-23~24 성장·전투·런처 마라톤 (이 절이 §4-판 보다 새롭다)

**한 줄**: 검기 라이브 반영 + 타격 피드백(대미지 숫자·HP바) + **전용 런처(Electron·공식 마크 재활용)**
+ **성장 v3 구현 3단계** + **전투 판정 재설계(2d6 제거)** 설계 완결. ★다음 = 전투 v2 코드 배선.

### ★★ 다음 세션 첫 걸음 — 여기서 시작하라
**전투 v2 코드 배선 (B-177 2단계)** — 설계·config 다 섰고 `combat_v2.enabled: false`라 라이브 무영향.
1. `docs/design/combat_resolution_v2.md` 읽기 (모델 정본) + `config/combat.yml` `combat_v2` 절
2. `SkillEngine.strike(cast, execBase, roll2d6, resist)` → roll2d6·tierOf 명중 삭제,
   피해 = `max(1, 공격력 − 방어력) × 크리배수`. `SkillListener` 전투 루프: roll2d6() 제거·resist→방어력·
   크리 확률 굴림(액션 RNG). `npcDefenseScore`→방어력. **enabled 스위치로 전환** (켜기 전 무영향)
3. ★라이브 전투를 바꾸는 큰 변경 — 신중히. 배선 뒤 인게임 실측으로 【제안】 수치 튜닝

### 이 마라톤에서 선 것 (전부 커밋)

| 트랙 | 무엇 | 상태 |
|---|---|---|
| 검기 | 시트판 8차 라이브 승격 + 대각(roll 45)·시전자 추종·판 뒷면 UV(C↔Ɔ 수리) | 라이브 |
| 타격 피드백 | 대미지 숫자·표적 HP바·처치 흩어짐 + **접촉 무딜(딜은 획만)** + 공통 몹 명패 | 라이브 |
| ★런처 | Electron 전용 런처 — **공식 마크 재활용**(재로그인 없음·설정 이어받기·기존 모드 안전) · Windows CI 빌드 · MS Store 프로필 파일 문제·베드락 오픈·Sodium/Iris 짝 전부 수리 | 배포 (honcheon-pack 릴리스 `honcheon_setup_0.1.0.exe`) |
| 모드팩 | Fabric 1.21.11 · 10모드 + Complementary Unbound · 게이트 v0(fabric 브랜드) · Emotecraft 양단 | 라이브 |
| **성장 v3 (B-135)** | 단계 0(cultivation.yml levels)·1(원장 backfill 무DDL)·2(판정치=floor√원장) **완료**. 단계 3~5 남음 | 구현중 |
| **전투 v2 (B-177)** | 설계·config 완결 (2d6 제거·공방·크리 감각/지혜/무기/장비). **코드 배선만 남음** | 설계 완 |

### ★ 성장/전투 설계 정본 (다 읽어라)
- `docs/design/attribute_scale_v3.md` §8.9 — 저울 12항 승인 (B-137 **닫힘** · floor√원장)
- `docs/design/growth_v3_impl_plan.md` — 성장 6단계 배선 (무DDL 발견)
- `docs/design/growth_v3_feel.md` — 체감: 두 속도(레벨=조용·승급=연출+칭호)·칭호(세계반응만·B-176)·무공 문턱=대가곡선
- `docs/design/combat_resolution_v2.md` — 전투 v2 (B-177)
- 눈: `tools/judgment_scale_harness.py`(옛/새 동등 위반0) · `tools/growth_v3_backfill_selftest.py`

### 성장 v3 남은 단계 (B-135)
- 단계 3: 파생치 √원장 (내력 풀·이속·내구가 √원장 실수치를 읽게 — "몸 체감")
- 단계 4: 성장 로직 (XP→레벨업→3포인트→배분+은행 · 무공 문턱 대가곡선 · Growth/ProgressionEngine v3)
- 단계 5: UI (레벨업 연출·시트 원장 표기·획득 토스트) + 칭호 B-176
- ★MVT 다리: 원장 독립 성장 시 √원장을 attrDays 로 동기화 (단계 4 — 지금은 backfill 로 동등)

### 함정·주의 (이 마라톤에서 물린 것)
- **MS Store 런처는 launcher_profiles_microsoft_store.json** 을 읽는다 (독립설치판과 다름) — 둘 다 써야 함
- **minecraft:// 는 베드락** — 자바 런처는 Get-StartApps 로 AUMID 조회해 연다
- **Iris↔Sodium 정확 짝** — Iris 1.10.7 은 Sodium 0.8.7 요구 (0.8.13 충돌)
- 런처 CI: `launcher/` push → windows-latest 빌드 → `gh run download` → pack 릴리스 업로드 (workflow scope 필요)
- 원장 backfill 비파괴: 판정 권위는 원장으로 넘어왔으나 결과 불변 (원장=능력치² · 하네스 증명)

---

---

## 0. 먼저 읽을 것 (순서대로)

| 문서 | 무엇 |
|---|---|
| **`docs/BACKLOG.md`** | ★ **살아 있는 장부** — 95항목. 무엇이 남았는지 여기 있다 |
| `tools/backlog_audit.py` | 그 장부가 **자기 자신에 대해 거짓말하는지** 재는 눈 |
| `docs/design/gate_and_watertown.md` | 조성의 계약 (사용자가 직접 씀) |
| `docs/design/scale_systems.md` | 크기의 자 — 장소마다 다른 자로 잰다 |
| `docs/design/ami_architecture.md` · `noklim_seokchae.md` | 사용자가 직접 설계한 원형 둘 |
| `CLAUDE.md` · `docs/design/*.md` (81개) | 세계관·시스템 |

---

## 1. 이 프로젝트가 무엇인가

**무협 공유세계 RPG.** 한 사람이 만든다 (한국어 사용자, 솔로 개발자).

| 모듈 | 무엇 | 언어 |
|---|---|---|
| `server-mvt` | 마인크래프트 플러그인 (Paper **1.21.11**) — **몸이 하는 것** | Java 21 |
| `server-bot` | 디스코드 봇 (JDA + SQLite) — **인증과 소셜** | Java 21 |
| `core` | 룰 엔진 (판정·경제·성장) — 두 몸이 공유 | Java 21 |
| `domain` | 세력·지역 서비스 (의존성은 `:core` 뿐) | Java 21 |
| `resourcepack/` + `tools/build_resourcepack.py` | 팩 (수묵 무협) | Python |
| `config/*.yml` | ★ **단일 진실 원천** — 코드는 수치·이름을 지어내지 않는다 | YAML |
| `tools/*.py`, `*.java` | **눈**(감사) 27종 | |

**축**: **디스코드 = 인증 + 소셜.** **마크 = 몸이 하는 모든 것.** 로직은 백엔드.

---

## 2. ★★ 헌법 — 이것을 어기면 되돌려라

### 2.1 등록제
> **코드가 이름·수치를 지어내지 않는다.** `config/*.yml` 이 정본이다.
> 근거가 없으면 **지어내지 말고 사용자에게 물어라.**

★ 사용자의 절대 규칙: **"모르는 것·임의로 정할 것은 나에게 물어보고 정해야 한다. 추측을 통해 생성하는 건 금한다."**

### 2.2 ★ 침묵하는 실패 — **이 저장소의 근본 병**
> **코드가 모를 때 조용히 뭔가를 고르면, 그것이 곧 거짓말이 된다.**

사용자의 말이 이 병의 이름이다:
> **"모르는 장소를 작은 장소로 간주하는 것도 하나의 창작이다."**

**2026-07-13 하루에만 여섯 번 잡았다:**

| 무엇이 말했나 | 세계의 진실 |
|---|---|
| 팩 검수: "위반 0건" | 사용자는 **보라 큐브**를 보고 있었다 (텍스처가 아틀라스 밖) |
| 로그: "허수아비 3" | **0마리** (평화 난이도가 좀비를 지우고 있었다) |
| 로그: "발판 6" | **0개** (표본이 4만 칸 중 6칸을 못 봤다) |
| 조성: "여덟 문파가 섰다" | **전부 같은 집** (`SECTS` 하드코딩) |
| 봇: "마크가 꺼졌다" | 서버는 돌고 있었다 (`int` 에 에폭 밀리초 → 넘침) |
| 튜토리얼: "뛰어라" | **아무도 못 뛴다** (개화 전엔 허공 딛기 0회) |

**전부 장부가 세계에 대해 거짓말했고, 그 거짓말은 조용했다.**

### 2.3 ★ 눈을 만들면 눈을 시험하라
> **감사를 만들었으면 일부러 어겨서 그 감사가 잡는지 확인하라.**

**이 세션에서 눈이 자기 주인을 여러 번 물었다** — 그리고 그때마다 진짜 버그가 나왔다.
- 청구서의 눈이 **첫 끼니로 자기 장부에서 위반 5건**을 찾았다
- 접합의 눈이 **문지기를 뜯어냈는데도 통과**시켰다 (검사가 느슨했다)
- 지형의 눈이 **`if (false)` 로 감싼 죽은 코드**를 통과시켰다
- 서장의 눈이 **「없는 것」과 「꺼진 것」을 구별 못 했다**

**시험 없는 눈은 눈이 아니다.**

### 2.4 ★ 3계층 헌법 — 지도 → 땅 → 건축 (2026-07-15 개정 · B-147)
> ★★ **지도가 정본이다. 지도는 건축물을 포함한 맵 디자인 전체다.**
> **땅은 지도에 맞게 서고, 건축은 그 땅 위에 오른다.** (사용자 원문 — B-147)
> 땅의 치수(봉우리 정상부 폭·단의 넓이·진입로)는 **지도의 건축 footprint 에서 유도**된다.

**폐지된 구판 조항** (묘비 — 다시 세우지 마라):
- ~~"땅에 맞게 건물이 올라가는 것이지, 건축에 맞게 지형이 생기는 게 아니다"~~ — 지도가 건축을
  포함하므로 거짓이 됐다. 이 조항 아래서 봉우리가 건물보다 얇게 서고 건물이 묻히고 떴다 (B-146).
- ~~"건축의 미결이 땅을 막아서는 안 된다 (땅 먼저)"~~ — 지도가 완성되지 않은 곳은 땅도 빚지
  않는다. v4 의 「땅 먼저」 관행이 만든 30곳의 땅은 v5 갈아엎기의 대상이다.

**집행 안전핀** (권위는 지도에 있다 — 핀은 지도를 섬기는 기계로 존속):
> **땅은 한 번만 선다** (`TerrainLedger`) — 같은 지도로 두 번 빚지 않는다. 지도가 바뀌면 그때 다시 빚는다.
> **건축 집행이 선 땅을 임의로 바꾸지 못한다** (`TerrainSeal`) — 지도에 없는 변형은 위반이다.

땅을 고쳐야 하면 `LandRequest` 로 **요청**한다. 자세한 것은 `docs/design/gate_and_watertown.md`
(★구판 문서 — 이 개정과 어긋나는 곳은 이 절이 이긴다. 개정 반영은 사용자가 직접 쓴 문서라 사용자 몫).

### 2.5 결정론
> **조성기에 난수 금지.** 좌표의 순수 함수여야 한다 (같은 좌표 → 언제나 같은 결과).
> ★ 그러나 **좌표 해시는 파장이 한 칸**이라 이웃과 상관이 없다 → **점묘(點描)**가 된다.
> **격자점에서 뽑고 보간하라** (`config/terrain_grain.yml`). 난수를 다시 들이지 마라.

### 2.6 전투의 문법
- **격(格) 사다리**: 외공기0 → 발경1 → 검기2 → 강기3 → 어검4 → 심검5. **판정 축**이지 이펙트가 아니다
- ★ **몸짓이 곧 선택**: **방패=막기 · 웅크림=흘리기 · 달림=회피**
- **경공 = 더블 점프** (공중에서 점프 한 번 더). **개화(일류) 전에는 못 한다**
- ★ **콤보 창(window)·입력 버퍼를 들이지 마라** — 삼문(三門)을 잡아먹는다. 외부 설계서가 권해도 **거부한다**
- **화면 흔들림은 껐다** (멀미). **무게는 카메라가 아니라 몸이 밀리는 것으로 말한다**

### 2.7 팩 게이트 — **팩이 있어야 플레이 가능** (2026-07-13 개정)
옛 「팩 게이트 불가침」(팩 없어도 된다)은 **폐지됐다.**
**거절·다운로드 실패·무응답(45초) 전부 퇴장.** 다만 **왜 못 들어왔는지 한국어로 말한다.**
★ **옛 폴백 경로가 6개 도구·여러 등록부에 남아 있다** — 장부 참조.

### 2.8 집안은 능력치를 주지 않는다
> **집안이 주는 것은 힘이 아니라 「입장권과 빚」이다** (무가의 검법 1, 무가의 청구서).

---

## 3. ★ 보안·운영 규약 (어기면 사람이 다친다)

| | |
|---|---|
| ★★ **`DISCORD_TOKEN`** | **프로세스 env 에만 존재.** 파일·깃에 **절대 기록 금지**. **봇을 kill 하기 전에** `/proc/<PID>/environ` 에서 확보해야 재기동 가능. ★ **이 세션에서 내가 봇을 죽였다가 토큰을 잃었다** — 사용자가 손으로 되살렸다. **재기동 명령이 실제로 되는지 먼저 검증하고 죽여라** |
| **RCON 25575** | **포워딩 절대 금지** (공개는 25565만) |
| **팩 HTTP 8123** | 사설망 전용 — 공유기 포워딩 금지. 지금은 **GitHub 외부 호스팅**을 쓴다 |
| **DB** | ★ **봇이 단일 작성자.** MVT 는 sqlite 를 직접 열지 않는다 — **다리(`run/bridge`)로 청한다**. **DB 작업 전 백업 필수. 마이그레이션은 봇 정지 상태에서** |
| **PUA 문자 리터럴** | 금지 |
| **에이전트 규약** | git commit 금지 · `git reset`/`stash` 금지 · **서버 기동/RCON 금지** (조율자만 한다) |

### 빌드·배포
```bash
export JAVA_HOME=$PWD/run/jdk-21          # ★ 이거 없으면 "invalid source release: 21"
./gradlew :server-mvt:jar :server-bot:jar  # :server-faction 은 기존 의존성 문제로 실패 — 무시

bash scripts/redeploy_mvt.sh               # jar 복사 + config 동기화 + 재기동 (RCON stop)
```
★★ **빌드 통과 ≠ 기동 성공.** 이 세션에서 `String.format("%.0f", (long) x)` **한 줄**이 `onEnable` 에서 터져 **플러그인 전체가 안 켜졌다** (팩·나루·HUD 통째로 죽음). **배포 후 반드시 확인:**
```bash
grep -q "Error occurred while enabling HoncheonMVT" run/mvt/server-console.log && echo 안켜짐
```

### 봇 재기동 — **사용자가 직접 한다** (토큰 때문에)
```bash
cd <repo>
DISCORD_TOKEN='...' HONCHEON_GUILD_ID='1525178103335489739' \
HONCHEON_LLM_URL='http://localhost:11434/v1' \
HONCHEON_LLM_MODEL='qwen3-30b-a3b-instruct-2507-bartowski:latest' \
nohup ./scripts/run_bot.sh > run/bot/bot.log 2>&1 & disown
```

---

## 4. 지금의 상태 (2026-07-14 09:50)

### 살아 있는 것
- **봇** ✔ (스키마 8 · 초대 링크 등록됨)
- **마크** ✔ (Paper 1.21.11 · 팩 게이트 닫힘)
- **팩** ✔ GitHub 호스팅. 접속마다 sha1 재측정 + `?v=<sha1>` 캐시버스터
- **장부** ✔ 95항목 · 위반 0건

### ★ 방금 한 큰 일 — **세계를 갈아엎었다**
사용자가 **새 지형(v4)을 승인**하고 **전부 갈아엎기**를 지시했다.
- 삭제: 월드 3개(강호·나루·연무장) · 지형 원장 · 영수증 · 앵커 · 구역 · 플레이어 데이터 · 원지
- 백업: `run/backup-20260714-091228-갈아엎기/`
- 남김: 화이트리스트 · ops · 봇의 장부(지역·사람·소문)
- `config/terrain_gate.yml` → `approved_forge_version: 4`

### ★★ 다음 사람이 이어서 할 일 — **세계를 마저 세운다**

**지금 세계에 있는 것**: **청하현**(전용 조성기, 68초) + **화산파**(지형만, 729초). **끝.**

```
/혼천 세계조성          → 청하현 (전용 조성기)
/혼천 지형조성 <id>     → 그 곳의 땅만 빚는다 (건축 미결이어도 땅은 선다)
/혼천 지역조성 <id>     → 땅 + 집
```

**남은 곳은 30곳 안팎. 한 곳당 최대 12분.** 총 **5~6시간**이다.

★★ **조성은 느리다. 조용한 것이 아니다.** 이제 **15초마다** `[조성·진행]` 이 로그에 남는다.
**조용하면 그때가 진짜 사고다.**

### ★ 먼저 올려야 할 것 — 안전핀
**`max_seconds: 900` 인데 화산파가 729초.** 여유가 **19%** 뿐이다.
**곤륜·설산처럼 화산보다 높은 산은 안전핀에 걸려 중단될 수 있다** (B-100).
→ **1800초로 올리고 시작하는 것을 권한다.** 사용자에게 물어라.

### 권하는 순서 (조율자가 사용자에게 제안했고, 사용자는 아직 답하지 않았다)
1. **안전핀을 올린다** (900 → 1800)
2. **문파 대여섯 곳만 먼저 세워** 새 지형을 **눈으로 확인**한다
3. 좋으면 나머지를 돌린다

★ **땅은 한 번만 선다.** 30곳을 다 세워 놓고 "이거 아닌데" 하면 **되돌릴 수 없다.**
**되돌릴 수 없는 일은 작게 시작하라.**

### 【정정】 「지형 조성이 조용히 죽는다」는 **내 오진이었다**
나는 5분 기다리고 죽었다고 판단했다. **실제로는 729초 걸려 정상적으로 섰다.**
**진짜 병은 따로 있었고 훨씬 무서웠다**:
> **RCON 의 `sender` 는 명령이 반환되는 순간 소켓을 닫는다.**
> 12분간의 진행 보고가 **전부 죽은 소켓으로** 갔다.
> ★ **`err` 도 같은 곳으로 갔다 — 조성이 정말 터졌어도 콘솔은 똑같이 조용했을 것이다.**

**고침**: `Announce` — **먼저 로그에 적고, 그다음에 사람에게.** **로그는 아무도 안 죽는다.**
**눈**: `tools/silence_audit.py` — **침묵은 그 자체로 결함이다.** 그 눈이 놓친 곳을 **셋 더** 찾았다.

### 【정정】 「새 지형이 험하다」도 내 오진이었다
```
지면 요철   구판 [-1,1]  →  v4 [-1,1]    ← 진폭 동일
```
**v4 가 바꾼 것은 진폭이 아니라 상관(相關)이다** — 같은 크기로 흔들리되 **덩어리로** 흔들린다.
물을 늘리거나 벼랑을 만드는 코드는 **한 줄도 없다.** 험해 보인 것은 **월드를 갈아엎어 바닐라 지형이 새로 생성된 것**이다.

---

## 4-밤. ★ 최신 — 2026-07-14 밤 세션 (이 절이 §4~6 보다 새롭다)

### 상태 (23:05 기준)
- **마크** ✔ (Paper 1.21.11 · 22:59 기동 · HoncheonMVT 오류 0) — **봇** ✔ (PostgreSQL · 21:05 기동, [마크와 잇기] 버튼 탑재)
- **팩** ✔ GitHub sha1 `8eebae49` (서장 사건음 sounds.json 포함)
- **장부** `docs/BACKLOG.md` — 이 밤에 B-116~127 신설 · **B-113·B-114 닫힘 (증거: 환경검수 ⑥ 0.00%)**
- HEAD 자립 ✔ — 표류 7,100줄 회수됨. **부분 커밋 전 격리 워크트리 빌드가 관행이다** (HEAD 가 빌드 불능이었던 적 있다)

### 이 밤에 닫힌 것 (전부 실사용 피드백 기원 · 커밋됨)
| 항목 | 무엇 | 사람 눈 확인 |
|---|---|---|
| B-113·114 | **지하 3.86% 의 진상 = 조성기가 산 채로 묻은 나무** (7차 수사). 치유 48,519칸 → ⑥ 0.00% | 닫힘 |
| B-116·123 | HUD 액션바 한 줄의 주인 (HudLine) + 입도진 과제 카운트는 판정 뒤에 | 격 순환·더블점프 |
| SJ-002 | 서장 대기("붓이 적고 있다")·도착 타이틀·사건음·자동 펼침 | 서장 재체험 |
| B-117 | 접속 = [마크와 잇기] 버튼+모달 (명령은 뒷문) | 안내판 재실행 후 |
| B-118 | 서장 끝나기 전엔 안 내보낸다 (다리 명단 판정) | 부계정 재현 |
| B-119 | **살아 있는 것은 다 맞는다** (NPC 무적 철거 · npc_death 다리 사건) — ★사용자 확정: "몰래 죽일수도 있어야" | 산길 짐승·NPC 타격 |
| B-120·124 | 자동 출도는 부두에서만 · 못 하는 과제는 이유를 말한다 | 마당 몸짓 |
| B-121 | 뱃사공 섭구 (입도 안내자 — 흑수나루) | 우클릭 인사 |

### ★ 이 밤의 사고 둘 (다음 사람은 같은 함정을 밟지 마라)
1. **나루에서 /혼천 조성** → 마을이 나루 세계 심부(y−56)에 재조성, 앵커 9종·구역·**지형 영수증까지 덮임**.
   복구: 장터 앵커 손 복원(서버 정지 상태에서) → 콘솔 조성. **뿌리 = 조성 명령의 세계 가드 부재 (B-126, P2)**.
   부산물: 나루 세계 심부에 묻힌 마을 잔해 + 그 위 스윕 채움 (실피해 없음 판정 — 정리는 나루 월드 재생성 때).
2. **지하정리 첫 실사격 미발** — 표적을 낡은 구역(−2,−56,−1)으로 오독, 원점 심부 85k칸 채움 (실피해 없음).
   수리됨: 해석기 공유(resolveTerrainTarget) + 안전핀(sweepTargetSane — cy<0·괴리 40칸 거부). **상태 변경 명령은 표적 좌표를 먼저 출력·검증한다.**

### ★★ 다음 세션이 이어서 할 일 (우선순위)
1. **B-127 (P3·경계 회귀)**: 복구 재조성 후 ③ 급단차 8.5%·④ 진입 0/4 (전엔 3.5%·4/4). 원인 후보:
   영수증 소실로 이미 깎은 땅을 또 깎음 + 손 복원 앵커 1칸 오차(y89↔90). **재조성 또 돌리지 마라 — 세 번 깎인다.**
   가장자리만 다듬는(feather 재적용류) 수리가 맞다. 사용자에게 눈에 밟히는 턱 좌표를 받아라.
2. **B-126 (P2)**: 조성 세계 가드 — 나루·연무장에서 거부.
3. **B-125 (P4)**: `cheongha_hyeon` 낡은 구역(나루 세계 전고도 상자) 정비 — 환경검수 지역 경로도 이걸 읽는다.
4. 사람 눈 대기: 위 표의 확인 항목들 + `/혼천 입도 재조성`(플레이어 전용 — 허수아비 12→6·글판 재생성. 그 후 `antechamber_audit` exit 0 확인).
5. R6~R8 커밋들의 Codex 검토 요청 (ACTIVE_V6 갱신 — 이 밤엔 병렬 트랙만 돌았다).
6. 대형 트랙 재개: B-110 구현(세계 시계) · SJ-003(서장 다리 계약) · B-122(몰래 죽이기 설계) · 상단 6곳 시안(D-2).

### 이 밤의 계율 추가분
- **안내에 "어디서"를 빼먹지 마라** — 조성 사고의 공범은 조율자의 반쪽 지시였다.
- **플레이어 전용 명령은 RCON execute-as 로 안 된다** (Bukkit sender 프록시 불가) — 사람에게 부탁하거나 콘솔 경로를 설계하라.
- **눈이 재는 곳과 손이 걷는 곳은 같은 해석기여야 한다** — 두 벌이면 언젠가 다른 곳을 가리킨다.

---

## 4-새벽. ★★ 최신 — 2026-07-15 심야 세션 (이 절이 §4-밤 보다 새롭다. 여기서 시작하라)

### 다음 세션의 첫 다섯 걸음
1. 이 절 → `docs/design/cultivation_v3_levels.md` → `docs/BACKLOG.md` (순서대로 읽는다)
2. `python3 tools/backlog_audit.py --run` (장부의 눈 — 이 절 끝의 상태와 대조)
3. 사람 눈 대기 항목을 사용자에게 확인 (아래 표)
4. **병렬 하네스를 연다** (아래 §웨이브-1 — 파일 소유가 갈라져 있다. 겹치면 빌드가 깨진다)
5. B-137 시안이 나오면 사용자 승인을 받고 나서야 B-135 구현이 풀린다 (직렬 관문)

### 이 세션에서 닫힌 것 (전부 커밋됨)
| 항목 | 무엇 | 증거 |
|---|---|---|
| B-019 | 허수아비 12몸의 진범 = **눈의 이중 계수** (ipdo_dummy ⊂ ipdo_dummy_label) | antechamber_audit exit 0 · 계수기 합성 시험 3종 |
| B-128 | ★ 커밋 8a50548 일괄 치환이 **장부를 독살** (닫힘 45곳 복제·상태 20건 전복) → 43곳 복원·가짜 닫힘 18건 재개방 | backlog_audit ③-b 복제 눈 + 상처 ⑫ 신설 |
| B-127 | 경계 회귀 — 원인 = 영수증 소실 재조성의 이음 띠 상실 → **`/혼천 경계다듬기`** 신설 (feather 재적용) | 환경검수 ③ 6.9% ✅ · ④ 통과 ✅ |
| B-131 | 글판 두 장 겹침 — 가림이 엔티티 적재보다 먼저 달렸다 → onPanelsLoad 재적용 | 사용자 확인 (순차 공개 첫 작동) |
| B-132 | 허수아비가 **대립 판정을 굴렸다** (계기가 주사위를) → basicMelee 우회 | 사용자 확인 (과제 해결·관문 진행) |
| B-126·B-119 보완 | 조성 세계 가드 (5명령) · NPC 이행 기동 스윕 (Codex P1 지적) | 기동 로그 — 사람 눈 대기 |
| R6~R8 | Codex 검토 **조건부 승인** (14 통과 · B-116/119 조건 보강 · B-129/130 등재) | CODEX_REVIEW.md §R6~R8 |

### ★★ 헌법 개정 — cultivation v3 (이 세션의 대사건)
사용자가 v2 「레벨은 없다」를 폐기하고 **경험치·레벨·스탯 포인트**를 확정했다.
정본: `docs/design/cultivation_v3_levels.md` — **페이싱까지 전부 승인됨** (실측 재보정 유보):
- 병행+영역 분리: **"스탯은 레벨업에서 찍고, 기술은 써야 는다"** (XP→능력치 · 행위→기술)
- 승급 = 자격 레벨 + 사건 마크 (혼합) · 만렙 없음 · **매 레벨 3포인트**
- 사다리 (base 20 · growth 1.0565 · R 200/h 가정): 삼류 1h/Lv10 (첫 시간에 무공!) · 이류 15h/40 ·
  일류 60h/65 · 절정 140h/80 · 초절정 240h/90 · **화경 400h/Lv100 (최종장 앵커)** · 현경 550h/105 · 생사경 900h/115
- 하드코어 8h/일 = 화경 50일 (삼파전 최소 개전 60일 — 간극 10일, 사건 마크가 둘째 핀)
- 모델: `tools/xp_pacing.py` (자기 시험 5눈) — 실측 오면 R 갈아 끼우고 재산출
- 죽음의 레벨 처분 없음 · 바닐라 XP바 = 경험 · 레벨 소비처 제거(B-136)
- ★구현 관문: **B-137 능력치 저울 재설계** (300pt 를 담는 새 눈금 + 판정 공식 재환산) — 이것이 안 서면 B-135 구현 금지

### ★ 웨이브-1 — 병렬 하네스 (파일 소유 분리 · 겹침 금지)
전부 서로 독립이다. 에이전트 규약: git 금지 · gradle 금지(조율자가 빌드) · 서버/RCON 금지.

| 트랙 | 일 | 소유 파일 | 낼 것 |
|---|---|---|---|
| A. B-137 저울 시안 | 300pt 눈금 후보 2~3안 (주사위 폭·판정 재환산·NPC/장비 연쇄 포함 장단점 표) | docs/design/attribute_scale_v3.md (신설) | 사용자 결정용 시안 |
| B. B-136 선행 구현 | 몹 XP 오브 흡수 + 모루·인챈트대 레벨 소모 철거 (B-137 무관 — 절연층) | 새 Listener 1파일 (예: XpEconomyGuard.java) | 코드 + 기동 실측은 조율자 |
| C. 커스텀 HUD 바 | 내공/내력 **별도 바** (보스바 채널 1순위) — XP바 이사의 선행 | 새 클래스 1파일 (예: EnergyBossBar.java) · SkillHud.energyBar 는 조율자가 이사 | 코드 (디자인 다듬기는 사용자 RP) |
| D. B-110 세계 시계 | WorldClockEngine — advanceWorld 편입·막 진입·박 발화·/막개전 (설계 완료 world_clock.md) | 봇 측 새 클래스 + world_clock.yml 배선 | 코드 + clock_audit 대조 |
| E. RP-4 몹 파일럿 준비 | horangi_v2.bbmodel + 5동작, A안 MobDisplay v2 확장 사양 vs B안 ModelEngine 어댑터 사양 (파트 예산 64 vs 140 선설계 포함). MythicMobs 는 복합 몹 수요 때 별도 ADR | docs/design/rp4_pilot.md (신설) | 비교 데모 계획 (구현은 승인 뒤) |
| F. 상단 6곳 원형 시안 | 태원·무한·소주·강남상로·고창·돈황 `archetype: pending` (B-047) — 기존 원형 21종·수향 문법 위에서 시안. **등록부 반영은 사용자 승인 뒤** | docs/design/sangdan_archetypes.md (신설) | 사용자 결정용 시안 |

**F′. 맵 건축 — 조성 실행 (★조율자 전담 · RCON — 에이전트 금지. 웨이브-1 이 도는 동안 배경으로)**
- 남은 곳 ~30곳: `/혼천 지형조성 <id>` (땅 먼저 — 건축 미결이어도 땅은 선다) → 등급 정해진 곳은 `지역조성`
- ★먼저 **B-100 안전핀 900→1800초 사용자 승인** (화산파 729초 — 곤륜·설산이 걸린다) · 순서는 `scripts/forge_world.sh` 의 우선순위 참조
- 조성 뒤 루프: `/혼천 환경검수 <id>` → 위반 시 **`/혼천 경계다듬기 <id>`** (이 세션 신설 — B-127 을 닫은 손) → 재검수
- 15초마다 [조성·진행] 로그 — **조용하면 그때가 사고다** · 나루·연무장에서 조성 명령 금지는 이제 코드가 막는다 (B-126)
- 딸린 미결: B-125 낡은 구역 정비 (환경검수 지역 경로가 읽는다) · B-085 (2계층, 진행 중) · 사용자 결정 대기: 수향 프로파일(B-094)·새외 4곳(B-048)·점창·청성(B-049 — 사용자 직접 설계)·제갈(B-050)

직렬 (웨이브-2, 관문 뒤): B-135 구현 (B-137 승인 뒤 — XP 원장 스키마는 PG 마이그레이션 경로 준수)
· B-134 기술 층 (미결: 기술 문턱·심안의 행위·발판 철거·나루 가르침) · B-136 가격표 (사용자)
· RP-4 파일럿 구현 (시안 승인 뒤) · 상단 6곳 조성 (원형 승인 뒤 F′ 루프에 편입).

### 사람 눈 대기 (사용자 확인 항목)
나루 허기(B-133 — 달림 과제) · 나루에서 조성 거부(B-126) · 마을 NPC/짐승 타격(B-119) ·
B-118/120/121/123/124 재확인 · 청하현 네 방위 도보 진입 (B-127 은 기계가 닫음 — 눈은 환영).

### 이 세션의 계율 추가분
- **일괄 치환을 장부에 대지 마라** — 닫힘 필드는 그 항목의 것이다 (B-128 의 병).
- **눈의 시험이 남의 병에 기대면 병이 나을 때 시험도 죽는다** — 표본은 설계상 불변이어야 한다 (always_barks).
- **기본값이 「보임」인 가림은 침묵이 곧 겹침이다** — 가림은 대상이 실린 뒤에 다시 걸어라 (B-131).
- **계기와는 대립하지 않는다** — 측정 대상에 주사위를 굴리면 측정이 죽는다 (B-132).
- 미커밋 외래 변경(팩 게이트 off)을 발견하면 **묻고 되돌린다** — 이 세션에서 실제로 있었다 (출처 불명).

### 상태 (세션 종료 시점)
마크 ✔ (01:20 기동 · 오류 0) · 봇 ✔ (PostgreSQL) · 장부 위반 0 (열림 ~53 · 닫힘 ~45) ·
HEAD = bfa1179 언저리 (전부 커밋·빌드 통과) · .orig 4파일은 PG 컷오버 백업 — 정리 여부 미결.

---

## 4-판. ★★★★ 최신 — 2026-07-22밤~23새벽 마라톤 세션 (이 절이 §4-검기 보다 새롭다. 여기서 시작하라)

**한 줄**: 검기가 「시트→판(일러스트)」 방식으로 확정 궤도에 올랐고(8회차 · 베기면 이론),
월아산이 봉으로 대체됐으며, 세계가 **RPG 규약**(블록 파괴·설치 없음)이 됐고, 원거리는
이펙트 문법의 기본 틀이 섰다. 명령어 모음집이 노션에 생겼다.

### 상태 (2026-07-23 05:30)

| 무엇 | 상태 |
|---|---|
| 커밋 | `0206298`(검기 시선추종) → `b98c78b`(봉) → `6ea2fe3`(RPG규약) → `1d54bcf`(활 1단계) → `234bc89`(원거리 fx) → `41c2b0f`~`0aed9d9`(시트판 1~8차) |
| 라이브(run/mvt) | 검기 = **v5 밴드 그대로** + 이 세션의 코드 전부 탑재(시선추종·봉·BlockCovenant·활 사선 v1). 마지막 재기동 02:35 · 오류 0 |
| 테스트(run/mvt-test) | 시트판 8차 구성 (medium 판 · 사본 config) — 봇 kigibot/kigicam 이 붙어 있을 수 있다 |
| 팩 | GitHub `c6b162b3` — 3자 일치(로컬=GitHub=플러그인). ★재빌드하면 라이브 서빙 zip 도 갈리므로 **즉시 재발행 필수** |
| 노션 | 명령어 모음집 — https://app.notion.com/p/3a5b362f1ade8104bb6fdfbcb720835b (정본은 코드 — 명령 바뀌면 갱신) |

### 이 세션에서 닫힌 것 (장부 전부 갱신·커밋됨)

| 항목 | 무엇 | 증거 |
|---|---|---|
| B-171 | 검기 국소축 **부호 버그**(yaw 45°에서 초승달 붕괴 — 4곳) + 「획은 하늘을 보지 않는다」 폐지(pitch 추종. 판정도 같은 거울) | 사용자 인게임 「시선대로 나감」 |
| B-172·B-162 | **월아산 삭제 → 봉 대체** (계열·디자인·이주 별칭 2겹 · 죽은 등록 5벌 청산 · 개방 타구봉 bong 베이스 제자리) | lint·combat_audit 0 · stone_shovel.json 이주 분기 |
| B-161 | ★ **"채굴은 없는 세상, 블록 부수기·설치 자체가 없음 — RPG 세상"** (BlockCovenant — 관리자 크리에이티브만 예외) | 파일 + 배포 |
| B-173 신설 | 병기 적합성 점검 절차 (weapon_fitness_review.md) — 1회차: 모션 죽은 행 보류 · 활 착수 · 암기/중병기 대기 | 문서 |

### ★ 검기 = 시트판(일러스트) — 8회차 확정 스택 (정본: skill_motion_eval_v6.yml)

**방식**: `tools/sheet_to_plate.py` 가 컨셉 시트 TPS 초승달을 RGBA 판 텍스처 3단(피크→빈짐→소멸)
× A/B 반전으로 굽고, kigi_slash 판 경로(medium: 판)가 스윙마다 세운다. **스윙당 엔티티 1장 —
점 2,600개의 예산 충돌이 사라졌다** (라이브 반영의 장애물 없음).

**확정값 (전부 사용자 승인)**: 베기면 고정(`yaw_deg: 90` 신설 축 · billboard FIXED — 정면·후면
칼날처럼 얇고 측면이 그림 전체) · **오목면이 시전자를 감싼다**(반전 텍스처 B 채택) · scale 2.6 ·
몸 앞 1.2m(forward) · 두 스윙 방향 통일 · **점 층 완전 오프 기본** · 농도 2차(문턱10·감마0.55·
×1.15 + 채도 1.25).

**실측 기록**: scratch/camtest/시트판r1~r8 · 아티팩트 https://claude.ai/code/artifact/b970b3c9-7802-4ca2-8294-44cb663961c9

**미결 4**: 빈짐 단 농도 추가 여부 · 시전자 1인칭 보강(본인에겐 얇게만 보인다 — 이론상 맞음) ·
지속 시간 체감 · **라이브 반영 결정**.

### ★ 원거리 — 이펙트 문법 (방향 수정 주의)

- 사선 판정(RangedShot — 벽=엄폐 · 도달 틱 판정)과 `ranged_fx` 3모드(즉발/먹줄/주행)는 산다.
  1회차 4판: 아티팩트 https://claude.ai/code/artifact/5bce9b8b-0e50-48ee-a747-5699ab0d254d
- ★사용자 방향 수정: **바닐라 활 폐기** — 활 형상의 **별도 무기 + 신규 모션 예정** (이름·베이스
  아이템·몸짓 = 사용자 결정 대기). EntityShootBowEvent 훅은 시안 계측용 임시 (B-174 추기 참조)
- 미결정: 사선의 그림 기본 틀 (먹줄 vs 주행 — 무기와 독립, 아직 답 없음)

### 함정 (이 세션에서 전부 실제로 물렸다 — §4-검기의 함정 3종에 추가)

- **팩 굽기 경로 겹침**: MODEL 상수가 이미 models/item 인데 "item"을 또 붙이면 클라만 죽는다
  (보라 큐브). **서버 진단(획 로그)은 못 잡는다 — 범인은 클라 로그** `Missing block model`
- **양면 판의 180° 회전은 시각 무효** — 앞·뒷면이 각자 제 시점에 맞게 그려진다. 좌우 반전은
  회전이 아니라 **텍스처(A/B)** 로 한다
- **봇 리퍼 자기일치**: bash 명령줄에 스크립트 본문이 실려 키워드 grep 이 자기 부모를 문다
  (exit 144). 판별은 **exe/cwd 로만** (`readlink /proc/pid/exe·cwd`)
- publish_pack/serve_resourcepack 은 **라이브 서빙 zip 을 갈아 끼운다** — 시안 굽기 후 GitHub
  재발행을 잊으면 라이브 접속자가 sha1 불일치로 퇴장한다 (이 세션에서 두 번 즉시 봉합)
- redeploy_mvt.sh 기동 대기 60→150초 수정됨 (성공을 exit 1 로 오보하던 것)

### 다음 세션 안건 (우선순위)

1. **시트판 미결 4** 중 라이브 반영 결정이 가장 크다 (장애물 없음 — 사용자 결정만)
2. **양대 관문**: A. 성장 저울(B-137 수치표 승인 → B-135 구현 해금) · B. 세계 밑감(B-148)
3. 원거리 새 무기 설계 대기 (이름·베이스·몸짓) + 먹줄/주행 결정
4. 친구 피드백 오면: 빨간펜→sheet_to_plate 재추출 (전달문 아티팩트 2종: cdb7832f · b970b3c9)
5. 장부 위반 3건 정리 (B-011 닫힘 근거 공란 · B-170 증거 형식·유령 파일 — 전 세션 것)

## 4-검기. ★★★ 최신 — 2026-07-22 검기 템플릿 세션 (이 절이 §4-새벽 보다 새롭다. 여기서 시작하라)

**한 줄**: 검기가 「시트→점 템플릿」 방식이 됐다 — 디자이너 컨셉 시트의 그림을 픽셀로 읽어
그대로 게임 점 2,600개로 옮긴다. 그림이 곧 배치도다. 13회 실측 루프 + 독립 검토 에이전트 1회.

### 상태 (2026-07-22 17:00)

| 무엇 | 상태 |
|---|---|
| 커밋 | `39358fb`(평가판 v6 색감) → `6464010`(템플릿 파이프라인+10회 루프) → `4cf396c`(먹선 백킹·r13) |
| 라이브(run/mvt) | **v5 밴드 그대로 — 안 건드렸다** (`geom_template` 키 없음 = 템플릿 미발동, 예산 600/틱 정본) |
| 테스트(run/mvt-test) | r13 빌드가 돌고 있다. 평가 전용 상향 2건: performance.yml 시야 예산 600→**4000**·전역 4000→**12000**, config 는 `config/skill_motion_eval_v6.yml` 사본 |
| 디자이너 전달물 | `작업물/검기/전달_시트그대로_v9.zip` (전달문·GIF 실속도+슬로모션·피크·오프라인 HTML) — **친구 평가 대기** |

### 파이프라인 지도 (전부 커밋됨)

- `2db88914-….jpg`(루트) = 컨셉 시트 정본 — `tools/sheet_to_template.py` 가 여기서 추출
  → `config/kigi_template_{tps,fps,peak}.json` (관전자/1인칭/대조) → `QiTemplate`(mtime 핫리로드)
  → `QiGeometry.emitTemplate`(관전자=베기면 데칼+먹선 백킹, 시전자=빌보드, y순 스윕 공개)
  → 발동 조건: `kigi_slash.geom_template` 키 (없으면 v5 밴드 — 기존 불변)
- 루프의 눈: `scratch/sheet2game/compare.py` (색 비율·종횡비·IoU) · 회차 기록 `scratch/camtest/v7템플릿_r1~r13`
- 13회 루프에서 배운 것 전부: `git log 6464010 4cf396c` 커밋 본문 (실패 포함 — 반복하지 마라)

### ★ 사용자가 정해야 할 것 (다음 세션 안건)

1. **친구 평가 결과 반영** — zip 전달 후 빨간펜·256×256 텍스처 가이드·도끼 그림이 오면
   `sheet_to_template.py` 입력으로 그대로 재추출
2. **라이브 반영 여부와 예산** — 템플릿 밀도는 시야 예산 4000/틱 전제. 정본 600은 20인 군집
   근거라 충돌. (절충안: 라이브용 점수 축소판 or 예산 층위화)
3. **★팩 지뢰** — `resourcepack/…/particle/generic_0~7.png` 이 축소형(불투명 16~43/64px)이라
   **팩 로드 화면에선 모든 dust 가 잘게 보인다**. camtest(팩 없음)와 실제 화면이 다른 원인.
   사용자 인게임 확인 후: dust 전용 텍스처 분리(`particles/dust.json`) 여부 결정
4. 미착수 개선안(독립 검토가 남김): 날 림 폴리라인 연속 획 · trail 소멸 드리프트 ·
   붓점 텍스처 4단(`ink_dot_0~3` 커밋됨 — 팩 재빌드부터 유효)

### 함정 (이 세션에서 전부 실제로 물렸다)

- `pgrep -f` 대기 루프는 **자기 cmdline 자기일치** → 무한 대기. 판별은 `readlink /proc/<pid>/cwd`
- jar 교체는 **정지→복사→기동** + `rm -rf plugins/.paper-remapped` (캐시 어긋나면 플러그인이
  **조용히** 안 실린다 — 혼천 명령 불완전·효과 0 이 증상)
- dust_color_transition 은 생애 전체 선형 감광 — 피크가 밝아야 하는 연출엔 못 쓴다
  (`SkillHud.emitTransition` 훅은 기술·보스용으로 남아 있다)
- 촬영 측정은 15fps vs 100ms 피크라 회차 간 편차 큼 — 단일 피크 프레임 수치로 회차 비교하지 말 것

## 5. 이 세션에서 세운 것 (요약)

| | |
|---|---|
| **팩** | 텍스처가 **아틀라스 밖**에 있어 획·짐승이 보라 큐브였다 → `textures/item/` 으로. 축 ⑮(아틀라스)·⑯(비율)·⑰(중심) 신설 |
| **전투** | 히트스톱 · 넉백(플레이어에게도) · **기본 초식의 시간 구조** · 참격 모션(호 110°) · 타격 파티클. ★ **무공 쿨다운 중 검이 피해를 한 점도 안 내던 버그** 수정 |
| **경공** | 자동 → **더블 점프** (`PlayerToggleFlightEvent` 가로채기). 크리에이티브 안 깨짐 |
| **나루(입도진)** | 발판 0개였다 (표본이 못 봄) → 이정표는 **전수로** 센다. 튜토리얼이 **못 하는 것을 시키던 것** 수정 |
| **연무장** | 리붓 시 **진짜 인벤토리·장부가 사라지던 것** → `dojang.yml` 금고 (원자적 저장) |
| **지도** | 크기 = 영향력의 함수. `roster` 의 `martial + mandate_weight`. **상업 거점은 별도 사다리** |
| **조성** | 원형 5종 → **21종**. 여덟 문파가 전부 「화산의 도관」이던 것 수정 |
| **접합** | 코드 폐기 → **닉네임 + 게임 안 수락**. *"닉네임은 열쇠가 아니라 수신인이다"* |
| **디스코드** | **상시 안내판 + 버튼**. 명령을 칠 일이 없다. `소문`·`전장`은 **이관이 아니라 형태의 소멸** (NPC 가 말한다) |
| **가문** | 집안이 **실체**가 됐다 (`houses` 표). 성씨 · 적서 · 형제(10%) · **집을 나와도 형은 형이다** |
| **서장** | 디스코드 스레드 → **마크의 책**. **배는 한 명씩** (LLM 직렬화) · **미리 쓰기** |
| **초기화** | `/혼천 초기화` · `/초기화` — 백업 자동 · 세계는 안 건드린다 |
| **청구서** | `docs/BACKLOG.md` + **그것을 재는 눈** |

---

## 6. ★ 사용자가 정해야 할 것 (지금 대기 중)

**`docs/BACKLOG.md` 의 `보류` 30건이 전부 이것이다.** 급한 것:

| # | 무엇 |
|---|---|
| **B-092** | **이미 빚어진 땅** — 갈아엎기로 해소됐는지 확인 필요 |
| **B-097** | 아우가 났을 때 **게임 안에서도 알려 줄 것인가** (지금은 디스코드 DM 뿐) |
| **B-094** | **「수향」 프로파일** — 강남의 물골목. 점묘 승인 뒤가 순서 |
| — | **소림·불가가 남성 전용인가** (등록부에 근거 없음. 아미만 여성 근거 있음) |
| — | **미결 원형**: 상단 6곳 · 새외 4곳 · 점창·청성(**사용자가 직접 설계하기로 함**) · 제갈 기관저택 |
| — | **§17 사다리의 80·40** 은 근거 없음 (64 만 실측 근거) |

---

## 7. ★ 조율자(coordinator)에게 — 이 세션에서 배운 것

1. **에이전트를 병렬로 돌리되 파일 소유를 갈라라.** 겹치면 빌드가 깨지고, 반쯤 만든 코드를 배포하면 **세계가 죽는다** (실제로 두 번 그랬다).
2. **배포 전에 `onEnable` 이 성공하는지 확인하라.** 빌드는 통과하는데 기동이 죽는다.
3. **에이전트의 보고를 그대로 믿지 마라.** 이 세션에서 **씨앗 중 10개가 사실이 아니었다** (레이캐스트 상한은 **있었고**, 파티 엔진도 **있었다**). 청구서 담당이 코드로 확인해서 잡았다.
4. **사용자의 말을 옮길 때 뒤섞지 마라.** 나는 *"성향 → 집안"* 과 *"집안 → 성격"* 을 뒤섞어 읽고 **담당에게 틀린 지시**를 내렸다. 사용자가 직접 바로잡았다.
5. ★ **모르면 물어라.** 사용자는 **지어내는 것을 싫어하고, 묻는 것을 싫어하지 않는다.** 그의 답은 거의 언제나 설계를 더 좋게 만들었다 (적서·거절·형제의 비대칭 전부 그가 냈다).
6. **되돌릴 수 없는 일 전에는 백업하고, 무엇을 지우는지 먼저 보여라.**
