# 혼천(渾天) — 세션 인수인계

> **2026-07-14 작성.** 다음 세션이 **읽고 바로 일할 수 있게** 쓴다.
> 이 문서는 **지도**다. 자세한 것은 각 문서로 넘긴다. **거짓말하지 않는 것이 유일한 목표다.**

---

## 4-삼도천. ★★★★★★★★★ 최신 — 2026-07-24 밤 세션 (이 절이 §4-거울 보다 새롭다. 여기서 시작하라)

**한 줄**: **나루 = 삼도천 화폭 구현 완료 (5차)** — 명계 개정(서=넋등 어둑·동=등롱 밝음 · 구간
갈림) + 새벽녘 고정 + 중경(고사목 12·진흙 둔덕·옛 잔교+석등·피안화) + 원경(동쪽만 실루엣+
불빛 한 점). 사용자 확정 문답 2건 · 눈의 시험 **65/65** · 감사·lint·장부 위반 0 · 배포 ✓
(기동 오류 0) · 월드 백업 후 제거 — **첫 입장 때 재생성** (census 로그가 그 순간을 기록).

### ★★ 다음 세션 첫 걸음
1. **튜토리얼 마무리** (사용자 지시: "인게임 실측은 튜토리얼 완성 이후"): B-179 서장=기억의
   회랑 설계 회차가 유력한 다음 조각. 그다음 실측 몰아서 — §4-거울 1번(단계 5 UI·뿌리내림
   정거장 9) + **삼도천 화폭 체감** (수치·문장 전부 【제안】 — 빨간펜 대상)
2. B-177 전투 v2 남은 빚 · B-176 다음 조각 · XP 재보정 — §4-거울 2~3번 그대로

### 이 세션에서 선 것 (커밋 · 라이브)
| 무엇 | 핵심 | 정본 |
|---|---|---|
| ★명계 개정 | 넋등(광량 10)과 잔교 밝기 계약(암흑 ≤15%)은 물리적으로 양립 불가 → **구간을 갈랐다**: 서=저승(어둑함이 정본·dark_pct 창 40~92) · 동=이승(TownAudit 눈금). 경계=나루 관문 서쪽 끝 (등록부 유도) | antechamber.yml lighting.soul |
| ★새벽녘 고정 | 확정 문안의 「황혼」은 서쪽 하늘이 주홍 — 「서=저승 청백·동=이승 주홍」 축과 반대말 → 새벽(22900 【제안】): 동쪽만 주홍 = 환생의 방향 | canvas.fixed_time |
| 화폭 | 넋등 잔교(서 5칸 촘촘·부두 등롱 9칸) · 고사목 12그루(남서 — grain_shift 위상·죽은 가지) · 진흙 둔덕(grain 중간띠) · 옛 잔교+석등 2(북 z-9) · 피안화(둔덕 위) · 원경 동쪽만(실루엣+불빛 x58) · 수련잎 9→21 | canvas 절 · plan() ⑤-2~4 |
| 문장 | 글판·사공 대사에 저승의 결 ("갓 온 넋들"·"산 자의 땅이 아니다") — 전부 【제안】 | arrival·ferryman |
| 눈 | audit ⑧ 광원 **세기** 모형(넋등 10·등롱 15) + ⑧-2 화폭 눈 9종 + 뮤테이션 10 신설·2 재표적(㉒㉓) | antechamber_audit(+selftest) |

### ★2부 (같은 날 밤) — 과제 폐지 = 순수 문지방 완결 (사용자 지시)

**한 줄**: "아직 입도진에 과제가 존재" → 3차 개정이 남긴 「접속」 과제와 **과제 기계 전부**를
걷었다 (진척 장부·감지 3종·명령 기입·순차 공개·예고 변형·done/all_done·revisit_ledger_line).
**나루는 시험하지 않는다** — 판=안내판(stations[나루].panel · 옛 문장 그대로 승계), 문=이름,
발판=명령 대행. 눈: lessons 절 **부활 자체가 위반** + 코드 잔재 눈 + 뮤테이션 9건 묘비·재표적.
★함께 잡은 실기 결함: **판이 같은 칸을 두 번 적어** 완결성 검증이 제 판에 속아 94% (매 진입
재조성 직전 — 콘솔 ERROR가 증거) → plan() 겹침 걷기(마지막 기록·마지막 자리) + 눈 ⑫ +
뮤테이션 2. 눈의 시험 **62/62** · 감사·lint·장부 0 · 재배포 ✓. 정본: B-121 ★6차 진행 기록.

### ★3부 (2026-07-25 새벽) — B-179 기억의 회랑 = 삼도천 항해 (시안 확정 4건 + SJ-101~104 배선)

**한 줄**: **강을 건너는 동안이 곧 서장이다** — 종(또는 접합)이 배를 띄우고, 배가 안개 물길을
느리게 동진하며 기억의 정거장 3곳(넋등 문)에서 그 장의 책이 열린다. 동쪽 기슭(이승의 불빛 —
원경이 기슭 자리 x92 로 이사)에 닿는 순간이 곧 출도. 책 파이프·봇 권위·조판은 한 줄도 안 바뀜.
`Voyage.java` 신설 · 승선 세 길 · 갇힘 금지(명단 끝=기슭 직행 · 재접속 relocate · 도강 탈출).
**붓이 느린 것이 전제** (사용자: "ai 출력 속도가 많이 느린 걸로 암") — 0.5칸/초 항해가 붓
~22초+를 덮는다. 눈 ⑧-3 11종 + 뮤테이션 7 — **눈의 시험 69/69**. 정본: seojang_presentation.md
§0 · B-121/B-179 진행 기록. 남은 조각: 계열별 기억 조형(디스플레이 — 조형 시안 회차) ·
**신규 캐릭터 실기동** (닫는 조건 — 사람 눈 "겪는 서장"인가).

### ★5부 (7/25 새벽~아침) — 실기동 마라톤: 도하 개정·세가 실전·모델 나룻배 (다음 세션이 여기서 시작)

**한 줄**: 첫 실사용자가 밤새 문 열여섯을 찾았고 다 고쳤다 — 회랑은 **3차 개정(정박 무대 +
암전 도하 · 탈것 폐지)**으로, 나룻배는 **팩 모델**(honcheon:naru/barge · 좌표 한계 34→32
함정)로, 세가·출분·발단 무대는 실전 첫 항해(디돈 · 비무행)를 통과했다.

### ★7부 (7/25 낮 2회차) — ★★★5차 개정: 별도 서장 월드 + 건축 나룻배 한 척 (이 절이 최신)

**한 줄**: 사용자 재설명("별도의 서장 월드 · 건축 나룻배 · **1장 배에서 2장 배가 보이면 안 됨**")
→ 서장이 **서장 월드**(혼천_서장 — 칠흑+달빛 밤바다 · FLAT 물 층)로 이사했다. 배는 건축 중선
13×5 **한 척**(반블럭·계단 곡선 · buildBarge 결정론·멱등), 장 전환 = 가짜 항해+눈깜빡임+닻
재정렬(이동 없음), 안개 링 상시. 나루의 항해 장치(문 3곳·정박 배 3척·팩 모델 셸)는 전부
철거·묘비 (월드 백업 20260725-125146 후 재생성 · 팩 3정본 sha1 763d5f27). ★「과제 » 맞는
쪽의 선택」의 진범 = **본토 뿌리내림이 배 위에서도 세던 것** → TutorialGuide.silenced 중앙
게이트(훅+트래커 — 소음 항목도 닫힘). 눈: ⑧-3 전면 재표적 + **묘비 부활 감시**(배는 하나뿐) —
**눈의 시험 99/99** · 감사 전부 0. 정본: B-179 5차 진행 기록.

**★★ 다음 세션 첫 걸음**: ① 신규 캐릭터 실기동 (사람 눈 — 밤바다 위 "겪는 서장"인가: 도하
체감·배 형태·패 자리(ahead 1.9 — 갑판 중앙에 뜬다, 빨간펜 후보)·안개 링 짙기) ② B-184
(나루 글판 1/2 — 재생성 후 census 확인) ③ 세가 미결 4건(B-182) · B-180/183 잔여.

### ★6부 (7/25 낮) — 도하의 숙제 셋 해소 (설계 문답 → 4차 개정 · ★7부가 이 위에 선다)

**한 줄**: 설계 문답 3건 확정 → **가짜 항해**(사람·배 제자리, 세계가 흐른다 — 물살·좌우 노
박자·마중 안개 · 어둠은 눈깜빡임 12틱으로 축소) + **안개 장막**(정거장 중간점·기슭 앞 · 항해자
눈에만 · 조성 변경 0) + **배 형태 1차 수리**(갑판 맞물림 — 확폭 선체가 갑판 6×3을 감싼다 ·
이물·고물 2단 굽힘 · scale 2.6 · 팩 3정본 sha1 ee1d4068). 등록부: voyage.transit 개정
(flow_ticks/row_period/blink_ticks)·voyage.fog 신설 — 전부 【제안】. 눈: audit 재표적+신설,
뮤테이션 (89)(104) 재표적·(112)~(116) 신설·★(109) 표류 수리(시험 자체 고장을 시험이 잡았다) —
**눈의 시험 93/93** · 감사 위반 0. 정본: B-179 4차 진행 기록.

**★★ 다음 세션 첫 걸음**: ① 사람 눈 실기동 — 도하 체감(흐름 5.5초 【제안】·노 박자·눈깜빡임) +
장막 짙기 + **새 배 스샷 빨간펜** (신규 캐릭터 = B-179 닫는 조건 겸용) ② 뿌리내림 트래커가
항해 중에도 뜨는 소음 ③ 세가 미결 4건(B-182) · B-180/183 잔여.

### ★5부의 옛 숙제 셋 (4차로 닫힘 — 기록만 남긴다)
1. ~~이동감 부재~~ → 가짜 항해 ✓ 2. ~~배 형태~~ → 아는 문제 셋 선수리 ✓ (스샷 빨간펜 대기)
3. ~~다음 정거장이 보인다~~ → 안개 장막 ✓ (간격 16칸·조성 불변)

### 이 새벽의 함정·계율 (다음 사람은 밟지 마라)
- **모델 좌표는 −16~32** — 요소 하나가 34를 넘으면 모델 전체가 체커로 죽는다 (경계 검증 스크립트로 재라)
- **팩은 세 정본을 함께 갱신하라** — 재컴파일 + run/pack-http 동기화 + GitHub 릴리스 clobber
  (serve 스크립트는 zip 을 재사용한다 — 재빌드 안 하고 "배포했다"고 믿는 함정)
- **접합자 재접속은 enter() 를 안 지난다** — 판이 자라면 기동 때 build 로 세계를 치유하라 (실증: 87→100%)
- **바닐라 보트**: 첫 좌석=조종석 · 투명 플래그 무시 · yaw 표류 — 탈것 편법의 값 (그래서 폐지했다)
- **한 다리에 두 몸 금지** — 잔류 테스트 서버가 명부를 지워 접합을 막았다 (격리 가드 + B-183)

### ★4부 (같은 새벽) — 실기동 회차: 부두 경주 수리 + ★무대 그릇 (책이 사라졌다)

**한 줄**: 실기동 1호 "이으니까 바로 책을 받고" = 접합 직후의 경주(다리 2초 > 승선 5틱) —
나루의 책은 승선을 기다리게 수리. 이어 **"책이 너무 루즈 → 글이 아닌 몸으로"** (확정 3건:
맥박 한 줄 · 물 위 등불 우클릭 선택 · 다층 무대) — 항해 중 서장에서 **책 제거**, 정거장 =
계열별 기억 무대(본인에게만 보이는 조형 애니메이션+파티클+사건음), LLM 전문은 기슭의
**필사본**으로. 정본 `seojang_stage.yml`(전부 【제안】·기본 계열 다층 파일럿)+`SeojangStage.java`.
강등: enabled:false = 책 그릇. 눈 ⑧-4 9종 + 뮤테이션 6 — **눈의 시험 75/75**.
사람 눈 대기: 무대 파일럿 체감 (조형·맥박·등불 빨간펜) → 수행_파견 증축.

### 함정·계율 (이 세션 것)
- **판이 같은 칸을 두 번 적으면 검증이 제 판에 속는다** — 겹쳐 쓰기(갈대가 물을, 나무가 공기를)는
  최종 세계와 판을 어긋나게 한다. 조성 판은 자기일치가 계약이다 (겹침 걷기 · 눈 ⑫)
- **뮤테이션이 겨누는 파일을 늘리면 백업도 같이 늘려라** — selftest 가 CFG·SRC 만 되돌리면
  SBK·VOY 뮤테이션이 영구 감염된다 (다중 파일 백업으로 개정)
- **과제를 폐지하면 그 과제를 겨누던 눈·뮤테이션도 같은 커밋에서 재표적하라** — 이번에 9건
  (3차의 계율 재이행 · 잔재 눈이 부활을 지킨다)
- **어둑함도 계약이다** — 저승 구간은 「밝아도 위반」(dark_pct 창). 어둠 눈만으로는 넋등 실종을
  못 잡는다 (동쪽 빛이 서쪽으로 번져 창 안에 남는다) — 리듬은 등록부에서 직접 잰다 (넋등≤등롱)
- **같은 grain 장을 두 번 쓰면 같은 자리에 몰린다** — 고사목이 갈대 장 그대로면 동남(이승 곁)에
  몰렸다. grain_shift 위상 분리로 저승 쪽에 앉혔다 (결정론 유지)
- **하늘도 등록부다** — 시각 고정값은 의미 축과 대조해야 한다 (황혼=서쪽 주홍 — 축과 반대)
- 표적 잃은 뮤테이션 ㉒(등롱 성김)·㉓(간격 조각)는 묘비를 남기고 재표적했다 (3차의 계율 이행)

---

## 4-거울. ★★★★★★★★ — 2026-07-24 오후 세션 (§4-수확 보다 새롭다)

**한 줄**: **성장 v3 단계 5(UI) 배선 완료 — 전부 라이브** (레벨 거울·조용한 레벨업 알림·바닐라
XP바=경험·승급 연출·시트 승급 조건·몹 명패 레벨 칸·획득 토스트) + **B-176 첫 조각**(경지 칭호 =
경지 이름 그대로 · 명패 팀 접두). 사용자 결정 6건 · 눈 29/29 · lint 0 · 장부 위반 0.

### ★★ 다음 세션 첫 걸음 — 여기서 시작하라
1. **인게임 실측** (사용자 체감 대기 — 2부까지 한 번에): ① 단계 5 UI — 레벨업 flash · XP바 ·
   승급 연출(`promotion_fx` 수치 【제안】 — end_rod 60발·도전과제 종) · 명패 `[경지]` 접두 ·
   「획득 » …」 토스트 (레벨업 검증은 `/혼천 소환 <id>`) ② **뿌리내림 과정 (B-178·2부)** —
   신규 캐릭터로 정거장 8 흐름 + 트래커 줄 + 안내인 3인 대사. **체감 수정 회차가 예약돼 있다**
   (문장 전부 【제안】 — 빨간펜 대상)
2. **B-177 전투 v2 남은 빚**: combat_audit·defense_audit 기대 모델 v1→v2 (눈이 세계와 어긋나 있다)
   + 수치 튜닝 + 장비 크리 슬롯 + PvP directOpposed
3. B-176 다음 조각: 세계반응 3갈래(위세→npc_combat morale·경제·친화) + 퀘스트→소문→칭호 삼각형
   (소문 시스템 의존) + 본인 선택 · XP 실측 재보정 (`xp_pacing --rate`) · B-109 잔여(echo·회상 책)

### ★2부 (같은 날 오후) — 뿌리내림 과정 = 본토 튜토리얼 (B-178 신설 · 전부 라이브)

**한 줄**: 튜토리얼 = **B-109 ①마디(뿌리내림)의 구체화**로 확정 (사용자 결정 8건 —
`tutorial_rooting.md` §1). 기존 NPC 릴레이(섭구→장쇠→산길→곽진 · 새 NPC 0) · 정거장 8
(`config/tutorial.yml`) · 사이드바 트래커(기초 과정만 카운터 — ★personal_story §6.4 부분 개정
묘비 있음) · 행위 자체가 보상. `TutorialGuide` + 훅 6곳 + PlayerLedger 영속 + 소급 거울.
눈: `tools/tutorial_audit.py` (등록부↔훅 표류 — 죽은 정거장 = 영영 안 끝나는 튜토리얼) 위반 0.
안내인 3인 실존 RCON 확인. ★함정 회피 기록: 운기는 개화 전 거절("단전이 비어 있다")도
가르침으로 센다 — 입도진 「뛰어라」 함정의 재발 방지. 수련·운기는 정거장을 갈랐다
(계수 공유면 수련 2번=완료 오배선).

### ★3부 (같은 날 늦은 오후) — 나루 재설계: 순수 문지방 (사용자 확정 3건)

**한 줄**: 나루의 목적을 파악(대기실·접합·서장·의식 — 가르침은 본토 안내 부재 시절의 보완물)하고
**맞이→나루 두 관문만** 남겼다. 격·경공(전원-범인에게 예고 전용)·수련·시트·허수아비·지급품 전부
제거, 태세→본토 「몸짓」 정거장(흘리기·회피 2문 · **막기는 방패 없어 예고만** — 판매 보류·등록 시
1500문 의향), 손→첫 사냥 흡수. 뿌리내림 정거장 8→**9**. 나루 월드 백업 후 제거 — 첫 입장 때
새 등록부로 재생성 (§4-밤의 나루 심부 마을 잔해도 함께 청산). 눈: antechamber_audit 를 새 정본에
맞춤 (과제별 검사 「있을 때만」 + 조기 return 이 사람 보호 검사를 감던 병 수리) · selftest 뮤테이션
재정비 — **눈의 시험 53/53** · tutorial_audit 9/9 · 정적 감사·lint 위반 0 · 기동 오류 0.
★계율: 과제를 이관하면 **그 과제를 겨누던 눈의 시험도 함께 이관하라** (표적 잃은 뮤테이션 33건이
「시험 자체 고장」으로 잠들 뻔했다 — 묘비를 남기고 폐기·재표적했다).

### 이 세션에서 선 것 (전부 커밋 · 전부 라이브)

| 무엇 | 핵심 | 정본 |
|---|---|---|
| ★정본 정리 | HANDOFF 요약 "레벨업 연출"은 정본과 어긋났다 — **레벨업은 조용히, 승급이 터진다** (feel §1 사용자 확정 준수) | growth_v3_feel.md §1·§4 |
| 레벨 거울 | mvtSheet level/xp/xp_need/points → Sheet·PlayerLedger — **마크가 처음으로 제 레벨을 안다** (전엔 XP 올려보내고 결과를 몰랐다) | GameListener.mvtSheet |
| 레벨업 알림 | syncSheet 델타 감지 → 액션바 flash (첫 스냅숏 = 도착이지 델타 아님 — 안 알린다) | SkillListener.syncSheet |
| XP바 | 바닐라 XP바 = v3 경험/레벨 (setLevel/setExp — XpEconomyGuard 절연 전제의 이행) | cultivation_v3_levels §4-b ② |
| 승급 연출 | Title=**새 경지 이름** · 부제=**cultivation_stages description 인용** · 버스트+소리 = `promotion_fx` 등록부 신설 (수치 【제안】) | skill_motion.yml promotion_fx |
| 시트 승급 조건 | promotionGates 리팩터 — **표시=판정 한 해석기** (자동 경지 ✅/❌ 현재/필요 · 벽 경지 trigger·요건 인용+현재값. 침묵 boolean 소멸) | GameListener.promotionGates |
| 몹 명패 레벨 | `[N] 이름` (한월 문법 · HitFeedback 예약 지점 — mobLevel 같은 해석기) | HitFeedback.upsertBar |
| ★경지 칭호 | **칭호 = 경지 이름 그대로** (사용자 확정 — 별도 이름표 없음 · 범인 없음). 남도 본다 = 스코어보드 팀 접두 `[경지]` (5초 전파) + 사이드바 Lv | B-176 첫 조각 |
| 획득 토스트 | 줍기 = 채팅 「획득 » '가죽' +1개」 (이름 있는 물건만) · XP = 액션바 「획득 » 경험 +N」 (도배 회피) | HuntListener |

### 눈 (전부 통과 상태로 인수)
`growth_v3_backfill_selftest.py` **29눈** (단계 5 신설 3: 한 해석기 32조합·부족 관문 표기·표시
갈라짐 오배선) · lint 0 · motion_audit 위반 0 · backlog_audit 위반 0 · 실측: 스냅샷에 level 실림
확인 (Lv11·30포인트) · MVT 기동 오류 0 · 봇 JDA 로그인 ✓

### 함정·계율 (이 세션 것)
- **요약과 정본이 다르면 정본이 이긴다** — HANDOFF 의 "레벨업 연출(Title+버스트)"은 feel §1
  (레벨업=조용·승급=연출)과 어긋났다. 사용자 확인으로 정본 준수 확정
- **표시와 판정은 한 해석기** — 승급 조건 표시를 promoteIfDue 와 딴 벌로 재면 언젠가 다른 곳을
  가리킨다 (promotionGates 한 벌 · selftest 가 오배선을 잰다)
- **첫 스냅숏은 델타가 아니라 도착이다** — 재기동 직후 레벨 알림이 터지면 그것이 오배선이다
- 봇 재기동 절차 재검증 (5회째): /proc environ 토큰 확보(길이 검사) → kill → run_bot_pg.sh

---

## 4-수확. ★★★★★★★ 최신 — 2026-07-24 낮~오후 세션 (이 절이 §4-점화 보다 새롭다)

**한 줄**: **성장 v3 단계 4 완전 마감** — 배분 손·이중 관문·내공 A안·★B안 개정(원장 무캡)·
XP 3원천(처치/디스코드 사냥/의뢰)·정예·두목 지정·**v2 수련→능력치 동결**까지 전부 라이브.
+ XP 다리 실기 결함 2건 수리 (사용자 인게임 실측 확인). 커밋 12개 · 눈 26/26 · 장부 위반 0.

### ★★ 다음 세션 첫 걸음 — 여기서 시작하라
1. **성장 v3 단계 5 — UI** (B-135 마지막 단계): 레벨업 연출(Title+버스트 — 한월 A5) ·
   시트에 승급 조건 표시(지금은 왜 승급 안 되는지 침묵 — 자격 레벨/마크/숙련을 말하라) ·
   획득 토스트 · 칭호 B-176 (세계반응만)
2. **B-177 전투 v2 남은 빚**: ★combat_audit·defense_audit 기대 모델이 아직 v1 (눈이 세계와
   어긋나 있다) + 수치 튜닝(사용자 체감 한 줄 대기) + 장비 크리 슬롯 + PvP directOpposed
3. XP 실측 재보정 (계약된 유보): `rate_xp_per_hour 200` 【제안】 — 텔레메트리 쌓이면 `xp_pacing --rate`

### 이 세션에서 선 것 (전부 커밋 · 전부 라이브)

| 무엇 | 핵심 | 정본 |
|---|---|---|
| 포인트 배분 손 | 시트 [포인트 배분] 버튼 → 축 7버튼(al: 네임스페이스) · 시트는 원장만 표기(⑪) | GameListener onAllocate |
| 승급 이중 관문 | promoteIfDue 에 자격 레벨 N_k AND ("레벨은 자격, 사건이 문") | levels.qualifying_level |
| 내공 A안 | 내력 풀 실수치 = √원장[내공] (시트·운기·마크 거울 3곳 — poolNaegong) | levels.naegong_unified |
| ★★B안 개정 | **원장 캡 폐지** — 배분 언제나 됨·파생 무캡. **판정만 min(floor√원장, 경지 캡)** (봇 genderStat(realm)·마크 judgmentAttr). 천장 너머 원장은 승급 때 판정으로 터진다. 은행 규약 소멸(B-152 종결) | attribute_scale_v3 **§8.10** |
| XP 3원천 | 마크 처치(등급 계수) + 디스코드 사냥(잡은 것만) + 게시판 의뢰(등급표 20/30/50/80/120) | levels.xp_sources |
| 정예·두목 | 호랑이·반달곰=정예 · 갈호·백영묘=두목 (npcs xp_grade — lint 5-d 가 오타를 잰다) | cheongha_npcs.yml |
| ★XP 다리 수리 | ① flush 를 onKill 끝으로 (gap 표 밖 등록 개체도 실린다) ② pendingXp 영속 | HuntListener·PlayerLedger |
| v2 동결 (마감) | 행위→기술·화후·축기만, 능력치=레벨 포인트. 화해 자연 무연산 (off = v2 되돌림 한 줄) | levels.training_attr_frozen |

### 눈 (전부 통과 상태로 인수)
`growth_v3_backfill_selftest.py` **26눈** (B안 4·동결 2·오배선 감지 7종 포함) ·
`lint_config.py` 5-d(xp_grade)·5-e(의뢰 등급↔XP 표) 신설 + selftest 케이스 · backlog_audit 위반 0

### 미결·대기
- 수리 전 증발한 XP 보상 — 사용자가 처치 내역을 답하면 다리 경로로 적립 (소액 · 방치 가능)
- 검증 절차 정착: 레벨업 검증은 새 월드가 아니라 **`/혼천 소환 <id>`** (관리자 · PDC 표식 포함 — XP 파이프 끝까지 흐른다)

### 함정·계율 (이 세션에서 물린 것)
- **flush 는 적립의 끝에서** — 적립(pendXp)이 실기(pushLedger) 뒤에 오면 한 박자 늦거나 영영 안 실린다.
  다리 수신함에 **당일 파일이 없으면** 그것이 실기 결함의 첫 증거다
- **pending 은 영속하라** — 메모리에만 있으면 재기동이 지운다 (pendingXp 가 그랬다)
- **gap 표(바닐라 몹 타입)와 npc 등록부는 다른 문이다** — 등록 개체(호랑이=Ravager)는 gap 표 밖
- **감시 명령의 자기일치** — bash cmdline 에 패턴이 실려 제 자신을 문다. 판별은 `readlink /proc/<pid>/exe` 로만 (이 세션에서 두 번 물렸다 — §4-판의 함정 재발)
- **`git add -A` 를 커밋에 쓰지 마라** — 추적 외 파일(.ai-review·exe)이 딸려 들어갔다 (amend 수습)
- **B안의 문**: 판정은 봇 genderStat(realm)·마크 judgmentAttrOf/attackBonus(realm) 를 지나야 캡이
  든다. **파생 독자(내구·이속·내력 풀)는 그 문을 지나지 마라** — 몸은 천장 없이 자라는 게 계약이다
- 봇 재기동 절차는 이 세션에서 4회 검증됨: /proc environ 토큰 확보(길이 검사) → kill → run_bot_pg.sh

---

## 4-점화. ★★★★★★ 최신 — 2026-07-24 낮 세션 (이 절이 §4-성장 보다 새롭다. 여기서 시작하라)

**한 줄**: **전투 v2 가 라이브에 켜졌다** (2d6 전투 사망 · 공방+크리) + 성장 v3 **단계 3~4 절반**
(√원장 다리 · 표류 수리 · XP 파이프 라이브) + 장부 위반 8건 전부 청산. ★다음 = **포인트 배분 손**.

### ★★ 다음 세션 첫 걸음 — 여기서 시작하라
**성장 v3 단계 4 잔여 (B-135)** — XP·레벨·포인트는 쌓이는데 **쓸 손이 없다**:
1. **포인트 배분 손** (봇 명령/버튼 — 원장 캡 c²(`cultivation.yml raw_attribute_cap_by_realm`) ·
   포인트 은행 무기한 · 시트는 원장만 표기 §8.9 ⑪)
2. 승급 게이트에 자격 레벨 N_k 편입 (`levels.qualifying_level` · 사건 마크와 이중 관문)
3. **v2 수련→능력치 동결** — ★배분이 서기 전엔 켜지 마라 (성장이 아예 멈춘다). 동결하면
   backfill 화해가 자연 무연산이 된다 (설계됨)
4. 내력 풀 = √원장[내공] 교체 (**A안 확정** — `naegong_unified: true` 선언만 있고 배선 전)
5. 정예·두목 등급 필드 등록 (지금 처치 XP 는 전부 잡졸 계수)

### 이 세션에서 선 것 (전부 커밋 · 전부 라이브 배포됨)

| 트랙 | 무엇 | 상태 |
|---|---|---|
| 장부 | 위반 8건 청산 — 증거 3건 기입(B-011/170/137) · 근시 눈 2개 수리(antechamber 글자수 창→몸통 검사 · perf 파이썬 독자 항상 합산) · 팔레트 밖 firework→end_rod(점층 오프라 시각 무변경) | `--run` 위반 0 |
| ★전투 v2 | 세 판정길(무공·평타·NPC) 공방 배선 + **라이브 점화** (`combat_v2.enabled: true`). 결정(사용자): 공격력=무기+숙련+능력치+격 **4항** · 격차±2/고갈−2 제거 · 평타=바닐라+능력치+격. 기동 고지 로그 있음 | **라이브** — 실측=일상 플레이 관찰 |
| 성장 단계3 | 다리 attrs=√원장 + ★표류 결함 2 수리: backfill 이 화후 소수부를 버렸고, 원장이 얼어 수련이 판정에 안 실렸다 → **화해(raise-only · max(원장,화후²))** — 판정 보존 증명·단계4 앞방향 호환 | 라이브 (봇 재기동됨) |
| 성장 단계4 | 결정 4건(XP=몹레벨×등급계수 클래식 고정 · 의뢰 80/50/40/30 · 사선 보너스 폐지 · **내공 A안 통일**) + XP 파이프: 처치(짐승+등록 NPC)→`+N 경험`→다리→봇 레벨업·3포인트 | 라이브 (배분 손 없음) |

### 정본·눈
- `docs/design/xp_sources_v3.md` §0 (확정) · `combat_resolution_v2.md` · `growth_v3_impl_plan.md`
- `cultivation.yml levels.xp_sources`·`naegong_unified` · `combat.yml combat_v2` (둘 다 결정 주석 포함)
- 눈: `growth_v3_backfill_selftest.py` **10눈** (화해 3 + XP 3 신설) · `judgment_scale_harness.py` 0

### ★ 전투 v2 의 남은 빚 (B-177 — 장부 참조)
【제안】 수치 튜닝 (per_body 0.5·크리표·상한 0.75 — 사용자 체감 한 줄이면 됨: "잡졸이 안 죽어" 류)
+ ★**combat_audit·defense_audit 기대 모델이 아직 v1** (v2 가 라이브인 지금 눈이 세계와 어긋나 있다 —
그들이 위반 0 이라 해도 v2 산술은 안 잰 것) + 장비 크리 슬롯 + PvP directOpposed 거취.
끄기·튜닝: config + `/혼천 모션 재적재` — **재기동 불필요** (엔진 전체 갈아끼움·리스너 rebind).

### 함정·계율 (이 세션에서 물리거나 세운 것)
- **감사 출력을 tail 로 자르지 마라** — 위반 상세가 날아가 두 번 돌렸다
- **글자 수 근접 창의 눈은 주석이 자랄 때마다 거짓 짖음** — 몸통(body_of)이 경계다 (antechamber 수리)
- **폴백은 가림이다** — perf 눈의 파이썬 독자가 자바 독자 등장에 실종됐다. 독자는 합산이 맞다
- **두 장부는 화해로 잇는다** — raise-only max() 는 판정을 보존하고 권위 이전 뒤 자연 무연산이 된다
- **운영 JDK 배치가 뒤집혔다**: ★라이브 = jdk-25 (BetterModel 라이브) · 테스트 = jdk-25 로 맞춤.
  redeploy_mvt.sh 가 정본 (빌드 21 · 기동 25). 봇 재기동은 토큰 확보 절차(HANDOFF §3)로 조율자 대행 가능
- 봇 jar 를 도는 중에 재빌드했으면 **그 봇은 재기동 대상이다** (지연 로드 함정 — 두 번 그랬다)

---

## 4-성장. ★★★★★ 최신 — 2026-07-23~24 성장·전투·런처 마라톤 (이 절이 §4-판 보다 새롭다)

**한 줄**: 검기 라이브 반영 + 타격 피드백(대미지 숫자·HP바) + **전용 런처(Electron·공식 마크 재활용)**
+ **성장 v3 구현 3단계** + **전투 판정 재설계(2d6 제거)** 설계 완결. ★다음 = 전투 v2 코드 배선.

### ★★ 다음 세션 첫 걸음 — 여기서 시작하라
**전투 v2 코드 배선 (B-177 2단계)** — 설계·config 다 섰고 `combat_v2.enabled: false`라 라이브 무영향.
1. `docs/design/combat_resolution_v2.md` 읽기 (모델 정본) + `config/combat.yml` `combat_v2` 절
2. `SkillEngine.strike(cast, execBase, roll2d6, resist)` → roll2d6·tierOf 명중 삭제,
   피해 = `max(1, 공격력 − 방어력) × 크리배수`. `SkillListener` 전투 루프: roll2d6() 제거·resist→방어력·
   크리 확률 굴림(액션 RNG). `npcDefenseScore`→방어력. **enabled 스위치로 전환** (켜기 전 무영향)
3. ★라이브 전투를 바꾸는 큰 변경 — 신중히. 배선 뒤 인게임 실측으로 【제안】 수치 튜닝

### 이 마라톤에서 선 것 (전부 커밋)

| 트랙 | 무엇 | 상태 |
|---|---|---|
| 검기 | 시트판 8차 라이브 승격 + 대각(roll 45)·시전자 추종·판 뒷면 UV(C↔Ɔ 수리) | 라이브 |
| 타격 피드백 | 대미지 숫자·표적 HP바·처치 흩어짐 + **접촉 무딜(딜은 획만)** + 공통 몹 명패 | 라이브 |
| ★런처 | Electron 전용 런처 — **공식 마크 재활용**(재로그인 없음·설정 이어받기·기존 모드 안전) · Windows CI 빌드 · MS Store 프로필 파일 문제·베드락 오픈·Sodium/Iris 짝 전부 수리 | 배포 (honcheon-pack 릴리스 `honcheon_setup_0.1.0.exe`) |
| 모드팩 | Fabric 1.21.11 · 10모드 + Complementary Unbound · 게이트 v0(fabric 브랜드) · Emotecraft 양단 | 라이브 |
| **성장 v3 (B-135)** | 단계 0(cultivation.yml levels)·1(원장 backfill 무DDL)·2(판정치=floor√원장) **완료**. 단계 3~5 남음 | 구현중 |
| **전투 v2 (B-177)** | 설계·config 완결 (2d6 제거·공방·크리 감각/지혜/무기/장비). **코드 배선만 남음** | 설계 완 |

### ★ 성장/전투 설계 정본 (다 읽어라)
- `docs/design/attribute_scale_v3.md` §8.9 — 저울 12항 승인 (B-137 **닫힘** · floor√원장)
- `docs/design/growth_v3_impl_plan.md` — 성장 6단계 배선 (무DDL 발견)
- `docs/design/growth_v3_feel.md` — 체감: 두 속도(레벨=조용·승급=연출+칭호)·칭호(세계반응만·B-176)·무공 문턱=대가곡선
- `docs/design/combat_resolution_v2.md` — 전투 v2 (B-177)
- 눈: `tools/judgment_scale_harness.py`(옛/새 동등 위반0) · `tools/growth_v3_backfill_selftest.py`

### 성장 v3 남은 단계 (B-135)
- 단계 3: 파생치 √원장 (내력 풀·이속·내구가 √원장 실수치를 읽게 — "몸 체감")
- 단계 4: 성장 로직 (XP→레벨업→3포인트→배분+은행 · 무공 문턱 대가곡선 · Growth/ProgressionEngine v3)
- 단계 5: UI (레벨업 연출·시트 원장 표기·획득 토스트) + 칭호 B-176
- ★MVT 다리: 원장 독립 성장 시 √원장을 attrDays 로 동기화 (단계 4 — 지금은 backfill 로 동등)

### 함정·주의 (이 마라톤에서 물린 것)
- **MS Store 런처는 launcher_profiles_microsoft_store.json** 을 읽는다 (독립설치판과 다름) — 둘 다 써야 함
- **minecraft:// 는 베드락** — 자바 런처는 Get-StartApps 로 AUMID 조회해 연다
- **Iris↔Sodium 정확 짝** — Iris 1.10.7 은 Sodium 0.8.7 요구 (0.8.13 충돌)
- 런처 CI: `launcher/` push → windows-latest 빌드 → `gh run download` → pack 릴리스 업로드 (workflow scope 필요)
- 원장 backfill 비파괴: 판정 권위는 원장으로 넘어왔으나 결과 불변 (원장=능력치² · 하네스 증명)

---

---

## 0. 먼저 읽을 것 (순서대로)

| 문서 | 무엇 |
|---|---|
| **`docs/BACKLOG.md`** | ★ **살아 있는 장부** — 95항목. 무엇이 남았는지 여기 있다 |
| `tools/backlog_audit.py` | 그 장부가 **자기 자신에 대해 거짓말하는지** 재는 눈 |
| `docs/design/gate_and_watertown.md` | 조성의 계약 (사용자가 직접 씀) |
| `docs/design/scale_systems.md` | 크기의 자 — 장소마다 다른 자로 잰다 |
| `docs/design/ami_architecture.md` · `noklim_seokchae.md` | 사용자가 직접 설계한 원형 둘 |
| `CLAUDE.md` · `docs/design/*.md` (81개) | 세계관·시스템 |

---

## 1. 이 프로젝트가 무엇인가

**무협 공유세계 RPG.** 한 사람이 만든다 (한국어 사용자, 솔로 개발자).

| 모듈 | 무엇 | 언어 |
|---|---|---|
| `server-mvt` | 마인크래프트 플러그인 (Paper **1.21.11**) — **몸이 하는 것** | Java 21 |
| `server-bot` | 디스코드 봇 (JDA + SQLite) — **인증과 소셜** | Java 21 |
| `core` | 룰 엔진 (판정·경제·성장) — 두 몸이 공유 | Java 21 |
| `domain` | 세력·지역 서비스 (의존성은 `:core` 뿐) | Java 21 |
| `resourcepack/` + `tools/build_resourcepack.py` | 팩 (수묵 무협) | Python |
| `config/*.yml` | ★ **단일 진실 원천** — 코드는 수치·이름을 지어내지 않는다 | YAML |
| `tools/*.py`, `*.java` | **눈**(감사) 27종 | |

**축**: **디스코드 = 인증 + 소셜.** **마크 = 몸이 하는 모든 것.** 로직은 백엔드.

---

## 2. ★★ 헌법 — 이것을 어기면 되돌려라

### 2.1 등록제
> **코드가 이름·수치를 지어내지 않는다.** `config/*.yml` 이 정본이다.
> 근거가 없으면 **지어내지 말고 사용자에게 물어라.**

★ 사용자의 절대 규칙: **"모르는 것·임의로 정할 것은 나에게 물어보고 정해야 한다. 추측을 통해 생성하는 건 금한다."**

### 2.2 ★ 침묵하는 실패 — **이 저장소의 근본 병**
> **코드가 모를 때 조용히 뭔가를 고르면, 그것이 곧 거짓말이 된다.**

사용자의 말이 이 병의 이름이다:
> **"모르는 장소를 작은 장소로 간주하는 것도 하나의 창작이다."**

**2026-07-13 하루에만 여섯 번 잡았다:**

| 무엇이 말했나 | 세계의 진실 |
|---|---|
| 팩 검수: "위반 0건" | 사용자는 **보라 큐브**를 보고 있었다 (텍스처가 아틀라스 밖) |
| 로그: "허수아비 3" | **0마리** (평화 난이도가 좀비를 지우고 있었다) |
| 로그: "발판 6" | **0개** (표본이 4만 칸 중 6칸을 못 봤다) |
| 조성: "여덟 문파가 섰다" | **전부 같은 집** (`SECTS` 하드코딩) |
| 봇: "마크가 꺼졌다" | 서버는 돌고 있었다 (`int` 에 에폭 밀리초 → 넘침) |
| 튜토리얼: "뛰어라" | **아무도 못 뛴다** (개화 전엔 허공 딛기 0회) |

**전부 장부가 세계에 대해 거짓말했고, 그 거짓말은 조용했다.**

### 2.3 ★ 눈을 만들면 눈을 시험하라
> **감사를 만들었으면 일부러 어겨서 그 감사가 잡는지 확인하라.**

**이 세션에서 눈이 자기 주인을 여러 번 물었다** — 그리고 그때마다 진짜 버그가 나왔다.
- 청구서의 눈이 **첫 끼니로 자기 장부에서 위반 5건**을 찾았다
- 접합의 눈이 **문지기를 뜯어냈는데도 통과**시켰다 (검사가 느슨했다)
- 지형의 눈이 **`if (false)` 로 감싼 죽은 코드**를 통과시켰다
- 서장의 눈이 **「없는 것」과 「꺼진 것」을 구별 못 했다**

**시험 없는 눈은 눈이 아니다.**

### 2.4 ★ 3계층 헌법 — 지도 → 땅 → 건축 (2026-07-15 개정 · B-147)
> ★★ **지도가 정본이다. 지도는 건축물을 포함한 맵 디자인 전체다.**
> **땅은 지도에 맞게 서고, 건축은 그 땅 위에 오른다.** (사용자 원문 — B-147)
> 땅의 치수(봉우리 정상부 폭·단의 넓이·진입로)는 **지도의 건축 footprint 에서 유도**된다.

**폐지된 구판 조항** (묘비 — 다시 세우지 마라):
- ~~"땅에 맞게 건물이 올라가는 것이지, 건축에 맞게 지형이 생기는 게 아니다"~~ — 지도가 건축을
  포함하므로 거짓이 됐다. 이 조항 아래서 봉우리가 건물보다 얇게 서고 건물이 묻히고 떴다 (B-146).
- ~~"건축의 미결이 땅을 막아서는 안 된다 (땅 먼저)"~~ — 지도가 완성되지 않은 곳은 땅도 빚지
  않는다. v4 의 「땅 먼저」 관행이 만든 30곳의 땅은 v5 갈아엎기의 대상이다.

**집행 안전핀** (권위는 지도에 있다 — 핀은 지도를 섬기는 기계로 존속):
> **땅은 한 번만 선다** (`TerrainLedger`) — 같은 지도로 두 번 빚지 않는다. 지도가 바뀌면 그때 다시 빚는다.
> **건축 집행이 선 땅을 임의로 바꾸지 못한다** (`TerrainSeal`) — 지도에 없는 변형은 위반이다.

땅을 고쳐야 하면 `LandRequest` 로 **요청**한다. 자세한 것은 `docs/design/gate_and_watertown.md`
(★구판 문서 — 이 개정과 어긋나는 곳은 이 절이 이긴다. 개정 반영은 사용자가 직접 쓴 문서라 사용자 몫).

### 2.5 결정론
> **조성기에 난수 금지.** 좌표의 순수 함수여야 한다 (같은 좌표 → 언제나 같은 결과).
> ★ 그러나 **좌표 해시는 파장이 한 칸**이라 이웃과 상관이 없다 → **점묘(點描)**가 된다.
> **격자점에서 뽑고 보간하라** (`config/terrain_grain.yml`). 난수를 다시 들이지 마라.

### 2.6 전투의 문법
- **격(格) 사다리**: 외공기0 → 발경1 → 검기2 → 강기3 → 어검4 → 심검5. **판정 축**이지 이펙트가 아니다
- ★ **몸짓이 곧 선택**: **방패=막기 · 웅크림=흘리기 · 달림=회피**
- **경공 = 더블 점프** (공중에서 점프 한 번 더). **개화(일류) 전에는 못 한다**
- ★ **콤보 창(window)·입력 버퍼를 들이지 마라** — 삼문(三門)을 잡아먹는다. 외부 설계서가 권해도 **거부한다**
- **화면 흔들림은 껐다** (멀미). **무게는 카메라가 아니라 몸이 밀리는 것으로 말한다**

### 2.7 팩 게이트 — **팩이 있어야 플레이 가능** (2026-07-13 개정)
옛 「팩 게이트 불가침」(팩 없어도 된다)은 **폐지됐다.**
**거절·다운로드 실패·무응답(45초) 전부 퇴장.** 다만 **왜 못 들어왔는지 한국어로 말한다.**
★ **옛 폴백 경로가 6개 도구·여러 등록부에 남아 있다** — 장부 참조.

### 2.8 집안은 능력치를 주지 않는다
> **집안이 주는 것은 힘이 아니라 「입장권과 빚」이다** (무가의 검법 1, 무가의 청구서).

---

## 3. ★ 보안·운영 규약 (어기면 사람이 다친다)

| | |
|---|---|
| ★★ **`DISCORD_TOKEN`** | **프로세스 env 에만 존재.** 파일·깃에 **절대 기록 금지**. **봇을 kill 하기 전에** `/proc/<PID>/environ` 에서 확보해야 재기동 가능. ★ **이 세션에서 내가 봇을 죽였다가 토큰을 잃었다** — 사용자가 손으로 되살렸다. **재기동 명령이 실제로 되는지 먼저 검증하고 죽여라** |
| **RCON 25575** | **포워딩 절대 금지** (공개는 25565만) |
| **팩 HTTP 8123** | 사설망 전용 — 공유기 포워딩 금지. 지금은 **GitHub 외부 호스팅**을 쓴다 |
| **DB** | ★ **봇이 단일 작성자.** MVT 는 sqlite 를 직접 열지 않는다 — **다리(`run/bridge`)로 청한다**. **DB 작업 전 백업 필수. 마이그레이션은 봇 정지 상태에서** |
| **PUA 문자 리터럴** | 금지 |
| **에이전트 규약** | git commit 금지 · `git reset`/`stash` 금지 · **서버 기동/RCON 금지** (조율자만 한다) |

### 빌드·배포
```bash
export JAVA_HOME=$PWD/run/jdk-21          # ★ 이거 없으면 "invalid source release: 21"
./gradlew :server-mvt:jar :server-bot:jar  # :server-faction 은 기존 의존성 문제로 실패 — 무시

bash scripts/redeploy_mvt.sh               # jar 복사 + config 동기화 + 재기동 (RCON stop)
```
★★ **빌드 통과 ≠ 기동 성공.** 이 세션에서 `String.format("%.0f", (long) x)` **한 줄**이 `onEnable` 에서 터져 **플러그인 전체가 안 켜졌다** (팩·나루·HUD 통째로 죽음). **배포 후 반드시 확인:**
```bash
grep -q "Error occurred while enabling HoncheonMVT" run/mvt/server-console.log && echo 안켜짐
```

### 봇 재기동 — **사용자가 직접 한다** (토큰 때문에)
```bash
cd <repo>
DISCORD_TOKEN='...' HONCHEON_GUILD_ID='1525178103335489739' \
HONCHEON_LLM_URL='http://localhost:11434/v1' \
HONCHEON_LLM_MODEL='qwen3-30b-a3b-instruct-2507-bartowski:latest' \
nohup ./scripts/run_bot.sh > run/bot/bot.log 2>&1 & disown
```

---

## 4. 지금의 상태 (2026-07-14 09:50)

### 살아 있는 것
- **봇** ✔ (스키마 8 · 초대 링크 등록됨)
- **마크** ✔ (Paper 1.21.11 · 팩 게이트 닫힘)
- **팩** ✔ GitHub 호스팅. 접속마다 sha1 재측정 + `?v=<sha1>` 캐시버스터
- **장부** ✔ 95항목 · 위반 0건

### ★ 방금 한 큰 일 — **세계를 갈아엎었다**
사용자가 **새 지형(v4)을 승인**하고 **전부 갈아엎기**를 지시했다.
- 삭제: 월드 3개(강호·나루·연무장) · 지형 원장 · 영수증 · 앵커 · 구역 · 플레이어 데이터 · 원지
- 백업: `run/backup-20260714-091228-갈아엎기/`
- 남김: 화이트리스트 · ops · 봇의 장부(지역·사람·소문)
- `config/terrain_gate.yml` → `approved_forge_version: 4`

### ★★ 다음 사람이 이어서 할 일 — **세계를 마저 세운다**

**지금 세계에 있는 것**: **청하현**(전용 조성기, 68초) + **화산파**(지형만, 729초). **끝.**

```
/혼천 세계조성          → 청하현 (전용 조성기)
/혼천 지형조성 <id>     → 그 곳의 땅만 빚는다 (건축 미결이어도 땅은 선다)
/혼천 지역조성 <id>     → 땅 + 집
```

**남은 곳은 30곳 안팎. 한 곳당 최대 12분.** 총 **5~6시간**이다.

★★ **조성은 느리다. 조용한 것이 아니다.** 이제 **15초마다** `[조성·진행]` 이 로그에 남는다.
**조용하면 그때가 진짜 사고다.**

### ★ 먼저 올려야 할 것 — 안전핀
**`max_seconds: 900` 인데 화산파가 729초.** 여유가 **19%** 뿐이다.
**곤륜·설산처럼 화산보다 높은 산은 안전핀에 걸려 중단될 수 있다** (B-100).
→ **1800초로 올리고 시작하는 것을 권한다.** 사용자에게 물어라.

### 권하는 순서 (조율자가 사용자에게 제안했고, 사용자는 아직 답하지 않았다)
1. **안전핀을 올린다** (900 → 1800)
2. **문파 대여섯 곳만 먼저 세워** 새 지형을 **눈으로 확인**한다
3. 좋으면 나머지를 돌린다

★ **땅은 한 번만 선다.** 30곳을 다 세워 놓고 "이거 아닌데" 하면 **되돌릴 수 없다.**
**되돌릴 수 없는 일은 작게 시작하라.**

### 【정정】 「지형 조성이 조용히 죽는다」는 **내 오진이었다**
나는 5분 기다리고 죽었다고 판단했다. **실제로는 729초 걸려 정상적으로 섰다.**
**진짜 병은 따로 있었고 훨씬 무서웠다**:
> **RCON 의 `sender` 는 명령이 반환되는 순간 소켓을 닫는다.**
> 12분간의 진행 보고가 **전부 죽은 소켓으로** 갔다.
> ★ **`err` 도 같은 곳으로 갔다 — 조성이 정말 터졌어도 콘솔은 똑같이 조용했을 것이다.**

**고침**: `Announce` — **먼저 로그에 적고, 그다음에 사람에게.** **로그는 아무도 안 죽는다.**
**눈**: `tools/silence_audit.py` — **침묵은 그 자체로 결함이다.** 그 눈이 놓친 곳을 **셋 더** 찾았다.

### 【정정】 「새 지형이 험하다」도 내 오진이었다
```
지면 요철   구판 [-1,1]  →  v4 [-1,1]    ← 진폭 동일
```
**v4 가 바꾼 것은 진폭이 아니라 상관(相關)이다** — 같은 크기로 흔들리되 **덩어리로** 흔들린다.
물을 늘리거나 벼랑을 만드는 코드는 **한 줄도 없다.** 험해 보인 것은 **월드를 갈아엎어 바닐라 지형이 새로 생성된 것**이다.

---

## 4-밤. ★ 최신 — 2026-07-14 밤 세션 (이 절이 §4~6 보다 새롭다)

### 상태 (23:05 기준)
- **마크** ✔ (Paper 1.21.11 · 22:59 기동 · HoncheonMVT 오류 0) — **봇** ✔ (PostgreSQL · 21:05 기동, [마크와 잇기] 버튼 탑재)
- **팩** ✔ GitHub sha1 `8eebae49` (서장 사건음 sounds.json 포함)
- **장부** `docs/BACKLOG.md` — 이 밤에 B-116~127 신설 · **B-113·B-114 닫힘 (증거: 환경검수 ⑥ 0.00%)**
- HEAD 자립 ✔ — 표류 7,100줄 회수됨. **부분 커밋 전 격리 워크트리 빌드가 관행이다** (HEAD 가 빌드 불능이었던 적 있다)

### 이 밤에 닫힌 것 (전부 실사용 피드백 기원 · 커밋됨)
| 항목 | 무엇 | 사람 눈 확인 |
|---|---|---|
| B-113·114 | **지하 3.86% 의 진상 = 조성기가 산 채로 묻은 나무** (7차 수사). 치유 48,519칸 → ⑥ 0.00% | 닫힘 |
| B-116·123 | HUD 액션바 한 줄의 주인 (HudLine) + 입도진 과제 카운트는 판정 뒤에 | 격 순환·더블점프 |
| SJ-002 | 서장 대기("붓이 적고 있다")·도착 타이틀·사건음·자동 펼침 | 서장 재체험 |
| B-117 | 접속 = [마크와 잇기] 버튼+모달 (명령은 뒷문) | 안내판 재실행 후 |
| B-118 | 서장 끝나기 전엔 안 내보낸다 (다리 명단 판정) | 부계정 재현 |
| B-119 | **살아 있는 것은 다 맞는다** (NPC 무적 철거 · npc_death 다리 사건) — ★사용자 확정: "몰래 죽일수도 있어야" | 산길 짐승·NPC 타격 |
| B-120·124 | 자동 출도는 부두에서만 · 못 하는 과제는 이유를 말한다 | 마당 몸짓 |
| B-121 | 뱃사공 섭구 (입도 안내자 — 흑수나루) | 우클릭 인사 |

### ★ 이 밤의 사고 둘 (다음 사람은 같은 함정을 밟지 마라)
1. **나루에서 /혼천 조성** → 마을이 나루 세계 심부(y−56)에 재조성, 앵커 9종·구역·**지형 영수증까지 덮임**.
   복구: 장터 앵커 손 복원(서버 정지 상태에서) → 콘솔 조성. **뿌리 = 조성 명령의 세계 가드 부재 (B-126, P2)**.
   부산물: 나루 세계 심부에 묻힌 마을 잔해 + 그 위 스윕 채움 (실피해 없음 판정 — 정리는 나루 월드 재생성 때).
2. **지하정리 첫 실사격 미발** — 표적을 낡은 구역(−2,−56,−1)으로 오독, 원점 심부 85k칸 채움 (실피해 없음).
   수리됨: 해석기 공유(resolveTerrainTarget) + 안전핀(sweepTargetSane — cy<0·괴리 40칸 거부). **상태 변경 명령은 표적 좌표를 먼저 출력·검증한다.**

### ★★ 다음 세션이 이어서 할 일 (우선순위)
1. **B-127 (P3·경계 회귀)**: 복구 재조성 후 ③ 급단차 8.5%·④ 진입 0/4 (전엔 3.5%·4/4). 원인 후보:
   영수증 소실로 이미 깎은 땅을 또 깎음 + 손 복원 앵커 1칸 오차(y89↔90). **재조성 또 돌리지 마라 — 세 번 깎인다.**
   가장자리만 다듬는(feather 재적용류) 수리가 맞다. 사용자에게 눈에 밟히는 턱 좌표를 받아라.
2. **B-126 (P2)**: 조성 세계 가드 — 나루·연무장에서 거부.
3. **B-125 (P4)**: `cheongha_hyeon` 낡은 구역(나루 세계 전고도 상자) 정비 — 환경검수 지역 경로도 이걸 읽는다.
4. 사람 눈 대기: 위 표의 확인 항목들 + `/혼천 입도 재조성`(플레이어 전용 — 허수아비 12→6·글판 재생성. 그 후 `antechamber_audit` exit 0 확인).
5. R6~R8 커밋들의 Codex 검토 요청 (ACTIVE_V6 갱신 — 이 밤엔 병렬 트랙만 돌았다).
6. 대형 트랙 재개: B-110 구현(세계 시계) · SJ-003(서장 다리 계약) · B-122(몰래 죽이기 설계) · 상단 6곳 시안(D-2).

### 이 밤의 계율 추가분
- **안내에 "어디서"를 빼먹지 마라** — 조성 사고의 공범은 조율자의 반쪽 지시였다.
- **플레이어 전용 명령은 RCON execute-as 로 안 된다** (Bukkit sender 프록시 불가) — 사람에게 부탁하거나 콘솔 경로를 설계하라.
- **눈이 재는 곳과 손이 걷는 곳은 같은 해석기여야 한다** — 두 벌이면 언젠가 다른 곳을 가리킨다.

---

## 4-새벽. ★★ 최신 — 2026-07-15 심야 세션 (이 절이 §4-밤 보다 새롭다. 여기서 시작하라)

### 다음 세션의 첫 다섯 걸음
1. 이 절 → `docs/design/cultivation_v3_levels.md` → `docs/BACKLOG.md` (순서대로 읽는다)
2. `python3 tools/backlog_audit.py --run` (장부의 눈 — 이 절 끝의 상태와 대조)
3. 사람 눈 대기 항목을 사용자에게 확인 (아래 표)
4. **병렬 하네스를 연다** (아래 §웨이브-1 — 파일 소유가 갈라져 있다. 겹치면 빌드가 깨진다)
5. B-137 시안이 나오면 사용자 승인을 받고 나서야 B-135 구현이 풀린다 (직렬 관문)

### 이 세션에서 닫힌 것 (전부 커밋됨)
| 항목 | 무엇 | 증거 |
|---|---|---|
| B-019 | 허수아비 12몸의 진범 = **눈의 이중 계수** (ipdo_dummy ⊂ ipdo_dummy_label) | antechamber_audit exit 0 · 계수기 합성 시험 3종 |
| B-128 | ★ 커밋 8a50548 일괄 치환이 **장부를 독살** (닫힘 45곳 복제·상태 20건 전복) → 43곳 복원·가짜 닫힘 18건 재개방 | backlog_audit ③-b 복제 눈 + 상처 ⑫ 신설 |
| B-127 | 경계 회귀 — 원인 = 영수증 소실 재조성의 이음 띠 상실 → **`/혼천 경계다듬기`** 신설 (feather 재적용) | 환경검수 ③ 6.9% ✅ · ④ 통과 ✅ |
| B-131 | 글판 두 장 겹침 — 가림이 엔티티 적재보다 먼저 달렸다 → onPanelsLoad 재적용 | 사용자 확인 (순차 공개 첫 작동) |
| B-132 | 허수아비가 **대립 판정을 굴렸다** (계기가 주사위를) → basicMelee 우회 | 사용자 확인 (과제 해결·관문 진행) |
| B-126·B-119 보완 | 조성 세계 가드 (5명령) · NPC 이행 기동 스윕 (Codex P1 지적) | 기동 로그 — 사람 눈 대기 |
| R6~R8 | Codex 검토 **조건부 승인** (14 통과 · B-116/119 조건 보강 · B-129/130 등재) | CODEX_REVIEW.md §R6~R8 |

### ★★ 헌법 개정 — cultivation v3 (이 세션의 대사건)
사용자가 v2 「레벨은 없다」를 폐기하고 **경험치·레벨·스탯 포인트**를 확정했다.
정본: `docs/design/cultivation_v3_levels.md` — **페이싱까지 전부 승인됨** (실측 재보정 유보):
- 병행+영역 분리: **"스탯은 레벨업에서 찍고, 기술은 써야 는다"** (XP→능력치 · 행위→기술)
- 승급 = 자격 레벨 + 사건 마크 (혼합) · 만렙 없음 · **매 레벨 3포인트**
- 사다리 (base 20 · growth 1.0565 · R 200/h 가정): 삼류 1h/Lv10 (첫 시간에 무공!) · 이류 15h/40 ·
  일류 60h/65 · 절정 140h/80 · 초절정 240h/90 · **화경 400h/Lv100 (최종장 앵커)** · 현경 550h/105 · 생사경 900h/115
- 하드코어 8h/일 = 화경 50일 (삼파전 최소 개전 60일 — 간극 10일, 사건 마크가 둘째 핀)
- 모델: `tools/xp_pacing.py` (자기 시험 5눈) — 실측 오면 R 갈아 끼우고 재산출
- 죽음의 레벨 처분 없음 · 바닐라 XP바 = 경험 · 레벨 소비처 제거(B-136)
- ★구현 관문: **B-137 능력치 저울 재설계** (300pt 를 담는 새 눈금 + 판정 공식 재환산) — 이것이 안 서면 B-135 구현 금지

### ★ 웨이브-1 — 병렬 하네스 (파일 소유 분리 · 겹침 금지)
전부 서로 독립이다. 에이전트 규약: git 금지 · gradle 금지(조율자가 빌드) · 서버/RCON 금지.

| 트랙 | 일 | 소유 파일 | 낼 것 |
|---|---|---|---|
| A. B-137 저울 시안 | 300pt 눈금 후보 2~3안 (주사위 폭·판정 재환산·NPC/장비 연쇄 포함 장단점 표) | docs/design/attribute_scale_v3.md (신설) | 사용자 결정용 시안 |
| B. B-136 선행 구현 | 몹 XP 오브 흡수 + 모루·인챈트대 레벨 소모 철거 (B-137 무관 — 절연층) | 새 Listener 1파일 (예: XpEconomyGuard.java) | 코드 + 기동 실측은 조율자 |
| C. 커스텀 HUD 바 | 내공/내력 **별도 바** (보스바 채널 1순위) — XP바 이사의 선행 | 새 클래스 1파일 (예: EnergyBossBar.java) · SkillHud.energyBar 는 조율자가 이사 | 코드 (디자인 다듬기는 사용자 RP) |
| D. B-110 세계 시계 | WorldClockEngine — advanceWorld 편입·막 진입·박 발화·/막개전 (설계 완료 world_clock.md) | 봇 측 새 클래스 + world_clock.yml 배선 | 코드 + clock_audit 대조 |
| E. RP-4 몹 파일럿 준비 | horangi_v2.bbmodel + 5동작, A안 MobDisplay v2 확장 사양 vs B안 ModelEngine 어댑터 사양 (파트 예산 64 vs 140 선설계 포함). MythicMobs 는 복합 몹 수요 때 별도 ADR | docs/design/rp4_pilot.md (신설) | 비교 데모 계획 (구현은 승인 뒤) |
| F. 상단 6곳 원형 시안 | 태원·무한·소주·강남상로·고창·돈황 `archetype: pending` (B-047) — 기존 원형 21종·수향 문법 위에서 시안. **등록부 반영은 사용자 승인 뒤** | docs/design/sangdan_archetypes.md (신설) | 사용자 결정용 시안 |

**F′. 맵 건축 — 조성 실행 (★조율자 전담 · RCON — 에이전트 금지. 웨이브-1 이 도는 동안 배경으로)**
- 남은 곳 ~30곳: `/혼천 지형조성 <id>` (땅 먼저 — 건축 미결이어도 땅은 선다) → 등급 정해진 곳은 `지역조성`
- ★먼저 **B-100 안전핀 900→1800초 사용자 승인** (화산파 729초 — 곤륜·설산이 걸린다) · 순서는 `scripts/forge_world.sh` 의 우선순위 참조
- 조성 뒤 루프: `/혼천 환경검수 <id>` → 위반 시 **`/혼천 경계다듬기 <id>`** (이 세션 신설 — B-127 을 닫은 손) → 재검수
- 15초마다 [조성·진행] 로그 — **조용하면 그때가 사고다** · 나루·연무장에서 조성 명령 금지는 이제 코드가 막는다 (B-126)
- 딸린 미결: B-125 낡은 구역 정비 (환경검수 지역 경로가 읽는다) · B-085 (2계층, 진행 중) · 사용자 결정 대기: 수향 프로파일(B-094)·새외 4곳(B-048)·점창·청성(B-049 — 사용자 직접 설계)·제갈(B-050)

직렬 (웨이브-2, 관문 뒤): B-135 구현 (B-137 승인 뒤 — XP 원장 스키마는 PG 마이그레이션 경로 준수)
· B-134 기술 층 (미결: 기술 문턱·심안의 행위·발판 철거·나루 가르침) · B-136 가격표 (사용자)
· RP-4 파일럿 구현 (시안 승인 뒤) · 상단 6곳 조성 (원형 승인 뒤 F′ 루프에 편입).

### 사람 눈 대기 (사용자 확인 항목)
나루 허기(B-133 — 달림 과제) · 나루에서 조성 거부(B-126) · 마을 NPC/짐승 타격(B-119) ·
B-118/120/121/123/124 재확인 · 청하현 네 방위 도보 진입 (B-127 은 기계가 닫음 — 눈은 환영).

### 이 세션의 계율 추가분
- **일괄 치환을 장부에 대지 마라** — 닫힘 필드는 그 항목의 것이다 (B-128 의 병).
- **눈의 시험이 남의 병에 기대면 병이 나을 때 시험도 죽는다** — 표본은 설계상 불변이어야 한다 (always_barks).
- **기본값이 「보임」인 가림은 침묵이 곧 겹침이다** — 가림은 대상이 실린 뒤에 다시 걸어라 (B-131).
- **계기와는 대립하지 않는다** — 측정 대상에 주사위를 굴리면 측정이 죽는다 (B-132).
- 미커밋 외래 변경(팩 게이트 off)을 발견하면 **묻고 되돌린다** — 이 세션에서 실제로 있었다 (출처 불명).

### 상태 (세션 종료 시점)
마크 ✔ (01:20 기동 · 오류 0) · 봇 ✔ (PostgreSQL) · 장부 위반 0 (열림 ~53 · 닫힘 ~45) ·
HEAD = bfa1179 언저리 (전부 커밋·빌드 통과) · .orig 4파일은 PG 컷오버 백업 — 정리 여부 미결.

---

## 4-판. ★★★★ 최신 — 2026-07-22밤~23새벽 마라톤 세션 (이 절이 §4-검기 보다 새롭다. 여기서 시작하라)

**한 줄**: 검기가 「시트→판(일러스트)」 방식으로 확정 궤도에 올랐고(8회차 · 베기면 이론),
월아산이 봉으로 대체됐으며, 세계가 **RPG 규약**(블록 파괴·설치 없음)이 됐고, 원거리는
이펙트 문법의 기본 틀이 섰다. 명령어 모음집이 노션에 생겼다.

### 상태 (2026-07-23 05:30)

| 무엇 | 상태 |
|---|---|
| 커밋 | `0206298`(검기 시선추종) → `b98c78b`(봉) → `6ea2fe3`(RPG규약) → `1d54bcf`(활 1단계) → `234bc89`(원거리 fx) → `41c2b0f`~`0aed9d9`(시트판 1~8차) |
| 라이브(run/mvt) | 검기 = **v5 밴드 그대로** + 이 세션의 코드 전부 탑재(시선추종·봉·BlockCovenant·활 사선 v1). 마지막 재기동 02:35 · 오류 0 |
| 테스트(run/mvt-test) | 시트판 8차 구성 (medium 판 · 사본 config) — 봇 kigibot/kigicam 이 붙어 있을 수 있다 |
| 팩 | GitHub `c6b162b3` — 3자 일치(로컬=GitHub=플러그인). ★재빌드하면 라이브 서빙 zip 도 갈리므로 **즉시 재발행 필수** |
| 노션 | 명령어 모음집 — https://app.notion.com/p/3a5b362f1ade8104bb6fdfbcb720835b (정본은 코드 — 명령 바뀌면 갱신) |

### 이 세션에서 닫힌 것 (장부 전부 갱신·커밋됨)

| 항목 | 무엇 | 증거 |
|---|---|---|
| B-171 | 검기 국소축 **부호 버그**(yaw 45°에서 초승달 붕괴 — 4곳) + 「획은 하늘을 보지 않는다」 폐지(pitch 추종. 판정도 같은 거울) | 사용자 인게임 「시선대로 나감」 |
| B-172·B-162 | **월아산 삭제 → 봉 대체** (계열·디자인·이주 별칭 2겹 · 죽은 등록 5벌 청산 · 개방 타구봉 bong 베이스 제자리) | lint·combat_audit 0 · stone_shovel.json 이주 분기 |
| B-161 | ★ **"채굴은 없는 세상, 블록 부수기·설치 자체가 없음 — RPG 세상"** (BlockCovenant — 관리자 크리에이티브만 예외) | 파일 + 배포 |
| B-173 신설 | 병기 적합성 점검 절차 (weapon_fitness_review.md) — 1회차: 모션 죽은 행 보류 · 활 착수 · 암기/중병기 대기 | 문서 |

### ★ 검기 = 시트판(일러스트) — 8회차 확정 스택 (정본: skill_motion_eval_v6.yml)

**방식**: `tools/sheet_to_plate.py` 가 컨셉 시트 TPS 초승달을 RGBA 판 텍스처 3단(피크→빈짐→소멸)
× A/B 반전으로 굽고, kigi_slash 판 경로(medium: 판)가 스윙마다 세운다. **스윙당 엔티티 1장 —
점 2,600개의 예산 충돌이 사라졌다** (라이브 반영의 장애물 없음).

**확정값 (전부 사용자 승인)**: 베기면 고정(`yaw_deg: 90` 신설 축 · billboard FIXED — 정면·후면
칼날처럼 얇고 측면이 그림 전체) · **오목면이 시전자를 감싼다**(반전 텍스처 B 채택) · scale 2.6 ·
몸 앞 1.2m(forward) · 두 스윙 방향 통일 · **점 층 완전 오프 기본** · 농도 2차(문턱10·감마0.55·
×1.15 + 채도 1.25).

**실측 기록**: scratch/camtest/시트판r1~r8 · 아티팩트 https://claude.ai/code/artifact/b970b3c9-7802-4ca2-8294-44cb663961c9

**미결 4**: 빈짐 단 농도 추가 여부 · 시전자 1인칭 보강(본인에겐 얇게만 보인다 — 이론상 맞음) ·
지속 시간 체감 · **라이브 반영 결정**.

### ★ 원거리 — 이펙트 문법 (방향 수정 주의)

- 사선 판정(RangedShot — 벽=엄폐 · 도달 틱 판정)과 `ranged_fx` 3모드(즉발/먹줄/주행)는 산다.
  1회차 4판: 아티팩트 https://claude.ai/code/artifact/5bce9b8b-0e50-48ee-a747-5699ab0d254d
- ★사용자 방향 수정: **바닐라 활 폐기** — 활 형상의 **별도 무기 + 신규 모션 예정** (이름·베이스
  아이템·몸짓 = 사용자 결정 대기). EntityShootBowEvent 훅은 시안 계측용 임시 (B-174 추기 참조)
- 미결정: 사선의 그림 기본 틀 (먹줄 vs 주행 — 무기와 독립, 아직 답 없음)

### 함정 (이 세션에서 전부 실제로 물렸다 — §4-검기의 함정 3종에 추가)

- **팩 굽기 경로 겹침**: MODEL 상수가 이미 models/item 인데 "item"을 또 붙이면 클라만 죽는다
  (보라 큐브). **서버 진단(획 로그)은 못 잡는다 — 범인은 클라 로그** `Missing block model`
- **양면 판의 180° 회전은 시각 무효** — 앞·뒷면이 각자 제 시점에 맞게 그려진다. 좌우 반전은
  회전이 아니라 **텍스처(A/B)** 로 한다
- **봇 리퍼 자기일치**: bash 명령줄에 스크립트 본문이 실려 키워드 grep 이 자기 부모를 문다
  (exit 144). 판별은 **exe/cwd 로만** (`readlink /proc/pid/exe·cwd`)
- publish_pack/serve_resourcepack 은 **라이브 서빙 zip 을 갈아 끼운다** — 시안 굽기 후 GitHub
  재발행을 잊으면 라이브 접속자가 sha1 불일치로 퇴장한다 (이 세션에서 두 번 즉시 봉합)
- redeploy_mvt.sh 기동 대기 60→150초 수정됨 (성공을 exit 1 로 오보하던 것)

### 다음 세션 안건 (우선순위)

1. **시트판 미결 4** 중 라이브 반영 결정이 가장 크다 (장애물 없음 — 사용자 결정만)
2. **양대 관문**: A. 성장 저울(B-137 수치표 승인 → B-135 구현 해금) · B. 세계 밑감(B-148)
3. 원거리 새 무기 설계 대기 (이름·베이스·몸짓) + 먹줄/주행 결정
4. 친구 피드백 오면: 빨간펜→sheet_to_plate 재추출 (전달문 아티팩트 2종: cdb7832f · b970b3c9)
5. 장부 위반 3건 정리 (B-011 닫힘 근거 공란 · B-170 증거 형식·유령 파일 — 전 세션 것)

## 4-검기. ★★★ 최신 — 2026-07-22 검기 템플릿 세션 (이 절이 §4-새벽 보다 새롭다. 여기서 시작하라)

**한 줄**: 검기가 「시트→점 템플릿」 방식이 됐다 — 디자이너 컨셉 시트의 그림을 픽셀로 읽어
그대로 게임 점 2,600개로 옮긴다. 그림이 곧 배치도다. 13회 실측 루프 + 독립 검토 에이전트 1회.

### 상태 (2026-07-22 17:00)

| 무엇 | 상태 |
|---|---|
| 커밋 | `39358fb`(평가판 v6 색감) → `6464010`(템플릿 파이프라인+10회 루프) → `4cf396c`(먹선 백킹·r13) |
| 라이브(run/mvt) | **v5 밴드 그대로 — 안 건드렸다** (`geom_template` 키 없음 = 템플릿 미발동, 예산 600/틱 정본) |
| 테스트(run/mvt-test) | r13 빌드가 돌고 있다. 평가 전용 상향 2건: performance.yml 시야 예산 600→**4000**·전역 4000→**12000**, config 는 `config/skill_motion_eval_v6.yml` 사본 |
| 디자이너 전달물 | `작업물/검기/전달_시트그대로_v9.zip` (전달문·GIF 실속도+슬로모션·피크·오프라인 HTML) — **친구 평가 대기** |

### 파이프라인 지도 (전부 커밋됨)

- `2db88914-….jpg`(루트) = 컨셉 시트 정본 — `tools/sheet_to_template.py` 가 여기서 추출
  → `config/kigi_template_{tps,fps,peak}.json` (관전자/1인칭/대조) → `QiTemplate`(mtime 핫리로드)
  → `QiGeometry.emitTemplate`(관전자=베기면 데칼+먹선 백킹, 시전자=빌보드, y순 스윕 공개)
  → 발동 조건: `kigi_slash.geom_template` 키 (없으면 v5 밴드 — 기존 불변)
- 루프의 눈: `scratch/sheet2game/compare.py` (색 비율·종횡비·IoU) · 회차 기록 `scratch/camtest/v7템플릿_r1~r13`
- 13회 루프에서 배운 것 전부: `git log 6464010 4cf396c` 커밋 본문 (실패 포함 — 반복하지 마라)

### ★ 사용자가 정해야 할 것 (다음 세션 안건)

1. **친구 평가 결과 반영** — zip 전달 후 빨간펜·256×256 텍스처 가이드·도끼 그림이 오면
   `sheet_to_template.py` 입력으로 그대로 재추출
2. **라이브 반영 여부와 예산** — 템플릿 밀도는 시야 예산 4000/틱 전제. 정본 600은 20인 군집
   근거라 충돌. (절충안: 라이브용 점수 축소판 or 예산 층위화)
3. **★팩 지뢰** — `resourcepack/…/particle/generic_0~7.png` 이 축소형(불투명 16~43/64px)이라
   **팩 로드 화면에선 모든 dust 가 잘게 보인다**. camtest(팩 없음)와 실제 화면이 다른 원인.
   사용자 인게임 확인 후: dust 전용 텍스처 분리(`particles/dust.json`) 여부 결정
4. 미착수 개선안(독립 검토가 남김): 날 림 폴리라인 연속 획 · trail 소멸 드리프트 ·
   붓점 텍스처 4단(`ink_dot_0~3` 커밋됨 — 팩 재빌드부터 유효)

### 함정 (이 세션에서 전부 실제로 물렸다)

- `pgrep -f` 대기 루프는 **자기 cmdline 자기일치** → 무한 대기. 판별은 `readlink /proc/<pid>/cwd`
- jar 교체는 **정지→복사→기동** + `rm -rf plugins/.paper-remapped` (캐시 어긋나면 플러그인이
  **조용히** 안 실린다 — 혼천 명령 불완전·효과 0 이 증상)
- dust_color_transition 은 생애 전체 선형 감광 — 피크가 밝아야 하는 연출엔 못 쓴다
  (`SkillHud.emitTransition` 훅은 기술·보스용으로 남아 있다)
- 촬영 측정은 15fps vs 100ms 피크라 회차 간 편차 큼 — 단일 피크 프레임 수치로 회차 비교하지 말 것

## 5. 이 세션에서 세운 것 (요약)

| | |
|---|---|
| **팩** | 텍스처가 **아틀라스 밖**에 있어 획·짐승이 보라 큐브였다 → `textures/item/` 으로. 축 ⑮(아틀라스)·⑯(비율)·⑰(중심) 신설 |
| **전투** | 히트스톱 · 넉백(플레이어에게도) · **기본 초식의 시간 구조** · 참격 모션(호 110°) · 타격 파티클. ★ **무공 쿨다운 중 검이 피해를 한 점도 안 내던 버그** 수정 |
| **경공** | 자동 → **더블 점프** (`PlayerToggleFlightEvent` 가로채기). 크리에이티브 안 깨짐 |
| **나루(입도진)** | 발판 0개였다 (표본이 못 봄) → 이정표는 **전수로** 센다. 튜토리얼이 **못 하는 것을 시키던 것** 수정 |
| **연무장** | 리붓 시 **진짜 인벤토리·장부가 사라지던 것** → `dojang.yml` 금고 (원자적 저장) |
| **지도** | 크기 = 영향력의 함수. `roster` 의 `martial + mandate_weight`. **상업 거점은 별도 사다리** |
| **조성** | 원형 5종 → **21종**. 여덟 문파가 전부 「화산의 도관」이던 것 수정 |
| **접합** | 코드 폐기 → **닉네임 + 게임 안 수락**. *"닉네임은 열쇠가 아니라 수신인이다"* |
| **디스코드** | **상시 안내판 + 버튼**. 명령을 칠 일이 없다. `소문`·`전장`은 **이관이 아니라 형태의 소멸** (NPC 가 말한다) |
| **가문** | 집안이 **실체**가 됐다 (`houses` 표). 성씨 · 적서 · 형제(10%) · **집을 나와도 형은 형이다** |
| **서장** | 디스코드 스레드 → **마크의 책**. **배는 한 명씩** (LLM 직렬화) · **미리 쓰기** |
| **초기화** | `/혼천 초기화` · `/초기화` — 백업 자동 · 세계는 안 건드린다 |
| **청구서** | `docs/BACKLOG.md` + **그것을 재는 눈** |

---

## 6. ★ 사용자가 정해야 할 것 (지금 대기 중)

**`docs/BACKLOG.md` 의 `보류` 30건이 전부 이것이다.** 급한 것:

| # | 무엇 |
|---|---|
| **B-092** | **이미 빚어진 땅** — 갈아엎기로 해소됐는지 확인 필요 |
| **B-097** | 아우가 났을 때 **게임 안에서도 알려 줄 것인가** (지금은 디스코드 DM 뿐) |
| **B-094** | **「수향」 프로파일** — 강남의 물골목. 점묘 승인 뒤가 순서 |
| — | **소림·불가가 남성 전용인가** (등록부에 근거 없음. 아미만 여성 근거 있음) |
| — | **미결 원형**: 상단 6곳 · 새외 4곳 · 점창·청성(**사용자가 직접 설계하기로 함**) · 제갈 기관저택 |
| — | **§17 사다리의 80·40** 은 근거 없음 (64 만 실측 근거) |

---

## 7. ★ 조율자(coordinator)에게 — 이 세션에서 배운 것

1. **에이전트를 병렬로 돌리되 파일 소유를 갈라라.** 겹치면 빌드가 깨지고, 반쯤 만든 코드를 배포하면 **세계가 죽는다** (실제로 두 번 그랬다).
2. **배포 전에 `onEnable` 이 성공하는지 확인하라.** 빌드는 통과하는데 기동이 죽는다.
3. **에이전트의 보고를 그대로 믿지 마라.** 이 세션에서 **씨앗 중 10개가 사실이 아니었다** (레이캐스트 상한은 **있었고**, 파티 엔진도 **있었다**). 청구서 담당이 코드로 확인해서 잡았다.
4. **사용자의 말을 옮길 때 뒤섞지 마라.** 나는 *"성향 → 집안"* 과 *"집안 → 성격"* 을 뒤섞어 읽고 **담당에게 틀린 지시**를 내렸다. 사용자가 직접 바로잡았다.
5. ★ **모르면 물어라.** 사용자는 **지어내는 것을 싫어하고, 묻는 것을 싫어하지 않는다.** 그의 답은 거의 언제나 설계를 더 좋게 만들었다 (적서·거절·형제의 비대칭 전부 그가 냈다).
6. **되돌릴 수 없는 일 전에는 백업하고, 무엇을 지우는지 먼저 보여라.**
