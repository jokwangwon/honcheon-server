# 화산파 모델링 계약 (D2) — 가까이서 봤을 때 무엇이 있어야 하는가

> **사용자 확정 2026-08-09.** `contract.md` 가 **공간/구조** 계약이라면 이 문서는 **모델링** 계약이다.
>
> ```
> 지금까지 : 무슨 건물인가 · 얼마나 큰가 · 어디에 서는가 · 어떤 지붕인가
> 이제부터 : 그 건물이 가까이서 봤을 때 얼마나 정교한가
> ```
>
> ★**「예쁘게 해라」를 계약으로 만들지 않는다.** 예쁘다는 건 검사할 수 없지만
> 「창이 벽보다 한 칸 들어가 있는가」 · 「기둥 아래 주초가 있는가」 ·
> 「처마 밑에 서까래가 있는가」는 **검사할 수 있다.** 지금까지의 방법론과 같다.

---

## 0. 시험체는 강당 한 채다

전 건물에 동시에 넣지 않는다. **강당**을 「화산파 디테일 시험체」로 삼는다 —
새 `assembly` 유형의 첫 건물이고, 크기가 넉넉하고, 단층이라 구조가 읽히고,
`sweep` 지붕과 적주·회벽·격자창이 다 있다. 산문보다 단순하고 행각보다 복잡하다.

```
강당에서 시험 → 전·후 사진 비교 → 만족하면 이 문서 확정
→ 본전 / 산문 / 생활관 / 암자 / 창고 순으로 전파
```
★**전파 전에 고치는 것이 전파 후에 고치는 것보다 훨씬 싸다.**

## 1. 입면 깊이 — 한 면에 다 붙이지 않는다

★**프로파일로 둔다** (사용자 2026-08-09). 적주 +1 과 격자창 −1 사이는 실제로 **2칸 차이**라
강당처럼 큰 집에서는 그림자가 잘 생기지만, 생활관·암자처럼 작은 집에 그대로 전파하면
창이 지나치게 움푹해 보일 수 있다. 값을 건물마다 흩지 말고 **프로파일**만 고른다:

```yaml
facade_depth_profile:
  full:    {column: 1, plaster: 0, lattice: -1}   # 강당 · 본전 · 산문
  compact: {column: 1, plaster: 0, lattice:  0}   # 작은 생활관 · 암자 (필요할 때)
```
★강당의 값이 곧 `full` 의 기준이다 — **지금 값은 바꾸지 않는다.**

색이 아무리 좋아도 **한 평면에 붙어 있으면 평평하다.** 칸마다 앞뒤를 준다
(도면의 `depth:` 절 · 부호는 **바깥이 +**):

| 요소 | 깊이 |
|---|---:|
| 적주 | **+1** |
| 창틀(인방) | +1 |
| 회벽 | 0 |
| 격자창 | **−1** |

## 2. 기둥은 막대기가 아니라 구조체다

```
   도리 / 창방
  ═══════════
      █ 기둥
      █
     ▓▓▓  주초
```
필요한 것: **주초 · 기둥 · 창방/인방 · 도리 · 처마 연결**. 실제 전통 결구를 복제할 필요는 없다.

## 3. 처마 밑 — 가장 크게 갈리는 곳

지붕을 「검은 덩어리」에서 나눈다: **기와면 · 처마 끝 · 서까래 · 도리 · 추녀**.
★**아래에서 올려다봤을 때 서까래가 보여야 한다.**

## 4. 창호는 최소 셋 — 모양과 개수를 <b>가른다</b>

★`family`(모양)와 `density`(개수)를 한 규칙으로 묶으면 생활관과 창고가 둘 다 W1 이라는
이유로 다시 비슷해진다. **갈라 둔다.**

```yaml
window: {family: W2, density: medium}
```

| usage | family | density |
|---|---|---|
| residence | W1 | high |
| storage | W1 / none | low |
| hermitage | W1 | low |
| assembly · corridor | W2 | medium |
| council · main_hall | W3 | medium |
| pavilion | none | none |

우선순위: **`usage` 기본값 → `rank` 보정 → `bay role` 최종 결정.** 해시는 어디에도 안 낀다.

| 창호 | 결 | 쓰는 곳 |
|---|---|---|
| `W1` | 세로살 | 생활 · 창고 |
| `W2` | 사각 격자 | 강당 · 행각 |
| `W3` | 복합 격자 | 본전 · 장로회 |

★`usage` / `rank` / 칸의 역할이 정한다. **해시로 고르면 그건 잡티다** (바닥에서 배운 자).

## 5. 기단 — 무게감은 여기서 온다

```
벽 · 기둥
──────────
석재 기단      ← rank 가 층수를 정한다
████████
한 단 돌출
██████████
마당
```
| 위계 | 기단 |
|---|---:|
| auxiliary | 1 |
| secondary | 2 |
| principal | 2 |
| ceremonial | 3 |

## 6. 공포 — 이름보다 <b>자리 계약</b>이 먼저다

> **공포는 적주의 머리 위에서만 시작한다.**

적주가 없는 곳에 붙으면 그건 공포가 아니라 「처마 밑에 블록을 막 붙여놓은 것」이다.
그리고 공포는 **처마를 받친다** — 기둥 머리 *위*가 아니라 **지붕 바로 아래**에 앉는다.

검사: `공포 anchor == 적주 상단` · `개구를 침범하지 않음` · `지붕 위로 안 튐` ·
`회전 시 outward 도 함께 회전`.

| 위계 | 공포 | 화산파 |
|---|---|---|
| auxiliary | `none` | 행각 · 암자 |
| secondary | `simple` | 생활관 · 정자 |
| principal | `medium` | **강당** · 장로회 |
| ceremonial | `elaborate` | 산문 · 본전 |

## 7. LOD — 모든 곳에 근경 디테일을 넣지 않는다

넣으면 오히려 지저분해지고 서버 자원도 낭비한다.

```
원경 : 지붕 실루엣 · 폭/높이 · 적백 대비 · 층수
중경 : 기둥 간격 · 창호 · 기단 · 처마 · 난간
근경 : 서까래 · 공포 · 문살 · 주초 · 현판 · 등롱 · 작은 장식
```

---

## 이 회차에서 실제로 넣은 것 (강당)

`①②③⑤` (1차) + `④⑥` (2차) — **여섯 다 들어갔다.**
★체계는 전부 만들되 **실물은 강당에만**: 창호 셋(W1·W2·W3) 규칙을 다 만들고 강당은 `W2`,
공포 사다리 넷을 다 만들고 강당은 `medium`(적주 머리 2단 · 밖으로 1 · 좌우 1 — 작게).

## 세 거리로 본다 (D2 부터의 판정 규약)

```
D2_FAR   40~60블록   지붕·실루엣이 망가지지 않았는가
D2_MID   15~25블록   적주·회벽·창호·기단의 반복이 읽히는가
D2_NEAR   4~8블록    주초·창살·서까래·공포가 실제 구조로 읽히는가
```
★**근경↑ 중경↑ 원경↓ 이면 실패다.** 디테일 때문에 원경이 지저분해져서는 안 된다.

## 다음 단계 — 환경 디테일 (D3 · 예고)

건축 모델링 다음은 **그 건물이 실제로 쓰이는 장소처럼 보이게** 하는 일이다:
생활관(빨랫줄·물동이·장작) · 강당(현판·북·무기 거치) · 창고(상자·수레·적재대) ·
암자(약초밭·등불·바위) · 연무장(목인·무기대·깃발). `scene/detail anchors` 로 구조화한다.

---

# REF-1 — 본전 가로 비례 (2026-08-09 · 사용자 확정)

**시각 정본이 옮겨졌다.** 본전이 따라야 할 그림이 `목표 7호`에서 **`mainhall_ref.png`** 로 바뀌었다.
그래서 이번 회차는 D2 를 더 고치는 일이 아니라, **D2 위에서 가로 비례만 교정**하는 일이다.

## 안 건드린 것 (그대로가 정본)

```
패드 9        38×32        ← 넓히지 않는다. 정원·망루도 안 움직인다
하층 몸체     31×14        하층 벽 5
상층 깊이     10           상층 벽 4
rise          현행          정면 9칸 · 중앙 5칸 · 재료
```
사용자 정정: 처음 제시한 절대치 `45/53/49` 는 **비례를 설명하려던 1차 가상 치수**였고
목표가 아니다. 패드 환산값이 정본이다. 정면 11칸도 **강제하지 않는다** — 31폭에 11칸은 너무 촘촘하다.

## 바꾼 것 — 네 군데뿐이다

| 항목 | 전 | 후 |
|---|---:|---:|
| 상층 몸체 폭 | 21 | **25** |
| 층간 지붕 | 35×18(실현 17) | **37×18** |
| 대지붕 | 25×14 | **33×14** |
| 기단 | 2단 | **3단** |

### 상층 폭비 — 0.66 은 superseded

```
구 목표  0.66   근거: 목표 7호 이미지
신 목표  0.806  근거: mainhall_ref.png   (25/31 · 레퍼런스 원본 37/45 = 0.822)
사유     사용자가 본전의 시각 정답을 mainhall_ref.png 로 재지정
```
**틀려서 지우는 것이 아니다.** 7호 시절의 0.66 은 그때의 정본을 정확히 지키던 값이다.
26 으로 가지 않는 까닭: 홀수 중심축과 좌우 대칭이 깨진다 (사용자).

### 좌우 ≠ 앞뒤 — `profile: main_hall_grand`

```yaml
profile: main_hall_grand
eave_x: 3   eave_z: 2      # 층간 : 31×14 → 37×18
upper: { eave_x: 4, eave_z: 2 }   # 대지붕 : 25×10 → 33×14
```
한 값짜리 `eave` 로는 「가로로 넓은 실루엣」이 안 나온다. 그래서 **중심±반폭이 아니라 경계 상자**로 짓는다.
★부수 소득: 몸체 깊이가 짝수(14)라 반폭 셈이 **남쪽 처마를 한 칸 깎아 먹고 있었다** —
옛 경로의 실현 깊이는 18 이 아니라 **17** 이었다. 상자로 바꾸며 대칭이 됐다.

★**기존 `sweep` 은 한 글자도 안 고쳤다** (산문 회귀 방지 — 사용자).

### 말없는 축소 금지

옛 `sweepRoof` 는 패드에 안 들어가면 내밈을 **스스로 줄였다**. 편하지만 역설계에는 못 쓴다 —
정본에 4 라 적고 실물이 2 면 그것이 이 저장소가 몇 번을 당한 **「신고표가 실물보다 넓다」**이다.
`main_hall_grand` 에는 clamp 가 없고, 눈은 **선언값이 아니라 실현 상자**를 잰다.

### 기단 3단 — 본체를 두 칸 올리지 않는다

```
마당 → M(1켜) → N(2켜) → 몸체(3켜)      한 칸씩 세 번
```
바깥으로 두 겹을 둘러 **몸체는 한 칸만** 올라간다. 두 칸이 한 번이라도 있으면 대문이 막힌다.

## 실측 (테스트 서버 · 패드 y55)

```
층간 지붕   x -17..19 (37) · z 38..55 (18)      = 선언과 같다
대지붕      x -15..17 (33) · z 40..53 (14)      = 선언과 같다
상층 몸체   x -11..13 (25) · z 42..51 (10)
하층 몸체   회벽 31 · 적주까지 33
기단        마당 55 → 56 → 57 → 58 (3단)
대문        개구 5켜 (y59~63) · 문지방 = 기단 맨 윗단
```

## 곁가지 — 산문 문루 정면 법선 교정

`outward()` 가 **한 켜짜리 기단(T·B)까지 벽으로 세어** 문루 정면(row 18)의 법선이 남이 아니라
동·서로 떨어져 있었다. **정면 살창 13칸이 옆을 보고 있었다.**
자를 「4켜 미만은 딛는 것이지 가리는 것이 아니다」(`WALL_MIN_COURSES`)로 바로잡았다.
치수·좌표·입면 배열은 한 글자도 안 건드렸다. 실측 `facing=south` 8/8 · 동서 0.
**동결은 오류까지 영구 보존하라는 뜻이 아니다** (사용자 판정).

## ★남은 것 — 하층 정면이 「벽」이 아니라 「주랑」으로 읽힌다

REF-1 을 세우고 정면에서 보니 하층이 열려 보인다. 실측이 까닭을 정확히 짚는다 (y=61 · z=53 정면 평면):

```
col  4 (P)  air          ← 적주가 +1 로 나가며 **벽면에 제 폭만큼 구멍을 남겼다**
col  5 (W)  bone_block
col  6 (D)  air          ← 격자는 −1 로 물러나 z52 에 있다 (의도대로)
```
즉 정면 평면의 세 칸 중 **둘이 비어 있다.** 격자의 −1 은 의도한 그늘이지만,
적주의 +1 은 **옮기는 것이라 자리를 비운다** — 덧대는 것이어야 한다.
강당(작고 비스듬히 봄)에서는 안 드러났고 본전(넓고 정면으로 봄)에서 드러났다.
→ **사용자 결정거리**: 「적주는 옮기지 않고 한 겹 덧댄다」로 갈지, 툇간으로 받아들일지.

---

# REF-1b — 본전 조형 교정 (2026-08-09 · 사용자 확정)

REF-1 의 치수·패드는 승인·유지. 재료도 그대로(`cobbled_deepslate` 계열). **형태만** 고쳤다.

## ★깊이는 「옮기는 양」이 아니다 — D2 계약의 의미 보정

정면 사진이 드러낸 것: `+1` 을 **이동**으로 구현하니 적주가 나가면서 **벽면에 세로 구멍**을 남겼고,
하층이 **폐쇄 전각이 아니라 주랑**으로 읽혔다. 레퍼런스의 하층은
「붉은 목구조가 흰 회벽·창호 **앞에 덧대어진** 폐쇄 전각」이다.

**부호가 방식을 정한다** — 도면에 새 글자를 안 늘린다:

| 부호 | 방식 | 뜻 |
|---|---|---|
| `+` | **overlay** | 기준면은 **배경(회벽)으로 채우고** 한 칸 밖에 덧댄다 |
| `0` | base | 기준면 그대로 |
| `−` | **recess** | 기준면엔 **창틀만** 남기고 **채움 켜만** 안으로 물린다 |
| — | carve | 개구는 처방의 `air` 켜가 이미 판다 |

배경 글자는 **깊이 0 이면서 회벽을 쓰는 처방**을 찾아 쓴다 (`Blueprint.backingChar()`).
★이 보정은 본전만이 아니라 **깊이를 쓰는 모든 도면**에 적용된다 (현재 강당·본전).

실측 단면 — D칸(x−12) 기준면 z53 / 한 칸 안 z52 (y56→67):

```
z53   ####...#####      창틀은 남고 창만 3켜 뚫렸다
z52   ###.###....#      살창이 그 뒤에 선다
```

## ★grand 물매는 오목하다

REF-1 로 지붕 **상자**는 맞췄는데 화면은 여전히 달랐다. 까닭은 물매다 —
한 켜에 한 칸씩 곧게 좁히면(1:1) 외피가 **계단식 피라미드**로 읽힌다.
`mainhall_ref.png` 의 지붕은 **처마 쪽이 완만하고 용마루로 갈수록 급하다**.

```
바깥 1/3 : 두 칸에 한 켜   (넓고 완만한 처마)
그 안    : 한 칸에 한 켜   (용마루로 갈수록 급해진다)
```

★한 켜에 **두 칸 이상 뛰지 않는다** — 뛰면 외피에 세로 구멍이 생긴다.
★평탄 구간의 바깥 켜는 **반블록**이다 — full block 이면 그 켜가 통째로 테라스 한 단이 된다.
★기존 `sweep`(산문 등)은 이 식을 **안 탄다**. 그대로 1:1 이다.

실측 물매 (남쪽 처마 z55 → 안으로) = `[0,0,1,1,2,3,4,5,6]` — **설계식과 같다.**

## 그 밖

- **단청 띠 분절**: 밝은 `cut_sandstone` 은 **적주 머리에만**. 회벽·격자 위는 어두운 도리로 잇는다.
- **상층 살창 배경**: 살창 뒤에 회벽을 댄다 — 안 대면 상층이 「적주 │ 검은 빈칸」의 되풀이로 읽힌다.

## 실측 (REF 카메라 · 패드 y55)

```
정면 기준면      창 개구(3켜) 말고는 전부 막혔다 — 세로 구멍 0
적주 덧댐        정면 8/8 (모서리 둘은 법선이 옆이라 옆으로 덧댄다 — 맞다)
층간 지붕        x −17..19 (37) · z 38..55 (18)     REF-1 그대로
대지붕           x −15..17 (33) · z 40..53 (14)     REF-1 그대로
귀솟음           네 귀 +2 · 처마 가운데 +0          (사용자 시험값 [0,0,+1,+2] 과 같다)
```

## ★남은 것 — 상부 띠가 아직 한 덩어리로 읽힌다

`cut_sandstone` 을 적주 머리로 몰았는데도 띠가 밝다. 까닭은 **재료가 아니라 두께와 그림자**다:
창방(판) + 도리(적주와 같은 목재) + 상인방이 **같은 평면에 세 켜로 붙어** 있고,
공포(elaborate 3단)가 그 띠 **안쪽**에 들어가 골을 못 만든다.
→ **사용자 결정거리**: ① 가로보를 어두운 목재로 내릴지 ② 공포를 한 켜 내려 띠와 처마 사이에
   그림자 골을 낼지.

---

# REF-1c-A — 본전 큰 면 정리 (2026-08-09 · 사용자 확정)

REF-1b 의 bbox·물매·귀솟음·상층 폭·기단은 승인·유지. 재료 family 도 그대로.
이번 회차는 **네 가지뿐**이다: 밝은 가로띠 / 관통 터널 / 그림자 골 / 지붕 외피.

## ★★★진범 — 「밝은 갈색 3단 띠」는 재료가 아니라 **공포**였다

재료를 세 번 바꿔도(`cut_sandstone` 을 적주 머리로, 상단 보를 `dark_oak` 로) 띠가 안 없어졌다.
실물을 칸마다 재서 나온 답:

```
정면에서 먼저 만나는 블록 (z55/z54)
 y63  dark_oak_planks   ← 공포
 y64  dark_oak_planks   ← 공포        모든 x 에서
 y65  dark_oak_planks   ← 공포        똑같이 나왔다
 y66  dark_oak_log      ← 서까래
```

**공포가 정면 전 폭에 깔려 있었다.** 까닭 둘 —

1. **「적주인가」 판정이 너무 넓었다.** 조건이 「처방에 `mangrove_log` 가 **들어 있는가**」였는데,
   본전은 **회벽·격자의 인방도 mangrove 한 켜**다. 그래서 **모든 벽 칸이 적주로 세어졌다.**
   → 적주는 **세로로 이어진 기둥**이다: `count >= POST_MIN_COURSES(3)`.
   ★강당은 회벽에 목재가 없어 **우연히** 멀쩡했다 — **우연은 계약이 아니다.**
2. **공포가 좌우 ±1 로 번졌다.** 본전 적주는 3칸 주기라 ±1 이면 3칸을 다 덮어
   **이웃 공포끼리 손을 잡았다.** → 공포는 **적주 위에서만**. 번지면 제가 만들려던 리듬을 지운다.

★**옛 눈은 소스에 조건이 적혀 있는지만 봤다.** 조건은 적혀 있었고, 다만 **너무 넓었다.**
  이제 눈이 **실제로 몇 칸이 골라지는지를 센다** — 글자가 아니라 결과를 묻는다.

## 그 밖 셋

- **중앙 5칸 = 개구 3 + 문설주 1+1.** 문설주는 **적주가 아니라 어두운 문틀(`J`)** 이다 —
  처음 `P` 로 세웠더니 col16·17 과 21·22 가 붙어 **7호 실측의 3칸 주기가 깨졌고** 눈이 잡았다.
  문틀이 앞으로 한 칸 나가므로 문이 그만큼 뒤에 앉는다 — 관통 터널이 아니라 **들어간 입구**.
- **그림자 골 1켜** — 공포 머리와 처마 밑 사이를 비운다 (`GROOVE`). 공포는 여전히 처마를 안 넘는다.
- **지붕 외피** — 물매값 `[0,0,1,1,2,3,4,5,6]` 유지. 평탄 구간에서는 **모서리도 반블록**이다.

## 실측 (REF 정면 카메라)

```
최장 연속 밝은 보   y62~65 = 2칸 (공포가 적주 위에만)  ·  y66 = 37칸 (서까래 줄 한 켜)
중앙 개구           3칸 (x0~2) + 좌우 문틀 2칸        ※그 밖의 빈칸은 창 개구다
그림자 골           회벽칸 앞 y65 비었고 y66(서까래)에서 막힌다 — 골이 보인다
노출 full block 턱  <b>0</b>  (표본 190칸: 계단 80 · 반블록 68)
지붕 상자           층간 37×18 · 대지붕 33×14        (REF-1 그대로)
```

## ★남은 것 — 서까래 줄 한 켜

`y66` 이 아직 37칸 통줄이다. 다만 이것은 **서까래**라 건축적으로는 이어지는 것이 맞다.
띠 넷이 한 켜로 줄었다. 더 죽이려면 **재료 회차(REF-2)** 에서 어둡게 하는 쪽이 맞다 —
형태로는 더 뺄 것이 없다.

---

# REF-1c-B — 본전 실루엣 마감 · 중앙 위계 (2026-08-09 · 사용자 확정)

REF-1c-A 승인. bbox·물매·귀솟음·상층 폭·기단·bay·재료 family 는 **변경 금지**로 두고,
**실루엣의 마감점과 중앙 위계**만 얹었다.

## 1·2. 용마루와 귀마루 — 지붕의 **선**

물매와 귀솟음이 잡힌 뒤에도 정상부가 검은 면으로 닫혀 있으면
「잘 만든 큰 지붕」이 아니라 「검은 경사지붕」으로 읽힌다. 그 차이가 **선**이다.

```
용마루   대지붕(33) 위에 17칸 · 가운데 15칸 평평 · 양 끝 한 칸만 +1
         마루선 stone_brick_slab · 끝 andesite_wall
귀마루   네 귀 각 5칸 — 귀솟음이 쥔 첫 칸(k=0)은 안 건드리고 그 안쪽부터 얹는다
         ★이미 올라간 모서리를 따라가는 선이다: <b>지붕을 새로 들어 올리지 않는다</b>
층간 지붕  중앙 용마루 <b>없음</b> — 가운데를 상층 몸체가 차지한다 (사용자)
```
※사용자 후보 `polished_andesite_wall` 은 **실재하지 않는 블록**이다 (연마안산암은 반블록·계단만).
  같은 밝은 회색 계열인 `andesite_wall` 로 갔다.

## 3·4. 현판 자리와 중앙 관 — 도면이 좌표를 쥔다

평면·기둥 처방으로는 못 그리는 **국소 장식**이라 `trim:` 절을 새로 뒀다
(「한 자리에 몇 칸」이라 압출이 아니다).

```
현판 자리   문 개구 3칸 바로 위 · 한 칸 나옴 · dark_oak_log
            ★자리만 만든다 — 글자·아이템·현판 블록은 D3 (사용자)
중앙 관     아랫단 5칸(중앙 칸 폭) + 윗단 3칸 · 높이 2 · 한 칸 나옴
            ★<b>독립 지붕이 아니다.</b> 중앙 처마선을 한 번 위로 튀게 하는 문두 장식.
            ★<b>용마루와 경쟁하면 안 된다</b> — 관 꼭대기가 층간 처마보다 낮아야 한다.
```

## 5. 상층 창턱 — **적주에서 끊긴다**

창 아래 어두운 수평선 한 켜(`dark_oak_slab`). ★전 폭 연속 판으로 만들면
**「밝은 띠」를 다시 짓는 셈**이다. 그래서 bay 안에서만 놓고 적주에서 끊는다.

## 실측 (REF 카메라)

```
용마루            17칸 (y81 · x−7..9) · 끝 솟음 2개 (y82)
귀마루            4벌 × 5칸 = 20칸 (y75~79) · 지붕 꼭대기 y82 불변 → 새 상승 0
현판 자리         3칸 (x0~2 · y64 · +1)
중앙 관           아랫단 5칸(x−1..3 · y65) + 윗단 3칸(x0~2 · y66) — 높이 2 · +1
                  관 꼭대기 66 < 층간 처마 67  → 용마루와 경쟁 안 함
상층 창턱         조각 8개 × 2칸 · 최장 연속 2칸 (적주에서 끊긴다)
지붕 상자         층간 37×18 · 대지붕 33×14  — <b>불변</b>
```

---

# REF-2A — 본전 지붕 재료 A/B (2026-08-09)

REF-1c-B geometry **동결**. 치수·물매·용마루·귀마루·관·창호·기단 한 칸도 안 건드렸다.
목적은 조형이 아니라 **재질의 판독성 비교**다.

```
A  cobbled_deepslate 계열   (현재)
B  deepslate_tiles 계열     (시험)
```

## 왜 재료가 문제인가

절벽도 회색, 석축도 회색, 기단도 회색, 지붕도 **거친 회색**이었다.
조약 심층암은 절벽·석축과 **질감 언어가 같아** 화면에서 지붕이 산과 한 덩어리로 뭉친다.
기와 계열로 가면 역할이 갈린다: **절벽=자연석 · 석축=가공 석재 · 지붕=기와**.

## ★「속은 안 보이니 둬도 된다」에 기대지 않았다

사용자는 내부 채움을 그대로 둬도 된다고 허용했다. 그런데 **실측해 보니 채움 열셋이
위에서 볼 때 맨 위로 드러났다.** 그러면 지붕 한 면에 두 재료가 선다 —
이 저장소가 이미 한 번 당한 **「한 면에 한 재료」** 위반이다. 그래서 채움도 계열을 따르게 했다.

```
고치기 전   기와 171 · 조약 심층암 13
고친 뒤     기와 184 · 조약 심층암 <b>0</b>
```

## 실측 (표본 197칸 · 위에서 본 맨 위 블록)

```
deepslate_tile_stairs   104      ← 드러난 경사
deepslate_tile_slab      63      ← 처마 끝 · 평탄 구간
deepslate_tiles          13      ← 채움
deepslate_tile_wall       4      ← 치미
stone_brick_slab         11      ┐ 마루 — 그대로 둔다
andesite_wall             2      ┘
cobbled_deepslate         0
```

상자 불변: 층간 **37×18** · 대지붕 **33×14**. 눈 **432/432** (A·B 동일).

## 되돌리는 법

도면 한 줄이다 — `roof.본전.material_family` 를 지우거나 `cobbled_deepslate` 로 되돌린다.
★기본값이 `cobbled_deepslate` 라 **산문·행각·정자 등 다른 도면은 한 칸도 안 바뀐다.**

---

# REF-2A 확정 · REF-2B 목구조 (2026-08-10 · 사용자 확정)

## REF-2A — B 채택

```
A cobbled_deepslate  →  superseded
B deepslate_tiles    →  <b>본전 정본</b>
```
근거(사용자): 원경에서 지붕이 절벽·석축에서 한 단계 떨어져 **「산의 일부」가 아니라
「가공된 건축 지붕」**으로 읽힌다. 정면에서는 처마가 돌덩어리로 뭉치지 않고 **얇은 기와선**이 된다.
33×14 에서 결이 잡티로 튀지도 않았다 — 원경에서는 하나의 어두운 면으로 합쳐진다.

★**화산파 모든 지붕의 기본값으로 승격하지 않는다** (사용자). 본전 도면이
`material_family` 를 쥐는 현재 구조를 유지한다 — 산문·강당·정자는 제 실루엣과 규모를 보고
**따로 판정한다.**

## REF-2B — 목구조: 더 붉게가 아니라 **역할을 가른다**

지붕이 기와가 된 뒤에도 목재가 한 갈색 덩어리로 뭉쳤다. 세 역할을 색으로 뗐다.

| 역할 | 재료 | 밝기 |
|---|---|---|
| 주요 적주 · 프레임 | `stripped_mangrove_log` | **붉은색** |
| 인방 · 창방 · 보조 보 | `mangrove_planks` | **어두운 적갈색** |
| 도리 · 긴 보 · 서까래 · 창호 | `dark_oak` 계열 | **매우 어두운 갈색** |

★밝은 사암은 여전히 **적주 머리 한 점**뿐이다 (띠가 아니라 점 — REF-1c-A).
★지붕 B판은 **완전히 동결**한 채 목재만 갔다 — 무엇이 색 때문인지 갈리게.

## 실측 — 정면이 네 층으로 갈리는가

```
회벽칸 (기준면)                     적주칸 (덧댐면)
 y67  기와 (deepslate tiles)         y67  기와
 y66  dark_oak_log      긴 보        y66  dark_oak_log
 y65  dark_oak_planks   도리         y65  dark_oak_planks
 y64  mangrove_planks   창방         y64  cut_sandstone    ← 밝은 점 (주두)
 y63  mangrove_planks   인방         y63  dark_oak_planks  ← 공포
 y62  bone_block        회벽         y62  stripped_mangrove_log  ┐
 y61  bone_block                     y61  stripped_mangrove_log  │ 붉은 적주
 y60  bone_block                     y60  stripped_mangrove_log  │
 y59  mangrove_planks   인방         y59  stripped_mangrove_log  ┘
 y58  smooth_stone      기단         y58  smooth_stone
```
격자칸 안쪽: `dark_oak_trapdoor` / `dark_oak_planks` — **창호가 가장 어둡다.**

지붕 상자 불변: 층간 **37×18** · 대지붕 **33×14**. 눈 **437/437** · 유출 0.

## ★자가 두 번 더 낡았다 — 팔레트에 얹힌 눈

목재를 역할대로 가르자 **재료 이름으로 구조를 고르던 눈 둘**이 헛짚었다.

1. **문설주 판정**이 「`mangrove_log` 를 포함하는가」였다 → 문설주가 어두운 참나무만 남자 0 을 셌다.
   → **세로로 이어진 통짜 기둥**이라는 구조로 고른다.
2. **공포 「넓은 자 vs 바른 자」** 증명이 「오늘의 도면에서 넓은 자가 더 고른다」였다 →
   인방이 `mangrove_planks` 가 되며 차이가 사라져 짖었다. **증명이 오늘의 팔레트에 얹혀 있었다.**
   → 이제 회벽에 한 켜짜리 붉은 통나무를 **일부러 끼워** 넓은 자가 무너지는 것을 직접 보인다.

**재료 이름으로 구조를 고르지 마라.** 팔레트는 바뀌고 구조는 안 바뀐다.

---

# REF-2.5 — 블록 조형 (Shape Language) · 2026-08-10 사용자 지시

> 「일단 블록의 색으로 분리시키는 것도 좋은데, 보통 마인크래프트 건축물은 **형태도 정리되어**
> 표현감을 주는 것 아닌가요? 지금 사진에서 볼 땐 그냥 색 분리인 것 같은데.」

**맞는 감각이었다.** REF-2A·2B 로 재료의 역할은 갈렸지만, 몸체는 여전히
`벽=full block · 기둥=full block · 보=full block` 이었다. 지붕이 좋아진 까닭은 재료가 아니라
**계단·반블록·담장·귀솟음·용마루로 형태를 썼기 때문**이다. 몸체에도 그것이 필요했다.

★**성공 조건**: 스크린샷을 **흑백으로 바꿔도** 기둥·창·보·공포·기단·중앙문이
서로 다른 부재로 읽힌다. 색을 다 빼고도 전·후가 달라야 한다.
★재료·팔레트·bbox·bay·기단 단수·지붕은 **한 칸도 안 건드렸다** — 무엇이 형태 때문인지 갈리게.

| | 무엇을 | 어떻게 |
|---|---|---|
| **S1** 기둥 | 통나무 한 줄 → **작은 모델** | 주초 `stone_brick_wall`(통나무보다 가늘다) → 몸통 → 주두 |
| **S2** 창 | 네모 구멍 → **창턱·상인방** | 창턱 `mangrove_slab` — 살창 아래에 턱과 그늘 |
| **S3** 보 | 통짜 판 → **반블록 단면** | 도리 `dark_oak_slab` — 정면을 가로지르는 **기하가 만든 그림자 골** |
| **S4** 공포 | 판재 더미 → **계단형 받침** | 계단 → 반블록 → 계단. 위로 갈수록 밖으로 받쳐 나간다 |
| **S5** 기단 | full block 케이크 → **몰딩** | 맨 밖에 `stone_brick_slab` 치마 한 바퀴. 단 높이·문지방 불변 |
| **S6** 중앙문 | 한 겹 → **세 겹 깊이** | 벽 0 · 문설주 +1 · **문두 처마 +2** (`dark_oak_slab`) |

## ★계단의 방향도 「자리가 정한다」

도면 한 장이 네 벽에 다 쓰이므로 도면은 방향을 못 적는다. 살창과 같은 규약으로,
**계단은 오르는 쪽이 안쪽**을 향한다 — 높은 면이 벽에 붙고 낮은 단이 밖으로 나와
처마 밑에 그늘이 지는 몰딩이 된다 (지붕 `putRoofStair` 와 같은 규약).

## 실측 (REF 정면 카메라)

```
S1 주초              8/8 (모서리 둘은 법선이 옆이라 옆으로 선다 — 맞다)
S4 공포 계단·반블록·계단   8/8 · 8/8 · 8/8
S3 도리 반블록 골     8/8
S2 창턱 반블록        8/8
S5 기단 몰딩 치마     6/6
S6 문두 (+2)          7/7
지붕 상자             층간 37×18 · 대지붕 33×14   <b>불변</b>
```

**흑백 비교**: 전(REF-2B)은 기둥·벽이 비슷한 회색 판으로 뭉치고 상부가 한 덩어리다.
후는 공포가 기둥마다 튀어나오고, 긴 보 밑에 어두운 골이 지나가고, 기둥 발치가 잘록하고,
창 밑에 턱이 생기고, 기단에 얇은 단이 하나 더 붙는다. **색 없이도 갈린다.**

## 남은 순서

```
REF-2C   회벽·창호 최종색   ← 형태가 잡힌 뒤에 색을 마감한다
REF-3    레퍼런스 세부
D3       사용 흔적·소품
```

---

# REF-2C — 회벽·창호 최종색 (2026-08-10) · **측정으로 끝냈다**

사용자: 「REF-2C 에서 또 여러 색 후보를 시험하며 시간을 많이 쓰면 안 된다.
현재 가장 큰 품질 상승 여지는 색이 아니라 REF-3 형태에 있다.」

그래서 후보를 렌더해 비교하는 대신 **지금 화면을 재서** 무엇이 실제로 안 갈리는지부터 봤다.
정면 사진을 구간별로 색 양자화해 **명도·채도**를 뽑았다.

```
회벽 판          명도 41   채도  3   ← 화면에서 <b>유일한 무채색 밝은 면</b>
주두 (밝은 점)   명도 50   채도 11
상부 띠 목재     명도 32~37 채도 15~16   ← 넷이 Δ5 안에 몰려 있다
적주             명도 24   채도 31~38
창살·실내        명도 16~17
```

## 세 질문에 대한 답

| 질문 | 답 | 근거 |
|---|---|---|
| `bone_block` 유지 vs 대체 | **유지** | 회벽은 화면에서 **유일한 무채색(채도 3) 밝은 면**이다. 목재는 전부 채도 15 이상이라 **채도로 갈린다.** `white_terracotta` 는 더 어둡고 붉어 목재 무리로 내려앉고, `calcite` 는 사용자 지적대로 석재로 읽힌다. |
| 창호 명도 | **유지** | 창살·실내 16~17 vs 적주 24 → Δ8. 갈리고, 더 죽이면 살창 결이 사라진다. |
| 밝은 베이지 accent | **주두는 통과 · 상부 띠는 색 문제가 아니다** | 주두 50 vs 띠 36 → Δ14 로 점이 살아 있다. 남은 것은 **띠 안의 목재 넷이 Δ5 안에 몰린 것**인데, 이는 처마 그늘이 색상차를 눌러 생긴 것이라 **재료를 바꿔도 안 갈린다.** |

## ★결론 — 색은 바꾸지 않는다

띠가 아직 한 덩어리로 보이는 까닭은 **재료가 아니라 그 자리의 빛**이다.
처마 밑 깊은 그늘이 채도차를 눌러, 서로 다른 네 목재가 같은 명도로 수렴한다.
**색 후보로는 못 고친다** — 켜 수를 줄이거나 골을 더 깊게 파는 **REF-3 형태의 일**이다.

그래서 REF-2C 는 **변경 없음**으로 닫는다. 이것이 이 회차의 결과다.

---

# REF-2.5-S1R — 기둥 재역설계 (2026-08-10 · 사용자 지적)

> 「현재 기둥은 아래가 `stone_brick_wall` 이라 몸통보다 확 가늘어져서, 전통 목조건축의
> 기둥이라기보다 **회색 금속 다리나 촛대 위에 붉은 기둥을 얹은 모습**으로 읽힙니다.」

## ★★합격 기준을 한 단계 올린다

```
① 색을 빼도 형태가 읽히는가          ← REF-2.5 는 통과했다
② 그 형태가 레퍼런스와 같은 방향인가  ← 기둥은 여기서 떨어졌다
```
**흑백에서 달라진다 ≠ 레퍼런스와 닮았다.** 앞 판의 담장 주초는 ①만 통과하고 ②에서 떨어졌다.
앞으로 조형 회차는 두 관문을 다 넘어야 한다.

레퍼런스(`화산파/…07_09_25 (4)` 근경)를 다시 보면 기둥은 **굵기가 그대로 내려와 석재 바닥에서
끝나고**, 머리 위는 밝은 석재 캡이 아니라 **어두운 목재 구조가 벌어지며 처마를 받는다.**

## 고친 것

| | 앞 판 | S1R |
|---|---|---|
| 주초 | `stone_brick_wall` — **몸통보다 가늘다** | `polished_andesite` — **같은 폭 · 낮다** |
| 몸통 | 통나무 5켜 (실은 4켜만 남았다) | 통나무 **5켜 온전** |
| 주두 | `cut_sandstone` 밝은 점 | **목재** `mangrove_stairs` — 공포의 첫 단이 곧 주두 |

★**공포가 적주 몸통을 먹고 있었다.** 그림자 골을 한 켜 비우느라 공포 아랫단이 적주 평면(+1)에
앉았고, 그 자리가 몸통의 맨 윗칸이었다 — 붉은 켜가 5가 아니라 **4**였다. 차례도 뒤집혀
`적주 → 공포 → 주두` 였다. 골을 0 으로 내려 `적주 → 주두 → 공포 → 도리` 로 바로잡았다.

★**골은 사라지지 않는다.** 아랫단만 적주 평면(+1)이고 위 단들은 처마 평면(+2)이라,
처마 평면의 그 아래 칸이 비어 그늘이 진다 — **켜로 비우는 대신 윤곽이 스스로 만든다.**

## 실측

```
주초 polished_andesite   8/8      담장·울타리 받침  0/8  (촛대 소멸)
몸통 적주 5켜 온전        8/8      주두 mangrove_stairs 8/8
공포 2단 dark_oak_slab   8/8      처마 평면 아래 그늘   6/8
```

## ★남은 것 — 공포 3단이 서까래와 자리를 다툰다

`elaborate` 는 3단인데 맨 윗단이 `y=처마−1` 로 가고, 그 켜는 **서까래**가 이미 쓰는 자리다.
지붕이 나중에 놓이므로 서까래가 이긴다 → **눈에 보이는 공포는 사실상 2단**이다.
처마가 2 라 세 단을 다 드러내려면 처마를 키우거나(REF-1 에서 실측으로 막힌 값) 서까래와
공포를 한 부재로 합쳐야 한다 — **REF-3 결정거리.**

## 눈도 틀린 조형을 지키고 있었다

앞 눈은 「담장 블록이 있는가」를 물었다. 그 담장이 바로 기둥을 촛대로 만든 범인이었으니,
**눈이 틀린 조형을 지키고 있었던 것**이다. 이제 묻는 것은 「주초가 몸통보다 가늘지 않은가」다.

---

# REF-3A — bay 역할별 형태 차이 (2026-08-10 · 사용자 확정)

**확대는 미룬다.** 사용자 판단: 「지금은 건물이 실제보다 작다기보다 **같은 형태의 반복이 많아서**
규모감이 충분히 표현되지 않는 부분도 크다.」 그래서 크기가 아니라 **반복**을 먼저 깬다.

★**랜덤이 아니라 자리가 깬다** — 기존 계율 「위치가 정하면 구조, 해시가 정하면 잡티」 그대로.

```
전   P W D P W D P W D P W D P J O O O J P D W P D W P D W P D W P
후   C W D P W D P W D P W D A J O O O J A D W P D W P D W P D W C
     └모서리                └입구 옆      입구 옆┘            모서리┘
```

| 역할 | 처방 | 왜 |
|---|---|---|
| **C** 모서리 | 주두 **두 켜** (`mangrove_stairs` + `dark_oak_slab`) | 머리가 굵어 **귀마루로 이어지는 끝**을 잡는다 |
| **A** 입구 옆 | **보조 인방** 한 켜 (`mangrove_planks`) | 중앙으로 갈수록 켜가 늘어 **중심을 예고**한다 |
| **P** 일반 | REF-2.5-S1R 기본형 | |
| 중앙 | 문설주 · 문두 · 현판 자리 · 관 | 이미 있다 (REF-1c-B) |

그래서 정면이 **강한 모서리 → 반복 → 강한 중앙 → 반복 → 강한 모서리** 로 읽힌다.

★셋 다 **켜 수는 11 로 같다** — 다르면 지붕이 기운다. 형태만 다르고 높이는 하나다.
★셋 다 몸통이 3켜 이상이라 **공포 계약이 그대로 걸린다.**

## ★★같은 병이 <b>세 번째</b>다 — 눈이 이름을 세고 있었다

`3칸 주기` 눈이 **글자 `P`** 를 세고 있었다. 역할을 나누자 기둥이 10 → 6 으로 줄어 보였다 —
조성은 멀쩡한데 눈이 글자를 센 것이다. 앞서 두 번과 같다:

```
① 공포 판정    「mangrove_log 를 포함하는가」  → 인방까지 적주로 셌다
② 문설주 판정  「mangrove_log 를 포함하는가」  → 팔레트가 바뀌자 0 을 셌다
③ 주기 판정    「글자가 P 인가」               → 역할을 나누자 6 을 셌다
```
**이름이 아니라 구조로 세라.** 세로로 이어진 통나무가 곧 적주다.

## 확대는 아직 안 한다 — 하게 되면 이렇게

패드 폭이 38 이라 층간 지붕 37 이 이미 한계다. **패드를 안 건드리면 본전은 더 못 커진다.**
REF-3 까지 하고도 원경에서 왜소하면 그때 **패드째** 넓힌다 (사용자 1차 목표):

```
하층 몸체 31 → 35~37    상층 25 → 29~31    층간 지붕 37 → 43~45
대지붕 33 → 39~41       기단 3단 유지 · 폭만 확대
정면 bay 9 <b>유지</b>  ← 11 로 늘리면 다시 촘촘해진다. 한 칸의 호흡을 넓힌다
패드 38×32 → 46~48 × 34~36   (장문인 정원·망루 재배치가 딸린 큰 회차)
```

## 순서 — 형태가 먼저, 리소스팩은 나중

```
Shape  →  Material  →  Texture
블록 모양으로 형태 · 블록 종류로 색/재질 · 리소스팩으로 미세 질감
```
★**리소스팩에 의존하면 안 된다.** 바닐라로 봐도 본전다워야 하고, 리소스팩은 그 위에
고급스러움을 더하는 것이어야 한다. 「팩을 껴야만 그럴듯한」 상태는 유지보수도 설계 기준도 흐린다.

---

# REF-3B — 위계를 원경까지 (2026-08-10 · 사용자 확정)

REF-3A 판정: 「근경·중경 승인 · **원경 부분 승인** — 방향은 맞고 구조도 맞으나 원경 위계는 아직 약하다.」
그래서 이번 회차는 장식을 더 붙이는 것이 아니라 **REF-3A의 역할이 실제 윤곽으로 보이게** 하는 일이다.
새 재료를 안 만든다 — 있는 블록의 stairs/slab/wall 배치만 바꾼다.

## 1. CENTER — 문두를 한 번 꺾는다

```
전   ┌──────────┐        후     ┌──────┐
     │          │            ┌─┘      └─┐
```
90도 네모는 크기만 클 뿐이다. 양 끝을 계단으로 한 번 꺾고, 문두와 개구 사이에
**내부 문틀 어깨** 한 단을 넣었다. ★crown·현판 자리 크기는 **안 키웠다** (사용자).

## 2. ★공포 3단이 드디어 보인다 — 순서를 바꿨다

공포가 **지붕보다 먼저** 놓이고 있었다. 그래서 맨 윗단이 서까래와 같은 켜를 다투다
**덮여 사라졌다** (실측 0/8). 처마를 키우지 않고 **순서만** 뒤집었다:

```
전   기둥·공포 → 지붕(서까래가 공포를 덮는다)      보이는 단 2
후   기둥 → 지붕 → <b>공포</b>                        보이는 단 <b>3</b>
     기둥 → 주두 → 받침 → 받침 → <b>서까래 신발</b> → 서까래
```
★맨 윗단은 **서까래 신발**이다. 「3단이 세로로 세 켜여야 한다」가 아니라
**윤곽이 3단으로 보이면 된다** (사용자). 실측 **6/6**.

## 3. C — 모서리는 굵은 기둥이 아니라 **두 축이 모이는 곳**

`outward()` 는 한 방향만 준다. 모서리에서는 **두 면 모두**에 공포를 얹는다.
판정은 이름이 아니라 자리다 — **가로로도 세로로도 벽이 이어지지 않는 칸**.
실측 **네 모서리 4/4** 가 두 축을 받는다.

## 4. A — 중앙으로 뻗을 칸이 없다

`17·21` 이 이미 문설주라 **가로로 뻗을 자리가 없다.** 그래서 세로로 밀도를 올렸다 —
주두 두 켜를 **둘 다 붉은 목재**로 두어, 어두운 두 켜인 모서리와 **형태는 같고 결이 다르게** 했다.

```
밀도 사다리 (주두 켜 수)   일반 1  <  모서리 2 · 입구 옆 2  <  중앙(문설주·문두·어깨·관)
```

## 실측 (REF 카메라)

```
문두 꺾임        2/2      문두 가운데 반블록  5/5     내부 문틀 어깨  2/2
공포 3단(신발)   6/6      모서리 두 축        4/4
귀마루 끝 ↔ 모서리 적주   한 칸 어긋남 (귀마루가 k=1 에서 시작하므로 설계값)
지붕 상자        층간 37×18 · 대지붕 33×14   <b>불변</b>
```

## ★자가 또 틀렸다 — 이번엔 <b>재료로 부재를 끊고</b> 있었다

주두 두께를 세는 자가 「`dark_oak` 이 나오면 도리」로 끊었다. 모서리 주두의 둘째 켜가
어두운 목재라 **주두를 도리로 세어** 1 로 읽혔다. → **자리로 끊는다**: 맨 위 두 켜가
도리·긴 보이고 그 사이가 주두다. (같은 병의 네 번째다 — 이름·재료가 아니라 **구조**로 재라.)

## 아직 안 한 것 (다음 회차 거리)

- **추녀 꼬리** — roof bbox 안에서 C 아래 2칸. 지붕 코드에 손이 더 들어간다.
- **기단 중앙축 정렬** — 중앙 몰딩을 문 3칸 축에 맞춰 끊기.
- 창은 의도적으로 안 건드렸다 (변수 섞임 방지 — 사용자).

---

# REF-3B 뒤 · Codex 독립 검토와 프로파일 분리 (2026-08-10)

사용자 지시로 Codex 를 **독립 검토자**로 띄웠다 (구현 금지 · 판단만).
※메모리엔 「Codex 협업 종료(2026-07-17)」가 남아 있으나 이번 지시가 우선한다.

## Codex 가 짚은 것 중 **실제 위험이었던 것 — Q3**

> 「공포를 지붕 뒤에 놓은 것이 **공통 조성 순서**를 바꿨다면 회귀 위험이 있다.
>  산문·강당·행각은 기준 커밋 대비 좌표→blockstate diff 가 0 인지 재는 것이 가장 강한 검사다.」

**맞았다.** `bracket` 을 쓰는 도면은 본전과 **강당** 둘뿐인데(나머지는 `none` 이라 무영향),
강당이 본전 회차를 따라 조용히 바뀌고 있었다. 실측 — 강당 남쪽 처마 켜:

```
고치기 전   L L S L P L S L P L P L P L S L P L S L      ← S = 공포가 서까래를 끊었다
고친 뒤     L L P L P L P L P L P L P L P L P L P L      ← 서까래가 온전하다
```

★**D2 로 이미 승인된 건물이 본전 회차마다 움직이면 무엇이 승인된 것인지 알 수 없다.**
사용자가 REF-2.5 때 미리 못박은 원칙 그대로다 — 「본전에서 만든 실제 수치를 모든 곳에 복붙하지 않는다」.

## 고침 — `bracket_shape` 프로파일

```yaml
bracket_shape: contour   # 본전만. 계단 윤곽 · 두 축 모서리 · 서까래 신발
                         # 안 적으면 flat = 옛 동작 (판재 더미 · 서까래 켜는 서까래가 갖는다)
```
실측 확인 — 본전 처마 켜에는 적주마다 `T`(서까래 신발)가 박히고, 강당은 `L·P`(서까래)만 남는다.

```
본전  L P L T L P T P L T L P T P L T L P L P L T L P T P L T L P T P L T L P L
강당  L L P L P L P L P L P L P L P L P L P L
```

## Codex 의 나머지 답 (미착수 · 다음 회차 거리)

- **Q1 (A 가 중앙으로 뻗기)** — 내 「불가」 판정을 **반박했다.** 같은 평면에서만 불가이고,
  깊이로 한 칸 비킨 뒤 중앙 쪽으로 꺾는 **2칸 대각 공포** `(A+n) → (A+n+t)` 는 J 와 안 부딪친다.
  끝 블록을 `toward_center_axis` 계단으로 두면 방향을 형태가 증명한다. → **타당해 보인다. 다음 회차.**
- **Q2 (추녀 꼬리)** — **지붕의 실현 외곽 링** 좌표계에 붙여라. 몸체 모서리에 붙이면 처마 돌출이
  바뀔 때 연쇄 회귀한다. 하층 37×18 과 대지붕 33×14 는 **각각 독립 링**. 배치 전 실현 bbox 안인지
  assert 하고 silent clamp 금지. → **다음 회차.**
- **Q4 (형태냐 스케일이냐)** — 투영 스케일과 정규화 실루엣을 **분리**해서 재라.
  스케일 = 같은 원경 카메라에서 건물별 **투영 bbox 면적/입체각 비**.
  형태 = 투영 크기를 **같게 정규화**한 뒤 층간 분리·처마 굴곡·귀솟음/용마루 극점의 잔존 수.
  **정규화하면 강한데 실제 원경에서 약하면 스케일 문제, 정규화 후에도 약하면 형태 문제.**
  → 확대 판단을 육안이 아니라 **수치로** 가를 수 있다. 채택할 만하다.

## REF-3B-Q1 — 입구 옆이 중앙으로 뻗는다 (Codex 반박 채택 · 2026-08-10)

내 「A 는 중앙으로 뻗을 칸이 없다」는 판정이 **틀렸다.** Codex 지적대로 그것은
**같은 평면에서만** 참이었다. 문설주는 **적주 평면(+1)** 에 있으므로,
**처마 평면(+eave=2)** 으로 한 칸 비킨 뒤 중앙 쪽으로 꺾으면 부딪치지 않는다.

```
       적주 평면 +1   ← 문설주(J)가 여기 있다
       처마 평면 +2   ← 대각이 여기로 간다   A ──╮
                                                ╰─→ 축
```

★**방향은 좌표에 안 박는다** — `toward = -signum(axisCol − c)` 로 **축을 향해** 계산한다.
도면이 회전해도 산다 (「방향은 자리가 정한다」 그대로).

★★**역할 글자는 도면이 선언한다** (`meta.bay_roles: {corner: C, entrance_adjacent: A}`).
코드가 `'A'` 를 알면 그것은 **「이름으로 구조를 고르는」 옛 병**으로 되돌아가는 길이다
(이 저장소에서 네 번 났다). 코드는 **역할**만 알고 글자는 모른다.

실측: 서쪽 A → x−1 · 동쪽 A → x+3 에 `mangrove_stairs` **좌우 대칭 ○** ·
문설주 평면(z54)과 **안 겹침** ○ · 눈 465 → **468/468**.

## REF-3B-Q2 — 추녀 꼬리 (Codex 권고 채택 · 2026-08-10)

지붕을 키우지 않고 **모서리 끝 윤곽만** 세운다. 수평으로 뚝 끊긴 처마가 아니라,
네 귀에서 **부재가 한 번 더 내려앉는** 모습이 아래·옆에서 보여야 한다.

★**어느 좌표계에 붙이는가가 회귀를 가른다** (Codex Q2). 몸체 모서리에 붙이면 처마 돌출이
바뀔 때마다 연쇄로 어긋난다 → **지붕의 실현 외곽 링** `(x0,z0)-(x1,z1)` 에 붙인다.
층간(37×18)과 대지붕(33×14)은 **각각 제 링**을 쓴다.

```
꼬리 머리   처마 모서리 바로 아래 (cy−1)          dark_oak_stairs
꼬리 끝     한 칸 안쪽 대각 · 한 켜 아래 (cy−2)   dark_oak_slab
```
★**추가 돌출 0** — 두 칸 모두 실현 상자 **안**이다. 밖이면 `put()` 이 던진다 (silent clamp 없음).

## ★모서리는 서까래가 아니라 **추녀의 자리**다

처음엔 층간 꼬리가 **0/4** 였다. 서까래가 나중에 놓여 **꼬리 머리를 덮었다.**
모서리를 추녀에게 넘겨 고쳤다 — 다만 그 양보는 **추녀를 놓는 지붕만** 한다.
안 그러면 강당처럼 추녀가 없는 건물의 **모서리 서까래가 사라진다**
(프로파일 분리에서 배운 것을 그대로 적용).

## 실측

```
층간 추녀 꼬리    4/4      대지붕 추녀 꼬리   4/4      합계 8/8
강당 모서리 서까래  L (살아 있다 — 양보 안 함)
지붕 상자   층간 37×18 · 대지붕 33×14   <b>불변</b>
```
눈 468 → **473/473**.

---

# Q4 — 「형태냐 스케일이냐」를 수치로 가른다 (2026-08-10 · Codex 공동 판정)

사용자 지시로 **매 판단을 Codex와 함께** 하기로 했다. 아래는 두 번의 검토를 거친 결과다.

## Codex 가 고친 내 설계 (2회차)

| 내 초안 | Codex 지적 | 반영 |
|---|---|---|
| 가로·세로 따로 정규화 | 그러면 **폭:높이 비가 사라진다** | **등방 정규화** (한 스칼라) |
| 1D 상단선 | 하층 처마·층간 분리를 놓친다 | **2D 실루엣 + w(y)** |
| 극대 수·단조 구간 | 블록 잡음에 민감, 많다고 좋은 값 아님 | **평활 뒤 prominence** |
| 비교자 미정 | 캠퍼스 평균은 역할·거리가 섞임 · AI 레퍼런스는 절대 크기 기준 불가 | **같은 단(B5)의 망루** |
| 문턱을 눈에? | 한 장면으로 「충분함」을 정하면 회벽 비율과 같은 자기정당화 | **도구는 수치만 · 눈은 계측기 검증만** |

## ★내가 스스로 잡은 것 · Codex 가 잡은 것

- **내가**: 처음엔 지붕을 **통짜 상자**로 뒀다. 그러면 실루엣이 실물이 아니라 **모형을 잰다.**
  조성과 같은 식(`grandRise`)으로 켜마다 상자를 내게 고치자 면적 166,581→144,220 ·
  어깨 21→10 으로 바뀌었다 — **오류가 지표를 부풀리고 있었다.**
- **Codex 가**: 잔존이 `[10, 9, 10, 14]` 로 **끝에서 되레 늘었다** — 흐릴수록 덜 보여야 하는데
  더 보였다. 다운샘플 뒤 표본이 짧아져 창이 상대적으로 넓어진 탓. **고정 길이로 되맞춰** 고쳤다
  → `[10, 9, 8, 5]` **단조 감소**.
- **Codex 가**: 망루를 손으로 그린 한 상자로 둔 것 — 망루가 바뀌어도 계측기는
  **아주 결정론적으로 틀린 채** 남는다. **실측 실루엣**에서 세우게 고쳤다.

## 결과

```
스케일     본전 144,220  ·  망루 73,005   →  <b>1.98배</b>
확대 효과량  31폭 1.00 → 35폭 1.12 → 37폭 1.18   (망루 대비 1.98 → 2.21 → 2.32)
형태       종횡비 1.44 · 어깨 10 · 세기 0.54 · 잔존 [10, 9, 8, 5]
           견줌: 망루 어깨 0 · 잔존 [0,0,0,0]
제3 원인   실루엣/외접상자 0.73
```

## 공동 판정

**스케일 문제가 아니다.** 본전은 이미 이웃(망루)의 **1.98배**로 투영되고, 패드를 통째로 옮기는
큰 회차를 치러도 **1.12~1.18배**밖에 안 커진다 — **효과량이 대가에 못 미친다.**
**형태 정보도 있다.** 정규화 뒤에도 어깨 10개가 남고 1/8로 흐려도 5개가 남는다
(망루는 0개 — 대조군이 제대로 작동한다).

→ 원경 체감이 약하다면 원인은 **제3 원인(가림·배경 대비)** 이다.
**Codex 최종 권고**: 「B5 이주는 열어 두되 다음 작업으로 선택하지 말고,
고정 카메라에서 **배경·명도 대비 회차**로 가라.」

## 계측기의 눈 — 14/14

미학 문턱이 아니라 **계측기**를 잰다: 결정론 · 집합 불변(순서·중복·내부 상자) ·
투영 오라클(멀어지면 준다) · 어깨 합성 시험(직사각형 0 · 계단 ≥1 · ±1 잡음은 0) ·
**잔존 단조 감소** · 확대 민감도(면적과 **투영 폭** 둘 다) · 음성 대조군 · 라벨↔실물 일치.

★음성 대조군의 자도 한 번 고쳤다: 「±2 화소 안」으로 쟀더니 원근에서 위로 옮기기만 해도
폭이 조금 변해(+0.6%) 짖었다. 절대 화소는 자가 아니다 —
물어야 할 것은 **진짜 확대(+19%)에 견줘 무시할 만한가**다.

---

# 배경 대비 회차 — 계측기를 두 번 갈아엎었다 (2026-08-10)

## ★실패 하나를 먼저 적는다 — 해석 투영은 못 썼다

`tools/ref_scale_shape.py` 의 실루엣을 **실제 렌더에 겹쳐 보니 빨간 선이 건물이 아니라
절벽을 지났다.** FOV·피치·요를 한 장에 맞추려다 발산했고, 설령 맞췄어도 그것은 계측기가 아니라
**곡선 맞추기**다 — Codex 경고 그대로 「계측기는 아주 결정론적으로 틀릴 수 있다」.

★그래서 **카메라를 흉내 내지 않기로** 했다. 게임에게 직접 묻는다:

```
① 본전이 있는 컷을 찍는다
② 본전을 지우고(fill air) 같은 자리에서 다시 찍는다
③ 화소로 뺀다 → 달라진 화소가 곧 실루엣
```
투영식이 필요 없고 오차가 0 이다.

## ★그 방법도 두 번 물렸다

1. **구름이 움직였다.** 두 컷 사이에 하늘이 흘러 마스크에 섞였고, 조밀 구간이 `y0` 부터
   잡혔다 (상단 대역이 「건물 178 · 배경 180」 이라는 헛것을 냈다).
   → 건물은 **한 덩어리**, 구름은 흩어진 줄무늬 ⇒ **가장 큰 연결 덩어리**만 남긴다.
2. 그 전에는 성긴 줄이 얇은 대역을 흔들었다 → 조밀 줄만 센다.

고친 뒤 마스크는 **127,173 화소 · y 254~669** — 사진의 본전과 정확히 맞는다.

## 결과

```
건물 명도 75.7  ·  그 자리 배경 117.2  →  차 41.5 / 255  ·  <b>Michelson 0.215</b>
높이대별 (위→아래)  57.2 · 50.2 · 41.4 · 37.8 · 21.6
```

## 판정 — 배경 대비도 <b>범인이 아니다</b>

지붕이 절벽에 묻히는 것이 아니다: 위쪽 대역이 **57·50** 으로 가장 잘 갈린다
(어두운 기와 ↔ 중간 회색 절벽). 가장 약한 곳은 **밑동(21.6)** — 밝은 기단이 밝은 마당에 붙는 자리다.

그래서 세 원인을 다 재 보면:

| 원인 | 수치 | 판정 |
|---|---|---|
| 스케일 | 망루의 1.98배 · 확대해도 1.12~1.18배 | **아니다** |
| 형태 | 어깨 10 · 잔존 [10, 9, 8, 5] (망루 0) | **아니다** |
| 배경 대비 | Michelson 0.215 · 지붕 대역 57 | **아니다** |

★**세 원인 모두 아니라면, 남은 것은 「판정 자체가 낡았다」는 가능성이다.**
마지막 원경 판정은 **REF-3A** 때 것이고, 그 뒤로 기와 채택·용마루 가시화·추녀 꼬리·
두 축 모서리·대각 공포가 들어갔다. **지금 화면으로 다시 판정해야 한다.**

---

# 툇간 — 「의도 없는 블록은 없다」 (2026-08-10 · 사용자 지적)

> 「건축에선 이유 없는 건축이 없다. 이 기둥이 왜 저기 있는지 판단해야 하는데,
>  지금은 사진 보고 **의도 없이 따라 하는** 것 같다.」
> 「앞으로 튀어나온 기둥이 **벽과 붙어 있어** 튀어나옴은 보이지만 형태가 명확하지 않다.
>  오히려 **한 칸 띄워져 있는 게** 낫지 않나.」

## 레퍼런스를 크게 놓고 다시 읽었다 — 무엇이 왜 있는가

`화산파/…07_09_25 (4)` 좌하단 근경에서 읽어 낸 것:

1. **적주는 벽에 붙어 있지 않다.** 돌바닥 위에 <b>홀로</b> 서고, 뒤로 어두운 틈이 있으며
   회벽·살창은 <b>한 겹 더 물러나</b> 있다 → **자립 열주 + 물러난 벽 = 툇간**.
2. **하중의 사슬이 보인다**: 기둥 → 창방 → 공포 → 처마. 각 부재가 **위를 받으려고** 있다.
3. **중앙은 기둥이 겹으로 선다.** 개구가 벽을 약하게 만드니 **거기에 구조를 더 넣는다** —
   장식이 아니라 그것이 중앙이 중앙으로 읽히는 까닭이다.
4. 기단 가장자리에 **석재 난간**이 돈다 — 사람이 걷는 자리라 가장자리가 분절된다.
5. 살창은 기둥보다 **더 뒤**에 있다 — 기둥 / 벽 / 살창 **세 평면**.

## ★Codex 가 내 첫 안을 막았다

내 첫 안은 「적주를 +1 → **+2** 로 내보낸다」였다. Codex 반론:

> **`+2` 는 지붕 동결과 모순이다.** 처마가 2 이므로 기둥을 처마 끝까지 내보내면
> **공포가 밖으로 나갈 자리가 없어진다** (`eave − post ≥ 1` 이 깨진다).

맞는 지적이다. → **기둥을 내보내지 않고 벽을 물린다.**

```
적주   +1        처마(2) 안이라 공포가 밖으로 1 나갈 자리가 남는다
툇간    0        <b>빈다</b> — 여기가 사람이 걷는 자리이자 그늘이다
회벽   −1
살창   −2        ← 세 평면
```
`post − wall = 2` — **사이에 실제 빈 칸 하나.** 이것이 툇간의 정의다.

## 실측 (y61 단면)

```
        z54(적주)              z53(툇간)   z52(회벽)     z51(살창)
회벽칸   —                     빔          bone_block    —
격자칸   —                     빔          —             dark_oak
적주칸   stripped_mangrove_log 빔          —             —
툇간 바닥  polished_andesite / smooth_stone (딛는 자리)
```

## 자를 셋 고쳤다

1. **도면의 깊이 계약**이 `−1..+1` 로 **양쪽을 같게** 묶고 있었다. 그 자의 까닭은
   「더 나오면 벽이 아니라 다른 건물이 된다」 — **바깥** 이야기다. 안으로 물리는 것은
   건물을 키우지 않는다. → **바깥은 +1 그대로, 안쪽만 −2 까지.**
2. **덧댐 배경**이 「깊이 0 이면서 회벽인 글자」를 찾고 있었다. 툇간에서는 기둥 뒤가
   **벽이 아니라 바닥**이다 → 도면이 `overlay_backing: "F"` 로 선언한다.
3. **「기준면에 구멍이 없다」** 눈이 이제 틀렸다. 툇간은 **일부러 비운다** →
   닫혀야 하는 것은 **물러난 벽 평면**이다. 거기서 묻게 바꿨다.

## Codex 가 준 좌표 계약 (눈에 넣었다)

```
post − wall ≥ 2          사이에 실제 빈 칸
lattice < wall < post    세 평면
eave − post ≥ 1          공포가 밖으로 나갈 자리
```
눈 473 → **476/476**.

## 기단 난간 · 겹기둥 결속 (2026-08-10 · Codex 검토 6회차)

### 난간 — 「사람이 걷는 자리라 가장자리가 분절된다」

장식이 아니라 **낙차가 있는 가장자리**에 서는 부재다.

★**어느 단인가** (Codex): **`M`** 이다. M 바깥선이 월대와 마당 사이의 **실제 외곽 경계**다.
`N` 바깥선은 아래 `M` 으로 이어지는 **내부 단차**라, 거기 난간을 두면 **월대 횡단**을 가른다.

```
R  ["smooth_stone", "stone_brick_wall"]          M 의 바깥 테두리
E  ["smooth_stone", "stone_brick_wall",
    "polished_andesite_slab"]                    엄지기둥 — 난간이 끊기는 두 끝
중앙 col 17~21 (문 3 + 문설주 2)                 <b>끊는다</b> — 계단이 올라올 자리 · 축선
```
실측: 앞 테두리 **30/35칸** (중앙 5칸이 정확히 비었다) · 엄지기둥 **2/2** ·
옆 테두리 3/3 · 뒤 테두리 3/3.

### 겹기둥 — 새로 만들지 않는다

Codex: 「새 겹기둥은 필요 없다. `A16+J17` · `J21+A22` 가 **이미 정확한 구조적 쌍**이다.
**재료는 맞추지 말고** 밑동·머리 높이와 **주두 윤곽**을 맞춰라. J 의 암색은 유지한다.」

그대로 했다 — J 의 주두를 A 와 같은 **계단 + 반블록 두 켜**로 바꾸되 **어두운 참나무**로 남겼다.
붉게 만들면 7호 실측의 **3칸 주기**가 다시 깨진다 (이미 한 번 물렸다).

## ★★자가 <b>다섯 번째</b>로 틀렸다 — 이번에도 재료 이름

주두 두께를 세는 자가 몸통을 「`mangrove_log` 인가」로 찾고 있었다.
문설주 J 의 몸통은 **어두운 참나무**라 못 봤고, 주두를 **0** 으로 셌다.
→ 「**세로로 이어진 통나무**」로 고쳤다 (재료 무관).

```
① 공포 판정   「mangrove_log 포함」  → 인방까지 적주로
② 문설주 판정 「mangrove_log 포함」  → 팔레트 바뀌자 0
③ 주기 판정   「글자가 P 인가」      → 역할 나누자 6
④ 주두 판정   「dark_oak 이면 도리」 → 모서리 주두를 도리로
⑤ 주두 판정   「mangrove_log 인가」  → 참나무 문설주를 못 봄   ← 이번
```
그리고 **모서리 역할 눈**도 한 번 더 물렸다 — 난간(`R`)·엄지기둥(`E`)이 생기자 그것들을
**몸체의 끝**으로 세었다. 난간은 **기단** 부재지 몸체가 아니다.

눈 476 → **482/482**.

---

# bay 모듈 A/B — 「크기가 부족한가」에 대한 답 (2026-08-10)

사용자: 「건물의 크기가 부족해 **표현의 한계**로 보이는데 맞을까요?」

## 두 질문을 갈랐다

- **원경에서 왜소해 보이는가** → 이미 쟀고 **크기가 아니었다** (망루의 1.98배 · 확대해도 1.18배)
- **부재를 표현할 자리가 없는가** → 이건 **총 크기가 아니라 모듈**이다:

```
주기 3 = 기둥 1 + 칸 2   →   한 칸의 폭이 <b>2칸</b>
그 2칸에 창턱·창틀·살창·상인방을 다 넣어야 한다  →  살창은 결국 1칸
```
★**총 크기를 키워도 주기가 3이면 칸은 여전히 2칸이다** — 패드 확장은 이 문제를 **안 푼다.**

폭을 안 늘리고도 칸을 넓히는 길이 있었다: **폭 31 고정 · 주기 4 → 정면 7칸** (폭이 정확히 31).

## ★Codex 권고대로 전면을 안 갈아엎고 <b>한쪽만</b> 시험했다

> 「`P-L-K-R-P` 한 bay 를 독립 시험체로 만들고 같은 카메라에서 A/B 하라.
>  읽히면 이행할 근거가 생긴다. **읽히지 않으면 문제는 가로폭이 아니라 세로 높이나 블록 형상**이다.」

정면 **서쪽 절반만** 주기 4 (3칸 창호 조 = 좌짝·문선·우짝), 동쪽은 그대로 두어
**같은 사진 안에서** 견줬다.

## 결과 — <b>실패</b>. 넓힌 쪽이 더 나쁘다

```
동쪽 (현재 · 주기 3)   붉은 적주 | 흰 회벽 | 살창   — <b>또렷하다</b>
서쪽 (시험 · 주기 4)   갈색 덩어리                  — <b>뭉갠다</b>
```
까닭 둘:
1. 창호 조가 **회벽을 밀어냈다.** 대비를 만들던 <b>밝은 흰 면</b>이 사라졌다.
2. 벽이 **5켜뿐**이라 창턱·하부판·살창·중방·상인방을 넣으면 살창이 **2켜**로 줄어든다.
   부재는 늘었는데 **각각이 읽히지 않는다.**

## ★그래서 진짜 병목은 <b>세로 높이</b>다 (Codex 가 미리 말한 그것)

Codex: 「주기 4는 **가로 문제만** 푼다. 하부판·중방·살창·상인방을 각각 읽히게 하려면
**세로 스택도 재배분**해야 한다 — 최소 6켜의 입면 문법이 필요하다.」

우리 하층 벽은 **5켜**다 (인방 + 회벽 3 + 인방). 6켜가 필요하다면 **벽 높이를 올려야** 하고,
그건 base_y·상층·지붕 표고가 다 따라 오르는 회차다.

**결론: 주기 4로 가지 않는다. 시험체는 되돌렸다** (도면 무변경 · 눈 482/482).
다음에 손댈 곳이 있다면 가로가 아니라 **하층 벽 높이**다.

---

# B안 — 하층 벽 5 → 6켜 (2026-08-10)

bay 모듈 A/B 가 낸 답: **가로가 아니라 세로가 병목이다.**
Codex 의 입면 문법대로 창호를 층으로 나눌 자리를 만들었다.

```
전   벽 5켜   인방 · 회벽 3 · 인방            창은 살창 3켜뿐
후   벽 6켜   창턱 · 하부판 · 중방 · 살창 2 · 상인방
```

★모든 기둥 처방이 **12켜**로 같아진다 (지붕이 앉는 높이는 하나여야 한다) ·
`base_y` 11 → **12** · 마감 조각(현판 자리·관·문두)도 한 켜씩 올랐다 ·
중앙 개구는 5 → **6켜**.

| | 전 | 후 |
|---|---|---|
| 하층 벽 | 5 | **6** |
| 기둥 처방 | 11켜 | **12켜** |
| 살창 | 3켜 | **2켜** (대신 창턱·하부판·중방·상인방이 생겼다) |
| 회벽 | 3켜 | **4켜** |

## 자를 한 번 더 넓혔다

「인방·창방은 적갈 · 도리·긴보는 짙은 갈」 눈이 **하부판**을 어긋남으로 셌다.
하부판은 가로보가 아니라 **창호의 일부**라 `dark_oak` 계열이 맞다 (창살·창틀과 같은 무리).
→ **창호 조 안은 이 자가 안 잰다** (재려면 「창호 안인가」를 먼저 물어야 한다).

폭·bay·패드·지붕 상자는 **안 건드렸다.** 눈 **482/482**.

---

# 기둥 재설계 — 「형태가 별로다」 (2026-08-10 · 사용자)

레퍼런스 근경을 크게 놓고 기둥만 다시 읽었다:

```
레퍼런스   주초(밝은 석재) → <b>끊김 없는 붉은 세로선</b> → 어두운 보
           몸통에 <b>옆으로 튀어나오는 켜가 하나도 없다</b>
           창호 부재는 기둥 <b>뒤로</b> 물러나 있다
```

## ★진범 — 기둥에 붙어 있던 「선반」은 <b>공포의 첫 단</b>이었다

실측(`x−11` 세로 단면)이 잡아냈다: `y65 = mangrove_stairs`.
공포의 첫 단이 **적주 평면(+1)** 에 계단으로 앉아, 기둥 머리에서 옆으로 튀어나오고
**몸통의 맨 윗 통나무까지 먹고** 있었다.

→ 첫 단을 **통짜**로 바꿨다. 기둥이 그대로 이어져 보이고, 벌어짐은
**처마 평면에 있는 위 두 단**이 맡는다 (레퍼런스도 그렇다).

## 그 밖에 고친 것 셋

1. **주두를 따로 두지 않는다.** 계단·반블록 주두가 또 다른 선반이었다.
   붉은 몸통이 어두운 보에 바로 닿는다 — 역할 차이는 이제 **공포**가 진다
   (C 는 두 축 · A 는 축을 향한 대각).
2. **가로 부재는 칸을 가로질러 이어진다.** 창턱·상인방을 회벽칸과 격자칸이
   **같은 높이에서 공유**한다. 한 칸에만 두면 **선반**처럼 튀어나온다.
3. **창턱을 통짜로.** 반블록이면 툇간 앞에서 **떠 있는 선반**처럼 보인다 —
   벽의 밑동은 **이어지는 하방**이어야 한다.

## 지금 기둥

```
smooth_stone*2        기단
polished_andesite     주초 — 몸통보다 가늘지 않다
stripped_mangrove_log*7   <b>끊김 없는 붉은 몸통</b>
dark_oak_planks       도리
dark_oak_log          긴 보
                      ↑ 그 위에 공포 (첫 단 통짜 · 위 두 단이 처마 평면에서 벌어진다)
```

## 자를 또 둘 고쳤다

- 「A+J 주두가 **두 켜**여야 한다」 → 그건 그때의 **처방**이지 계약이 아니었다.
  계약은 「A 와 J 가 **같은가**」이지 「몇 켜인가」가 아니다.
- 「모양 있는 켜 ≥5」가 난간(`R`)·엄지기둥(`E`)을 **목록에 안 넣어** 덜 셌다.

눈 **482/482** · 폭·bay·패드·지붕 상자 무접촉.

## 「기둥에 붙은 블럭이 뭔지 이해가 안 감」 — 재서 답했다 (2026-08-10)

추측하지 않고 기둥 한 그루 둘레를 통째로 쟀다 (`x=−11` · z 를 앞에서 뒤로):

```
   y     z55 처마평면   z54 적주평면   z53 툇간   z52 벽
  58        ·          <b>주초석</b>        석재       석재
  59~64     ·          붉은통나무      ·         (회벽·살창)
  <b>65        ·          붉은판  ← 이것이 「붙은 블럭」</b>
  66      짙은반        짙은판(도리)    ·         ·
  67      짙은계단      짙은통나무(긴보) ·         ·
  68      기와반        기와계단       기와       기와
```

**답: 그것은 공포(栱包)의 첫 단이었다.** 적주 평면(+1)에 앉아 있었고,
**게다가 몸통의 맨 윗 통나무를 먹고 있었다** — 도면은 몸통 **7켜**라 적었는데 실물은 **6켜**였다.
**신고가 실물과 다른 상태** (이 저장소가 가장 싫어하는 것).

## 고침 — 첫 단을 <b>몸통과 같은 재료</b>로

```
전   공포 1단 = 다른 재료  →  기둥에 블럭이 붙어 보이고, 신고 7켜 ≠ 실물 6켜
후   공포 1단 = 몸통과 같은 통나무  →  기둥이 보까지 <b>한 줄</b> · 신고 7켜 = 실물 <b>7/7</b>
                                    공포는 <b>처마 평면으로 나온 두 단</b>으로만 보인다
```
레퍼런스도 그렇다 — 공포는 기둥 위 **보 구역에서 앞으로** 나오지, 몸통에 붙지 않는다.

실측: 몸통 **7/7** · 공포 2단(처마평면 y66) ○ · 공포 3단(y67) ○ · 적주평면 y66 = 도리 ○.
눈 **482/482**.

---

# 덮임 감사 — 「신고 = 실물」을 체계로 지킨다 (2026-08-10 · Codex 권고 8회차)

> **Codex**: 「482/482 와 유출 0 은 <b>생성 성공</b>이지 <b>최종 생존·가시성 성공</b>을 보증하지
> 않는다. 좌표 → 작성자·작성순서·블록의 claim map 을 만들고 중복 좌표를 전부 출력하라.
> 부재별 **계획 / 최종 생존 / 덮인 수**를 집계하면 같은 오류가 바로 드러난다.」

공포가 적주 몸통을 먹은 것을 **아무 눈도 못 잡았다.** 사람이 사진을 보고 물어서야 알았다.
그래서 조성이 **좌표마다 누가 썼는지**를 적고, **다른 부재가 덮으면** 센다.

```
전   신고 = 블록 수 하나
후   신고 = <b>생성 · 결합 · 덮임</b> 셋
```
★**결합**은 깊이 문법이 만드는 필연적 겹침이다 (기단 단과 몸체 칸이 같은 바닥 켜를 쓴다 ·
마감 조각이 문설주 위에 앉는다). **덮임**은 도면이 신고한 블록을 **다른 부재가 지운 것**이다.

## 첫 감사가 바로 잡은 것

```
칸:P ← 공포 23 · 칸:C ← 공포 4 · 칸:A ← 공포 2      ← <b>문제</b> (29회)
칸:N ↔ 칸:P/D/C/J · 칸:F ↔ 칸:W/O · 칸:J ← 마감      ← 결합 (의도)
```
공포의 첫 단이 **적주가 이미 선 자리**에 겹쳐 쓰고 있었다. 재료를 같게 해 눈에는 안 보였지만
**도면이 신고한 블록을 다른 부재가 덮는** 상태였다. → **겹치면 아예 안 쓴다. 그 칸은 적주의 것이다.**

## 고친 뒤

```
본전   칸 1216 · 블록 3381 · 결합 202 · <b>덮임 0</b>
강당   칸  187 · 블록  402 · 결합   0 · <b>덮임 0</b>
산문   칸 1320 · 블록 3708 · 결합   0 · <b>덮임 0</b>
```
실측: 몸통 7켜 온전한 적주 **6/6** · 공포 2단 **6/6** · 3단 **6/6** — **신고 = 실물**.
눈 **482/482**.

## Codex 가 남긴 다음 것들 (미착수)

- 검사 축을 **좌표 정확성 / 최종 생존 / 목표 시점 가시성** 세 층으로 나눠라 (지금은 앞의 둘까지)
- 5→6켜 상승 뒤 **처마·난간·개구의 절대 위치**가 함께 이동했는지 **높이 불변식**으로 재검증
- 하층이 원경 판정을 통과하면 다음은 **상층을 같은 문법으로 재작성**

---

# 정면 재작성 — Codex 디자인 판정 (2026-08-10)

사용자: 「여전히 개선 안 됨. 레퍼런스와 다른 점 탐색 및 수정.」
그리고 **「디자인적 평가는 Codex가 전담」**. 역할을 갈랐다 —
**나 = 관측·측정·구현 · Codex = 디자인 평가와 처방.**

## Codex 판정

> **우리는 「회벽이 넓고 창이 작다」, 레퍼런스는 「창이 넓고 회벽이 작다」. 그 반전이 핵심이다.**

```
전   y59 하방 · y60 하부판 · y61 중방 · y62~63 창(2켜) · y64 상인방
후   y59 하방 1켜 · y60~63 <b>창 4켜</b> · y64 <b>회벽 1켜 띠</b>
     하부판·중방 제거 · 창호를 −2 → <b>−1 전진</b>
```
> 「한 차례의 리빌드가 허용된다면 **반드시 9칸 → 7칸까지 함께** 해야 한다.
>  2블록 폭의 칸을 유지한 채 디테일만 늘리는 데에는 명확한 한계가 있다.」

그대로 시행했다:
```
C DDD P DDD P DDD A J OOO J A DDD P DDD P DDD C     정면 <b>7칸</b> · 주기 4
```
벽 6켜 · 기둥 12켜 · 지붕 상자 · 패드 · 기단은 **안 건드렸다.**

## ★내 실수 하나 — 회벽을 이름으로 안 불렀다

새 창호 처방에 `bone_block` 을 **직접 박았다.** 이 저장소의 규약은
「회벽은 `plaster` 라는 **이름 하나**로 부른다 — 도면에 재료를 박으면 갈 때 두 곳을 고쳐야 하고
그 둘이 어긋난다」. 눈이 「회벽 0」으로 잡아 줬다. `plaster` 로 고쳤다.

## 자를 여섯 고쳤다 (계약이 바뀐 것 · 헐거워진 것)

| 자 | 왜 |
|---|---|
| 인방 「붉어야 한다」 | 창 위가 **회벽 띠**가 됐다 → 뜻은 「채움이 통짜로 안 이어진다」 |
| 세 평면 | 창호가 벽 평면으로 **전진**했다 → 「창호가 적주보다 뒤」로 |
| 적주 수 「10」 | 7칸이 되며 8 → **수를 박지 않는다** |
| 주기 「3」 | 7호 실측값이고 superseded → **도면이 신고**하고 실물과 대조 |
| 공포 앵커 「≥6」 | Codex: 헐겁다 → 정면 적주 역할 칸 수와 **정확히** 같아야 |
| 망루 흰 면 ≤ 본전 | Codex: **블록 수는 못 쓰는 대용품** → **은퇴**. 후계자는 화면에서 재는 것 |

## Codex 가 미리 재라고 한 것 (네 장)

원경 40~60 · 중경 15~25 · **45도 중경** · 근경 4~8 — 같은 시간·광원·팩으로.
그리고 블록 수가 아니라 **화면의 최대 밝은 연결 성분 · 밝은 화소 비율 ·
중앙 대문과 측면 창의 명도차**를 재면 D-35 의 진짜 후계자가 된다.

★Codex 경고 셋 (아직 안 잼):
1. `DDD`도 3칸, `OOO`도 3칸이라 **창과 중앙 대문이 경쟁**할 수 있다
2. **정면만 줄었다** — 회벽 켜 정면 18 · 측면 20 · 후면 38 → 45도에서 측면이 더 희게 보일 수 있다
3. 공포 리듬이 10 → 8 로 성겨졌다

## ★남은 덮임 하나

```
칸:D ← 칸:W 21     (뒤·옆면이 만나는 모서리에서 창이 벽에 덮인다)
```
창호를 −1 로 전진시키며 네 면의 물러난 평면이 **모서리에서 만난다.**
정면에는 안 보이지만 **신고 ≠ 실물**이므로 다음 회차에 닫는다.
