# `plasterHall` 계열 전수 조사 — 아직 도면이 없는 구역

> **2026-08-07.** 02~101 구간을 정리·동결한 뒤의 첫 걸음 (사용자 확정 순서).
> 여기부터는 옛 코드 교체가 아니라 **각 건물 유형 자체를 새로 도면화**하는 단계다.
>
> 세는 자: `HwasanCampusBuilder` 의 실제 호출부 + `structureBoxes` 가 **마른 조성**으로 낸 발자국.
> ★신고가 아니라 **센 것**이다 — 눈이 호출부 수를 이 표와 대조한다.

## 1. `plasterHall` 호출 — **0곳** ★전부 도면으로 갔다 (2026-08-07~08)

`plasterHall(cx, cz, hf, hl, windows, red)` · 몸체 = `(2hf+1) × (2hl+1)` · 벽 높이 5 · 모임지붕

| 구역 | 이름 | 패드 | 표고 | 몸체 | 발자국(로컬) | 창 | 비고 |
|---|---|---|---:|---|---|:-:|---|
| ~~4~~ | ~~강당·무기고~~ | 40×30 | h80 | 17×11 | — | — | ★**도면으로 옮겼다** (`hwasan_hall.yml` · 모임 유형 1호) |
| 5 | 생활 하 | 36×32 | h54 | **11×9** ×2 | `[7,11] 17×15` · `[21,12] 15×13` | ✓ | 두 채 · 서편은 계단 2→5 통로를 비킨다 |
| 8 | 생활 중 | 32×28 | h92 | **9×5** | `[9,9] 15×11` | ✓ | 한 채 · 가장 작다 |
| 17 | 물자 창고 | 34×22 | h42 | **15×11** | `[7,5] 21×17` | ✗ | + 상자 더미 · 창이 없다 |
| 12 | 장로회 | 44×24 | h128 | **17×7** | `[12,13] 21×11` | ✓ | 깊이가 얕다 (7) |
| 13 | 정상 암자 | 26×18 | h148 | **9×7** | `[1,5] 15×13` | ✗ | + 옛 정자 1 (아래 2절) |
| 20 | 부속 암자 | 18×14 | h148 | **9×7** | `[3,1] 15×13` | ✓ | 패드가 가장 작다 · 다리 어귀를 비킨다 |

- **쓰임이 셋으로 갈린다**: 모임(강당·장로회) · 거처(생활 하·중·암자) · 저장(창고).
  같은 `plasterHall` 이 셋을 다 맡고 있어서 **지금은 위계도 표정도 없다.**
- 창 유무만 다르고 나머지가 같다 — 도면화의 첫 일은 **유형을 가르는 것**이다.

## 2. 아직 남은 옛 `pavilion()` — **1곳** (정원 10 만 남았다 — 연못·매화와 한 벌)

| 구역 | 이름 | 자리 | 비고 |
|---|---|---|---|
| 13 | 정상 암자 | `cx+5, cz+2` half 3 | 암자 도면화와 함께 |
| 19 | 절벽 전망대 | `cx+1, cz` half 2 | **정자 도면(`hwasan_pavilion.yml`)을 그대로 쓸 후보** |
| 10 | 장문인 정원 | `cx+6, cz+6` half 2 | 연못·매화와 한 벌이라 정원째 볼 일 |

★외원·중정 정자는 이미 도면이다. 이 셋이 남아 **같은 역할이 두 언어로 공존**한다 —
19 는 계약대로면 `pad: 19` 인스턴스 하나로 끝난다 (패드 14×12 · 발자국 11×11 이라 실측 먼저).

## 3. 그 밖의 통짜 부품 (도면화 대상 아님 · 참고)

`gateGrand`(산문 1 · 종문 6) · `gateEW`(측문 16) · `dummyRow`·`rack`(연무장 3·14 · 훈련장 7) ·
`chests`(창고 17) · `gardenPond`·`gardenPlum`(정원 10) · `watchtower`(망루 11 — **사모로 전환 완료**)

## 4. 다음 순서 (사용자 확정)

```
강당(4) → 생활(5·8) → 창고(17) → 장로회(12) → 정상 암자(13)
```

각 회차는 계약(`contract.md`) 의 「새 구역을 세울 때의 순서」를 따른다 —
**패드를 훑는 것부터**, 몸체·지붕을 줄이는 것은 마지막.
