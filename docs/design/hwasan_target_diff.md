# 목표 사진과의 차이 목록 — 이것만 지운다 (2026-08-04 확정)

> ★★★**사용자 확정**: 목표는 하나다 — **`화산파/ChatGPT Image 2026년 8월 2일 오후 07_09_24 (1).png`
> (1호 · 산문 접근로)와 동일하게 만든다.** 앞으로 모든 회차는 **이 한 장과의 차이만** 지운다.
> 진행 방식: 차이를 목록으로 만들고 **하나씩 지우며 「몇 번을 지웠다」로 보고**한다.
>
> ★이 문서가 서기 전의 회차들이 헤맨 이유: 목표를 사진 한 장에 고정하지 않고 매번 다른 것을
> 고쳤다 (산 표면 → 이음매 → 처마 …). 무엇을 향하는지 없으면 개선은 표류한다.

## 목표 사진(1호)이 담은 것 — 장면의 정의

대계단을 다 올라 **산문 앞에 선 순간**. 화면 안에 있는 것:
계단(아래에서 위로) · 계단 양옆 깃대와 석등 · 좌우 매화 두 그루 · 잔디 비탈 ·
산문 문루(중앙) · 좌우로 이어진 성벽과 곁채 · 문 너머 위쪽 단의 전각들 · 왼쪽 절벽.

---

## 차이 목록 (D-01 ~ )

각 항목: **[상태] 번호 · 무엇이 다른가 — 목표 / 우리 것**
상태: `열림`(아직) · `진행` · `지움`(해결) · `보류`(사용자 판단 대기)

### A. 계단과 접근로

- `열림` **D-01 · 계단 양옆의 깃대(당간)** — 목표: 계단 난간을 따라 **검은 깃발 단 기둥이 6개 이상**
  일정 간격으로 늘어서 시선을 위로 이끈다. 우리: 없음 (석등만).
- `열림` **D-02 · 석등의 모양과 밀도** — 목표: 기둥 위에 **네모난 등롱**이 얹힌 형태, 계단 좌우로
  촘촘. 우리: 담장 기둥 위 랜턴 — 형태가 다르고 성기다.
- `지움` **D-03 · 계단 폭과 층참** — 목표: 계단이 **넓고 완만하며 중간에 넓은 참**이 있다.
  우리: 좁고 가파르며 참이 작다. (실측 필요)
- `열림` **D-04 · 계단 옆 난간 벽** — 목표: 계단 양옆에 **낮은 돌 난간벽**이 계단을 감싸고 오른다.
  우리: 여장(성곽 이빨)이라 결이 다르다.

### B. 산문 문루

- `열림` **D-05 · 문루가 성벽에 박혀 있다** — 목표: 문루가 **좌우 성벽과 한 몸**으로 이어지고,
  성벽 위로 지붕만 솟는다. 우리: 문루가 독립 건물로 서 있고 성벽과 따로 논다.
- `열림` **D-06 · 문 앞 처마 그늘** — 목표: 문 위 처마가 깊어 **문간이 어둡게 그늘진다**.
  우리: 그늘이 얕다.
- `열림` **D-07 · 붉은 기둥 열의 밀도** — 목표: 문루 정면에 **적주가 촘촘히**(간격 2~3) 서고
  기둥 사이가 좁다. 우리: 간격이 넓어 벽이 더 보인다.
- `열림` **D-08 · 문루 좌우 곁채** — 목표: 문루 양옆에 **낮은 곁채**가 붙어 처마가 층층이 겹친다.
  우리: 문루 하나만 있다.

### C. 성벽과 담

- `열림` **D-09 · 성벽 위 회랑 지붕** — 목표: 성벽 위에 **기와 지붕을 인 회랑**이 달린다.
  우리: 성벽 위가 비어 있다(여장만).
- `열림` **D-10 · 성벽 높이와 두께** — 목표: 성벽이 사람 키의 3~4배로 낮고 두껍다.
  우리: 훨씬 높다. (실측 필요)

### D. 식생과 지면

- `열림` **D-11 · 매화 두 그루의 위치와 크기** — 목표: 계단 좌우에 **크고 둥근 매화**가 각 한 그루,
  수관이 화면의 1/6을 차지한다. 우리: 작고 여럿.
- `열림` **D-12 · 잔디 비탈** — 목표: 계단 양옆이 **잔디 덮인 흙 비탈**이고 관목이 점점이.
  우리: 돌·자갈이 우세하다.
- `열림` **D-13 · 계단 곁 소나무** — 목표: 잔디 비탈에 **가는 소나무**가 서너 그루 섞인다.
  우리: 있으나 형태가 다르다.

### E. 색과 재료

- `열림` **D-14 · 석재의 색** — 목표: 계단·성벽이 **밝은 회백**(거의 흰색에 가깝다).
  우리: 중간 회색으로 어둡다.
- `열림` **D-15 · 기와의 색** — 목표: 지붕이 **짙은 청회색**(순검정이 아니다).
  우리: 거의 검정.

---

## 새 차이 — 1호 부분 확대 분석 (2026-08-05)

목표 사진을 세 구역으로 크롭·확대해 실측했다 (계단 하단 · 문루 중앙 · 성벽·곁채 연결부).
각 항목에 **칸 수**를 붙인다.

### 계단 주변 (확대 A)

- `지움` **D-20 · 축선 시야 회랑** — 「회백 기둥이 문루 정면을 세로로 가로지른다」의 범인은
  **비석**이었다. 옛 자리 ±7 은 통행을 안 막아 통로 눈이 조용했으나, 계단 아래 축선에 선
  눈에는 **가까운 5칸 비석이 멀리 있는 21칸 문루를 가린다**. → `AXIS_CLEAR = 13` 신설,
  비석을 그 밖으로. ★계율: **통행의 폭과 시야의 폭은 다르다.**
- `열림` **D-21 · 깃대(당간)가 난간 위에 선다** — 목표: **난간벽 캡 위**에 가는 짙은 기둥
  (높이 5~7) · 상단에 **가로대**(붉은/흰) · 거기서 **짙은 남색 배너**가 늘어진다 (흰 원형 문양).
  좌우 각 2~3기, 세로 간격 8~10. 우리: 없음.
- `열림` **D-22 · 석등의 형태** — 목표: 난간 위 **가는 기둥(2~4칸)** + 그 위 **네모 등롱
  1×1 발광 고체**(금빛 창살) + 작은 캡. 우리: `LANTERN` 아이템형이라 형태·부피가 다르다.
- `열림` **D-23 · 계단 양옆이 잔디 비탈** — 목표: 난간 바깥이 **잔디 블록 + 풀·덤불**로
  덮인 흙 비탈. 우리: 돌·자갈이 우세하다. (GREEN 되살릴 때 함께)
- `열림` **D-24 · 난간벽 형태** — 목표: 두께 1~2 · 계단면 위 2~3칸 · **평평한 캡**(반블록)이
  계단을 따라 계단식으로 내려간다. 우리: 여장(성첩 이빨)이라 결이 다르다. (D-04 와 같은 뿌리)

### 문루 (확대 B)

- `열림` **D-25 · 처마 밑 단청 띠** — ★목표 문루의 인상을 가장 크게 좌우한다: 처마 바로 아래
  **금빛·청록 무늬 띠**가 가로로 길게 돌고, **공포(브래킷) 열이 촘촘**하다. 우리: 다크오크
  보 띠만 있어 밋밋하다.
- `열림` **D-26 · 지붕이 회색이다** — 목표: **회색~짙은 회색**(순검정 아님) · 기와 골이 세로로
  촘촘 · 처마 내밈 **3~5칸**. 우리: 거의 검정 · 내밈 2칸. (D-15 와 같은 뿌리, 수치 확정)
- `열림` **D-27 · 벽면이 붉은 격자문으로 채워진다** — 목표: 적주 사이가 **붉은 격자 창호/문짝**
  으로 가득 차고 백벽은 그 사이 좁은 띠뿐. 우리: 백벽이 넓고 창이 작다.
- `열림` **D-28 · 적주 간격 3~4** — 목표 실측. 우리: 3 (되돌린 뒤) — 근접하나 벽 비중이 다르다.

### 성벽·곁채 (확대 C)

- `열림` **D-29 · 곁채가 성벽 위에 올라탄다** — 목표: 성벽이 **기단 노릇**을 하고 그 위에 폭
  7~11 작은 채가 앉는다. 두 채가 나란히 붙어 지붕 골을 이룬다. 우리: 곁채가 없거나 따로 선다.
  (D-08·D-09 와 한 뿌리)
- `열림` **D-30 · 성벽이 낮고 두껍다** — 목표: **높이 4~6 · 두께 2~3** · 위 여장은 한 단
  담장에 가끔 이빨. 우리: 훨씬 높고 여장이 화려하다.
- `열림` **D-31 · 석재가 밝다** — 목표: 성벽·계단이 **밝은 회백~베이지**. 우리: 중간 회색.
  (D-14 의 수치 확정)
- `열림` **D-32 · 매화 수관이 크다** — 목표: 지름 **12~16칸**. 우리: ±4~5 (지름 9~11).

### 본전 (7호 실측 · 2026-08-05)

목표 사진이 1호(산문)에서 7호(캠퍼스 전경)로 넓어졌다. 본전을 같은 자(1블록 = 14px)로 재고
도면 `config/blueprints/hwasan_honjeon.yml` 에 박았다.

| | 목표 실측 (7호) | 우리 |
|---|---|---|
| 하층 몸체 폭 | **38** | 31 (패드가 38×32 라 여백이 없어 비만 지켰다 — **D-33**) |
| 상층 몸체 폭 | **25** (하층의 0.66) | 21 (0.68 — 도면이 물러남 5 를 정한 뒤) |
| 하층 벽 높이 | 3.9 ≈ **4** | 5 (중앙 문이 걸어 지나야 해서 한 칸 — 【절충】) |
| 하층 지붕 두께 | **3** | 3 |
| 상층 벽 높이 | **4** | 4 |

- `지움` **D-34 · 본전 하층이 회벽 지배가 아니었다** — ★★**산문과 본전은 벽의 문법이 반대다.**
  산문은 「기둥+격자 문짝」이 하층을 지배하고(회벽은 좁은 띠), **본전은 하층을 넓은 회벽이
  지배하고 격자문이 점점이 박힌다.** 도면에 회벽 12 : 문짝 6 으로 박았고, 두 도면을 나란히
  놓고 「문법이 서로 반대인가」를 재는 눈을 세웠다 — 누가 둘을 「통일」하면 짖는다.
- `지움` **D-35 · 본전 상층이 회벽이었다** — 목표의 상층은 **격자창이 띠를 이루고 회벽이 없다**
  (하층과 정반대의 강조). 코드가 상층을 늘 회벽으로 채우고 있었다 → 도면이 고르는
  `upper.infill: lattice` 를 만들었다.
- `지움` **D-36 · 상층 물러남이 코드에 2 로 박혀 있었다** — 그 값이면 본전은 31→27(**0.87**)
  이 되어 상층이 하층만큼 넓은 **다른 건물**이 선다. 실측 비는 0.66 — 도면이
  `upper.inset` 을 정하게 하고 본전은 5 를 준다(31→21 = 0.68). 산문은 안 적어 기본 2 로
  그대로 선다(그 눈도 세웠다).
- `열림` **D-33 · 본전이 실측보다 좁다 (38 → 31)** — 본전 패드가 38×32 라 목표 폭을 그대로
  넣으면 여백이 0 이 된다. 하층:상층 비는 지켰다. 패드를 넓히는 것은 **통단 재설계**라
  이 회차 밖 — 다음 회차의 결정거리.
- `열림` **D-37 · 본전 몸체 깊이가 추정치다 (14)** — 7호는 한 방향이라 깊이를 못 잰다.
  폭:깊이 2.2:1 로 잡았다. 다른 각도의 레퍼런스가 생기면 다시 잰다.

## 진척

| 회차 | 지운 항목 | 남은 수 |
|---|---|---|
| (시작) | — | 15 |
| 2026-08-04 계단 | D-03 · D-19 | — |
| 2026-08-04 나무 끄기 | D-16 | — |
| 2026-08-05 축선 | **D-20** | **열림 20** (D-01·02·04·05·06·07·08·09·10·11·12·13·14·15·17·18 + D-21~32 중 신규 12) |
| 2026-08-05 본전 도면 | **D-34 · D-35 · D-36** | **열림 22** (신규 D-33·D-37 둘이 열리고 셋이 닫혔다) |

## ★방법론 — 깊이 추정 → 설계 → 쌓기 → 비교 → 확장 (사용자 확정 2026-08-04)

> 「사진은 한 방향이기에 **정면 샷만을 의도하는 게 아니라 깊이 추정 방식**을 사용해 사진 속
> 깊이를 추정하고, 설계하고, 블록을 쌓고, 비교하고, 늘리는 방식으로 반복.」

1호는 **정면이 아니라 45° 부감(아이소메트릭)**이라 z 방향이 보인다 — 블록 격자가 화면에
드러나므로 **격자를 세어 실제 좌표를 역산**할 수 있다. 그래서 절차는:

1. **깊이 추정** — 사진의 격자를 추적해 각 요소(계단·문루·성벽·곁채·전각·매화)의 **평면
   좌표(x, z)와 높이(y)**를 블록 단위로 역산한다. 산출: **평면 배치도 + 입면 높이표**
   (사진에 안 보이는 뒤쪽은 「추정」으로 표기하고, 대칭·건축 논리로 채운다).
2. **설계** — 그 좌표표를 그대로 명세로 옮긴다 (지금까지처럼 「비슷하게」가 아니라 **좌표로**).
3. **쌓기** — 명세대로 조성.
4. **비교** — ★**목표 사진과 같은 카메라 각도**(45° 부감·같은 높이·같은 거리)로 렌더해
   나란히 놓는다. 정면 컷이 아니라 **사진과 같은 시점**이어야 비교가 성립한다.
5. **확장** — 맞은 부분은 고정하고, 어긋난 부분만 좌표를 고쳐 2로 돌아간다. 한 구역이 맞으면
   이웃 구역으로 **늘려 간다**.

★이 절차의 핵심은 **좌표로 말한다**는 것이다. 「처마가 더 깊어야 한다」가 아니라
「처마가 벽선에서 z+4 까지 나와야 하는데 지금 z+2 다」로.

## ★★★ 계단 폭 확정 — 도보 7 (사용자 실측 2026-08-04)

> 「계단이 너무 넓다. 레퍼런스 사진으로 보니 **계단의 폭이 7칸(도보길만)**이다.」

★이 값은 **슬라이스 8 이전 우리 값(보행 7 + 측석 2 = 9)과 정확히 같다.**
→ **슬라이스 8의 「대계단 20」 척도 교정(×2.2)이 통째로 오류였다.** 그때 「사용자 기준자
20」을 받아 전 캠퍼스를 2배로 키웠는데, 그 20 이 무엇을 잰 값이었든 **레퍼런스의 도보 폭은
7 이다.** 오늘 「20 = 보행 폭」 확인도 이 실측으로 갈음한다 (사용자 재실측이 정본).

**귀결**: 캠퍼스 전체가 목표의 ~2.8배로 커져 있다. 계단부터 되돌리고, 나머지는 한 항목씩.

### 새 차이 (나란히 놓고 발견)

- `지움` **D-16 · 계단이 나무에 가려 안 보인다** — 목표는 계단이 화면 하단 절반을 차지하며
  시선을 문루로 이끈다. **계단 근처에는 나무가 없다** (사용자 지적).
- `열림` **D-17 · 광장이 너무 넓고 비었다** — 목표는 문루 앞이 계단 참 정도로 좁다.
- `열림` **D-18 · 건물 배치가 다르다** (사용자 지적 — 구체는 다음 회차에 목록화)
- `지움` **D-19 · 계단 폭 20 → 7 (도보)** — 위 확정.

## 규칙

1. 매 회차 **목표 사진과 같은 구도**로 촬영해 나란히 놓고 판정한다.
2. 목록에 없는 것은 고치지 않는다 (표류 방지). 새로 발견하면 **목록에 추가하고** 나서 고친다.
3. 「지움」은 사용자 확인으로만 확정한다 — AI 판정은 참고다 (2026-08-04 실증: AI 둘이 함께 빗나갔다).

---

## 본전 도면 실물 판정 (2026-08-05)

`run/stage_render/honjeon_1정면.png` · `honjeon_2사선.png`

**성립**: 상층 물러남(비 0.68)이 실루엣에 반영됐다 — 코드에 박혀 있던 2(비 0.87)를 도면이
5로 바로잡은 것이 실물로 확인된다. 하층 벽/상층 창의 대비도 도면대로 섰다.

**새 차이**:
- `열림` **D-34 · 하층 회벽이 목표보다 넓다** — 목표 7호는 상하 **모두** 붉은 기둥+격자가
  촘촘하고 회벽은 거의 안 보인다. 도면의 「하층은 넓은 회벽」 실측이 다른 각도/건물을 본
  결과일 수 있다 — **7호를 다시 재서 도면을 고쳐야 한다.**
- `열림` **D-35 · 망루가 본전보다 눈에 먼저 든다** — 망루 3층이 통째로 흰 판이라 위계가
  밀린다. 본전이 중심이어야 한다.
- `열림` **D-36 · 본전 처마가 벽을 덮는다** — 정면에서 지붕이 몸체를 거의 가린다. 목표는
  처마 아래로 붉은 기둥열이 충분히 드러난다.
- `열림` **D-33 · 본전이 실측(38)보다 좁다(31)** — 패드 38×32라 여백이 0이 된다.
  패드를 넓히려면 통단 재설계 (사용자 결정거리).

---

## D-34·35·36 재실측과 처방 (2026-08-05)

### 7호 재측정 — 앞 회차 결론이 틀렸다

측정 대상 `화산파/…07_09_26 (7).png` (1블록 = 14px · 자는 계단 도보 7칸으로 검산).
화소를 채도·명도로 갈라 셌다 (붉은색 S≥45 & 색상 <25°/>340° · 회벽 S<28 & V≥40).

| 부위 | 붉은색 | 회벽 | 나무 | 어둠 |
|---|---|---|---|---|
| 본전 하층 | **46.8%** | **9.4%** | 15.5% | 23.2% |
| 본전 상층 | 33.8% | 8.1% | 8.8% | 33.2% |
| 산문 하층 (대조) | 26.3% | 4.3% | 32.9% | 27.6% |

- **적주 주기 = 3칸.** 깨끗한 붉은 기둥 x509-518 · 552-562 · 599-608 · 646-653 · 915-923
  (폭 ≈10px ≈ 1블록), 틈 33/36/37px, 주기 ≈45px = 3블록. **산문과 같다.**
- **회벽은 소수다** (9.4%). 앞 회차의 「회벽이 2, 문이 1」과 정반대.
- **★빈 회벽 면이 없다** — 목표의 모든 칸은 회벽 판 **안에 격자창이 박혀** 있다.

**앞 회차가 왜 빗나갔나**: 첫 분류자가 `회벽 = S<18` 이었는데 실제 회벽은 S≈22 라
회벽이 어디에도 0% 로 나왔고, 그 다음 눈대중이 「하층은 넓은 회벽」으로 메웠다.
표본 줄도 문/그늘 띠(y455-505)에 앉아 있었다. **분류자를 못 박기 전의 화소 판정은 판정이 아니다.**

### 처방

- **D-34** — 진범은 회벽의 **양**보다 **통짜 5켜 빈 판**이었다. 도면 평면을 고쳤다:
  적주 주기 4→**3** (col 4·7·10·13·16 | 대문 17~21 | 22·25·28·31·34), 칸은 언제나 `W D`
  한 짝이고 축선 기준으로 뒤집힌다(좌 `WD` · 우 `DW`) → **회벽 두 켜열이 이웃하는 자리가
  한 곳도 없다.** 회벽 12켜열 → 8, 격자 6 → 8.
- **D-36** — 진범은 **오프바이원**이다. 앞 회차가 `base_y` 를 「벽 5 + 도리·단청 2 = 7」로
  셈하며 기둥 처방 맨 아래 **기단 켜(smooth_stone)를 빼먹었다.** 실제 켜열은 8 이라
  처마 첫 켜가 단청 띠(y7)를 그대로 덮었다 → `base_y: 8`.
  ★곁가지로 드러난 것: `roof.eave` 는 **읽히지 않는 죽은 값**이었다 (`sweepRoof` 가
  `over=3` 을 박아 두어 도면에 무엇을 적든 결과가 같았다). 배선을 잇고 실측대로 `eave: 2`.
- **D-35** — 진범은 높이가 아니라 **흰 면의 넓이**다. 망루 네 면이 통짜 회벽이라 세 층 합쳐
  ≈372블록, 본전 정면(40)보다 훨씬 넓은 흰 덩어리였다. 높이를 깎으면 「절벽 망루」(9호)가
  죽으므로 높이는 그대로 두고 본전·산문과 **같은 문법**(3칸 주기 적주 + 격자창)으로 쪼갰다.
  한 면 흰 칸 **93 → 30** (본전 정면 40보다 좁다) · 전체 회벽 372/424 → **120/424**.

세 항목 모두 **「고침 — 사용자 확인 대기」**다. 규칙 3에 따라 「지움」은 사용자가 실물을
보고 정한다.

### 눈 (tools/TerraceForgeSelfTest.java) — 197/197

새 눈 10종. **변이 8건 전부 짖었다**:
base_y 8→7 · eave 2→3 · `rf.eave()` 배선 끊기 · 앵커 깨기(컴파일 유지) ·
회벽 두 켜열 붙이기 · 주기 3→4 · 망루 통짜 회벽 복원 — 그리고 앵커를 깨자
「눈의 눈」이 조용히 통과하지 않고 짖었다.

★여기서 **틀린 것을 지키던 눈 둘을 뜯어냈다** (「본전 하층은 회벽이 지배한다」·
「산문과 본전의 문법이 서로 반대다」). 눈이 틀린 결론을 지키면 **고치려는 손을 눈이 막는다.**
망루 눈은 소스 글자를 뒤지지 않고 조성이 쓰는 그 함수(`HwasanCampusBuilder.towerWall`)를
불러 흰 칸을 **실제로 센다** — 글자를 뒤지는 눈은 글자가 바뀌는 순간 조용히 아무것도 안 잰다.

### 교정 실물 판정 — fix3 (2026-08-05 저녁)

- **D-35 망루 위계 — 성립** (사용자 확인 대기): 통짜 흰 판 3층이 사라지고 층마다 붉은 기둥과
  격자가 갈라 섰다. 본전이 다시 중심으로 읽힌다.
- **D-36 처마가 벽을 덮음 — 성립** (사용자 확인 대기): 하층 처마 아래로 붉은 기둥열과 크림빛
  단청 띠가 드러난다 (base_y 오프바이원 수리가 실물에 반영).
- **D-34 하층 회벽 — 부분**: 흰 면이 통짜로 이어지던 것은 사라졌으나 **크림 면적이 아직 넓다**.
  목표(붉은 46.8% · 회벽 9.4%)에 못 미친다 — 다음 손잡이는 **회벽 칸 자체를 붉은 문짝으로 더
  바꾸는 것**.
- `열림` **D-38 · 본전 상층도 크림색이 넓다** — 사선 컷에서 드러났다. D-34 와 같은 처방 필요.
